# سیستم معاملاتی Xmoon + HelpMe
## مستند جامع — نسخهٔ ۵.۰ — جولای ۲۰۲۶
### گردآوری، تکمیل و بازنویسی نهایی پس از ۵ تحلیل مستقل و ۷ پچ — v14.20

---

> **نویسندهٔ سیستم:** علی نادری
> **همکاران تحلیل:** DeepSeek, Claude Sonnet, GPT-4o, Claude Sonnet (Max), GLM 5.2
> **آخرین نسخهٔ اکسپرت:** HelpMe v14.20 — ۱۸,۳۲۵ خط MQL5
> **وضعیت:** آمادهٔ لایو برای GBPNZD

---

# بخش اول: پیدایش

## ۱. از کجا شروع شد — فلسفه و دغدغهٔ اصلی

سال‌ها معاملهٔ دستی، بکتست‌های متعدد، و یک سؤال ساده که ذهنم را رها نمی‌کرد:

> **چرا ۹۷٪ معاملات با سود بسته می‌شوند، ولی همان ۳٪ کل حساب را صفر می‌کنند؟**

پاسخ این سؤال در ذات روش معاملاتی نهفته بود. روشی که انتخاب کرده بودم — **Mean-Reversion با مارتینگل معکوس** — یک نقطه‌ضعف مادرزاد دارد: در بازار رنج، فوق‌العاده سودده است، اما وقتی یک روند قوی و پایدار شکل می‌گیرد و بازار از بازگشت به میانگین سر باز می‌زند، پله‌های معامله یکی‌یکی فعال می‌شوند تا جایی که کل بالانس تمام می‌شود.

پس مسئله این نبود که «چگونه سود کنیم؟» — سیستم Xmoon این کار را بلد بود. مسئله این بود: **«چگونه بفهمیم که این یک روند واقعی است و بازار قرار نیست برگردد؟»**

اینگونه بود که ایدهٔ HelpMe متولد شد: نه یک ربات معامله‌گر، بلکه یک **اتاق فرمان** که بالای سر Xmoon بنشیند، بازار را زیر نظر بگیرد، و بگوید: «الان وقت ورود نیست»، «الان وقت خروج است»، «الان می‌توانی برگردی».

---

## ۲. تاریخچهٔ پروژه — از سقوط AUDCAD تا تولد HelpMe

### ۲.۱ دوران AUDCAD (اوایل ۲۰۲۶)

همه چیز با **AUDCAD** شروع شد. یک جفت‌ارز که در سال‌های ۲۰۲۴ و ۲۰۲۵ رفتاری کاملاً رنج داشت — مناسب برای Xmoon. ماه‌ها سود داد، حساب رشد کرد، و همه چیز خوب بود.

تا اینکه **۲۰۲۶** از راه رسید.

شرایط بازار جهانی تغییر کرد: **ریسک‌گریزی (Risk-Off)** شدت گرفت. AUDCAD که سال‌ها در یک رنج آرام نوسان می‌کرد، ناگهان تبدیل به یک ارز ترنددار شد. یک پوزیشن Sell در کف‌ترین نقطهٔ ممکن باز شد — و قیمت **هرگز برنگشت.**

**نتیجه:** حساب ۵۰ میلیون تومانی — کال کامل.

### ۲.۲ درس بزرگ اول

> **هیچ ارزی برای همیشه رنج نمی‌ماند.**

این جمله را روی دیوار ذهنم حک کردم. فهمیدم که مشکل از Xmoon نیست — Xmoon دقیقاً همان کاری را می‌کند که برایش طراحی شده. مشکل این است که **Xmoon روند را نمی‌فهمد.** فقط الگوی 3Drive Divergence را می‌بیند. اگر یک روند ۶۰۰۰ پوینتی شکل بگیرد، Xmoon همچنان سیگنال خلاف روند می‌دهد — چون این کارش است.

سیستم به **چشم** نیاز داشت. به یک نگهبان.

### ۲.۳ تولد HelpMe و انتخاب ارزهای جدید

پس از کال AUDCAD، همه چیز را از نو ساختم:

- **ارز اصلی:** GBPNZD — نوسانی، پررفتوآمد، با بازگشت‌های مکرر به میانگین. ایده‌آل برای Mean-Reversion.
- **ارز پشتیبان:** EURGBP — بسیار رنج، پایدار، با کمترین ریسک روندهای بلندمدت (هر دو طرف اروپایی هستند و از شوک‌های NZD/AUD مصون می‌مانند).
- **AUDCAD** را هم کنار نگذاشتم — قرار شد بعد از کالیبراسیون مجدد، در آینده برگردد (که در v14.19 این کار انجام شد).

و مهم‌تر از همه: ساخت **HelpMe** آغاز شد.

---

## ۳. محیط معاملاتی

| پارامتر | مقدار |
|---|---|
| بروکر | LiteFinance |
| نوع حساب | Cent |
| اهرم | ۱:۱۰۰۰ |
| سرمایهٔ ورودی | ۴۰ دلار (۴۰۰۰ سنت) |
| تایم‌فریم اصلی | M1 (یک دقیقه) |
| سبک معاملاتی | اسکالپ — تعداد معاملات بسیار زیاد |
| زوج ارزهای فعال | GBPNZD (اصلی) + EURGBP (پشتیبان) + AUDCAD (آماده برای آینده) |
| حجم پایه | ۴.۵ لات سنتی (۵ پله) |
| TP Extended | فعال |

### ۳.۱ چرا حساب Cent؟

در حساب Cent هر لات = ۰.۰۱ لات استاندارد. با اهرم ۱:۱۰۰۰ و سرمایهٔ ۴۰ دلار، می‌توان پله‌های مارتینگل را با حجم واقعی آزمایش کرد — بدون ریسک سرمایهٔ کلان. این حساب مانند یک شبیه‌ساز واقعی با پول کم عمل می‌کند.

### ۳.۲ چرا ۴۰ دلار — نه ۶۰؟

تحلیل تمام بکتست‌ها با ۴۰۰۰ سنت نشان داد که هر بار کال اتفاق افتاده، میزان ضرر بین ۲,۹۸۷ تا ۳۸,۶۶۰ سنت بوده. یعنی ۲۰۰۰ سنت اضافه (از شروع با ۶۰$) **در هیچ‌یک از کال‌ها** نجات‌دهنده نبود. اما ۶۰$ یک مزیت روان‌شناختی دارد: در روزهای اول که GBPNZD drawdown می‌خورد، حساب زودتر ریکاوری می‌کند. از طرف دیگر، اگر بازنده شوی، ۵۰٪ بیشتر باخته‌ای. تصمیم نهایی: **۴۰ دلار — دقیقاً همان شرایط بکتست.** اگر تجربهٔ لایو نشان داد که drawdown اولیه ناراحت‌کننده است، می‌توان به ۵۰$ رفت — ولی نه بیشتر.

### ۳.۳ چرا M1؟

هرچه تایم‌فریم کوچک‌تر، سیگنال‌های 3Drive Divergence بیشتر = معاملات بیشتر = سود بیشتر در بازار رنج. بکتست نشان داد که بردن معامله به M5 یا فیلتر کردن ساعات، تعداد معاملات را کم می‌کند و نهایتاً سود کل پایین می‌آید. M1 انتخاب شده و بر آن می‌مانیم.

---

# بخش دوم: موتور معامله — Xmoon

## ۴. Xmoon چیست؟

Xmoon یک **اکسپرت معامله‌گر تخصصی** است که صفر تا صد کار معامله را انجام می‌دهد. دخالت دستی در آن وجود ندارد.

### ۴.۱ وظایف اصلی Xmoon

| وظیفه | توضیح |
|---|---|
| یافتن سیگنال | تشخیص الگوی **3Drive Divergence** روی تایم‌فریم M1 |
| باز کردن معامله | پلهٔ اول به‌صورت **Market Order** |
| مدیریت پله‌ها | پله‌های ۲ تا ۵ به‌صورت **Limit Order** با فواصل مشخص |
| تنظیم TP | هر بار که پلهٔ جدید فعال می‌شود، TP دینامیک آپدیت می‌شود |
| بستن معامله | وقتی TP زده شد، همهٔ پوزیشن‌ها یکجا بسته می‌شوند |
| جلوگیری از ورود مضاعف | تا یک Buy باز است، Buy جدید باز نمی‌کند (همین برای Sell) |

### ۴.۲ منطق 3Drive Divergence

این الگو معمولاً **خلاف روند کلی** سیگنال می‌دهد. یعنی اگر بازار نزولی باشد، Xmoon سیگنال **Buy** می‌دهد — به امید اینکه یک Pullback یا برگشت کوچک بیاید و TP زده شود.

این دقیقاً همان چیزی است که این روش را:
- **در بازار رنج:** فوق‌العاده قوی می‌کند.
- **در روند قوی:** بسیار آسیب‌پذیر می‌کند.

و اینجاست که HelpMe وارد میدان می‌شود.

---

## ۵. مفهوم Base — قلب سیستم

**Base = فاصلهٔ بین پلهٔ ۱ و پلهٔ ۲.**

این مهم‌ترین پارامتر Xmoon است، زیرا:

- **شدت حرکت خلاف** را اندازه‌گیری می‌کند.
- **فاصلهٔ تمام TPها** از Base محاسبه می‌شود.
- **فاصلهٔ پله‌های بعدی** مضربی از Base است (۱.۲x, ۱.۴x, ۱.۶x).
- **میزان ریسک کل معامله** را مشخص می‌کند.

### ۵.۱ اثر الاکلنگی Base

| حالت | مزیت | معایب |
|---|---|---|
| **Base بزرگ** | امنیت بیشتر — پله‌ها دیرتر فعال می‌شوند | سود کمتر — TP دورتر است |
| **Base کوچک** | سود بیشتر — TP نزدیک‌تر است | ریسک بیشتر — پله‌ها زودتر فعال می‌شوند |

Base به‌صورت تجربی و بر اساس بکتست تنظیم شده و مقدار بهینهٔ آن **حدود ۷۰ پوینت** برای GBPNZD است.

---

## ۶. ساختار پله‌ها — Reverse Martingale

سیستم از **مارتینگل معکوس** با ۵ پله استفاده می‌کند. حجم پله‌های بعدی **بیشتر** از پله‌های قبلی است، تا سود پلهٔ جدید بتواند ضرر پله‌های قبلی را جبران کند.

| پله | نوع سفارش | حجم (لات سنتی) | فاصله از پلهٔ قبل |
|---|---|---|---|
| ۱ | Market | ۱.۰ | — ورود اولیه |
| ۲ | Limit | ۲.۰ | Base × ۱.۰ |
| ۳ | Limit | ۳.۰ | Base × ۱.۲ |
| ۴ | Limit | ۴.۰ | Base × ۱.۴ |
| ۵ | Limit | ۵.۰ | Base × ۱.۶ |
| **مجموع** | | **۱۵.۰** | |

> اگر حجم کل دو برابر شود (مثلاً ۳۰ لات سنتی)، هر پله هم دو برابر می‌شود — ساختار ثابت می‌ماند.

### ۶.۱ توزیع پله‌ها در بکتست واقعی (۴۳۰ معامله، GBPNZD+EURGBP)

| پله | تعداد | درصد | میانگین سود |
|---|---|---|---|
| ۱ | ۱۱۳ | ۲۶٪ | ۶۴ سنت |
| ۲ | ۱۴۲ | ۳۳٪ | ۱۰۹ سنت |
| ۳ | ۷۸ | ۱۸٪ | ۲۳۶ سنت |
| ۴ | ۴۲ | ۱۰٪ | ۳۷۹ سنت |
| ۵ | ۵۵ | ۱۳٪ | ۵۹۸ سنت |

پلهٔ ۵ در ۱۳٪ معاملات فعال می‌شود. از ۵۵ معاملهٔ پله‌۵، **تنها یک بار** منجر به کال شد — اما همان یک بار **۱۱,۶۷۷ سنت ضرر** زد.

### ۶.۲ خطر پلهٔ ۵

وقتی پلهٔ ۵ فعال می‌شود، قیمت فاصلهٔ زیادی از نقطهٔ ورود گرفته. حتی اگر کمی برگردد، TP که بسیار دورتر است ممکن است هرگز زده نشود. اینجاست که **HelpMe باید تصمیم کمکی بدهد** — و این دقیقاً جایی است که قانون CLOSE وارد عمل می‌شود.

---

## ۷. TPهای دینامیک و TP Extended

### ۷.۱ فرمول محاسبهٔ TP

TP بر اساس نقطهٔ **Risk-Free** محاسبه می‌شود — نقطه‌ای که اگر همهٔ پوزیشن‌ها همان‌جا بسته شوند، سود و زیان صفر است:

| مرحله | فاصلهٔ TP از Risk-Free |
|---|---|
| فقط پلهٔ ۱ فعال | ۵ × Base |
| پلهٔ ۲ فعال شد | ۲ × Base |
| پلهٔ ۳ فعال شد | ۲ × Base |
| پلهٔ ۴ فعال شد | ۲ × Base |
| پلهٔ ۵ فعال شد | ۴ × Base (آخرین امید) |

### ۷.۲ TP Extended

TP Extended در لحظهٔ بسته‌شدن معامله فعال می‌شود: اگر روند در تایم‌فریم M15 موافق پوزیشن باشد، TP را بزرگ‌تر می‌کند:

- **پلهٔ اول:** TP اصلی + ۲× Base → مجموعاً ۴× Base
- **پله‌های ۲ تا ۵:** TP اصلی + ۱× Base → یک پله بزرگ‌تر

### ۷.۳ نتایج بکتست: Extend vs No-Extend

| شاخص | Extend TP | No Extend TP |
|---|---|---|
| تعداد معاملات | ۴۳۰ | ۶۲۰ |
| نرخ برد | ۹۸.۶٪ | ۹۹.۲٪ |
| میانگین سود هر معامله | ۲۰۵ سنت | ۱۳۳ سنت |
| سود میانگین پلهٔ ۵ | ۶۲۳ سنت | ۴۴۱ سنت |
| تعداد کال | ۲ | ۲ |
| **ROI نهایی** | **۲۲۶٪** | **۱۹۰٪** |

**تصمیم نهایی: TP Extended فعال است.** با ۳۶٪ ROI بیشتر، معاملات کمتر ولی پرسودتر.

---

## ۸. معاملهٔ همزمان

Xmoon می‌تواند **یک Buy و یک Sell** را هم‌زمان باز داشته باشد:

- تا Buy بسته نشود، Buy جدید باز نمی‌شود.
- تا Sell بسته نشود، Sell جدید باز نمی‌شود.
- حداکثر ۲ معاملهٔ هم‌زمان — هرکدام تا ۵ پله.
- **بدترین سناریو:** ۵ پله Buy + ۵ پله Sell = ۱۰ پوزیشن باز هم‌زمان.

**Stop New Signal** (جلوگیری از ورود جدید وقتی پله به حدی رسیده) در بکتست خاموش بوده و خاموش می‌ماند — مدیریت دستی توسط HelpMe جایگزین آن شده است.

---

## ۹. مدیریت سرمایه و چرخهٔ رشد

### ۹.۱ برنامهٔ برداشت

| بالانس رسید به | اقدام | بالانس بعد از برداشت |
|---|---|---|
| ۸۰$ | برداشت ۲۰$ | ۶۰$ |
| ۱۰۰$ | برداشت ۴۰$ | ۶۰$ |
| ۱۲۰$ | برداشت ۲۰$ | ۱۰۰$ |
| ۱۴۰$ | برداشت ۲۰$ | ۱۲۰$ |
| ... | هر ۲۰$ رشد → ۲۰$ برداشت | ادامهٔ رشد |

### ۹.۲ منطق این سیستم

- **هرگز تمام سود در حساب نمی‌ماند** — بخشی برداشت می‌شود.
- **رشد از طریق افزایش حجم** اتفاق می‌افتد، نه افزایش ریسک.
- **وقتی کال شویم** (که بالاخره اتفاق می‌افتد)، سرمایه‌ای را از دست می‌دهیم که بیش از نصفش قبلاً خارج شده است.

### ۹.۳ چرا این مانی منیجمنت؟

بکتست نشان داد که:
- فیلتر کردن ساعات شب → سود کمتر
- معامله فقط هم‌جهت روند → سود کمتر
- استاپ لاس گذاشتن → سود کمتر

بازار همیشه یک روی باخت دارد. هر فیلتری که به Xmoon بزنیم، بیش از آنکه ضرر را کم کند، سود را هم کم می‌کند. **روش را عوض نمی‌کنیم — مانی منیجمنت را درست می‌کنیم.**

---

## ۱۰. حالت تهاجمی — قمار کنترل‌شده

این یک حالت **اختیاری و کاملاً آگاهانه** است، نه بخشی از استراتژی اصلی.

### ۱۰.۱ ایده

در شرایط بسیار مطلوب (۱۵ روز اول ماه در بازار رنج، Rule=Run، همهٔ چراغ‌ها سبز)، حجم را از ۴.۵ به ۱۵ لات افزایش دهیم:
- تا پلهٔ ۶: لیکوییدیشن
- اگر شانس بیاوریم و چند معاملهٔ پلهٔ ۵ موفق داشته باشیم: **سود ۶ ماه در ۲ هفته**

### ۱۰.۲ شرایط استفاده

۱. ۱۵ روز اول از یک چرخهٔ جدید (بعد از ریست/شروع تازه)
۲. بازار در این ۱۵ روز کاملاً رنجی — Rule = RUN بدون هیچ تهدید
۳. تمام ۱۰ چراغ HelpMe سبز یا زرد
۴. **سرمایهٔ جدا از حساب اصلی** — این ۴۰$ اضافه را از قبل «باخته» حساب کن
۵. لحظه‌ای که Rule به STOP رفت → فوراً برگرد به حجم ۴.۵

---

## ۱۱. زوج ارزها — تاریخچه، بکتست، و تصمیم نهایی

### ۱۱.۱ AUDCAD — زوج اولیه (کنار گذاشته شد)

| دوره | وضعیت |
|---|---|
| ۲۰۲۴–۲۰۲۵ | کاملاً رنج — سودده و پایدار |
| ۲۰۲۶ | ترنددار شد — ریسک‌گریزی جهانی |
| **نتیجه** | یک Sell در کف بازار باز شد و هرگز برنگشت — کال کامل |

### ۱۱.۲ GBPNZD — زوج اصلی فعلی

| ویژگی | توضیح |
|---|---|
| رفتار | نوسانی و رفت‌وبرگشتی |
| نقش | **موتور اصلی سوددهی** |
| ریسک | دو روند شارپ ۶۲۰۰ پوینتی در ۲۰۲۶ |
| نتیجه | نیازمند مراقبت ویژه با HelpMe — **و HelpMe آماده است** |

### ۱۱.۳ EURGBP — زوج پشتیبان

| ویژگی | توضیح |
|---|---|
| رفتار | بسیار رنج — **پایدارترین ارز تست‌شده** |
| نقش | تقویت‌کنندهٔ بالانس — سود منظم و آرام |
| ریسک | یک پوزیشن در مارس ۲۰۲۶ تا ژوئن باز ماند (۲۵۷۹ ساعت) |
| مزیت استراتژیک | هر دو طرف اروپایی — مصون از شوک‌های NZD/AUD |

### ۱۱.۴ AUDCAD — بازگشت در v14.19

پس از کالیبراسیون کامل با اجماع ۷ تحلیل مستقل، AUDCAD از نسخهٔ ۱۴.۱۹ دوباره به سیستم اضافه شده — ولی با احتیاط. این ارز هنوز در مرحلهٔ مانیتورینگ است و تا تأیید نهایی در Forward Test، معاملهٔ واقعی روی آن انجام نمی‌شود.

---

## ۱۲. نتایج بکتست ۲۰۲۶ — تحلیل کامل

### ۱۲.۱ شرایط بکتست

- سرمایه: ۴۰$ = ۴۰۰۰ سنت
- حجم: ۴.۵ لات سنتی (۵ پله)
- بروکر: LiteFinance — Cent — اهرم ۱:۱۰۰۰
- نوع: Every tick based on real tick5
- زمان: تمام روزهای هفته، تمام ساعات (بدون فیلتر)
- HelpMe تأثیری روی بکتست نداشته (صرفاً تحلیلی)
- هر کال → ردیف بعدی با ۴۰$ جدید

### ۱۲.۲ جدول نتایج کامل

| جفت ارز | سرمایهٔ کل | نتیجهٔ نهایی | سود خالص | ROI | تعداد کال | تعداد معاملات |
|---|---|---|---|---|---|---|
| **GBPNZD+EURGBP (Extend TP)** | ۱۲۰$ | ۳۹۱$ | +۲۷۱$ | **۲۲۶٪** | ۲ | ۴۳۰ |
| GBPNZD+EURGBP (No Extend) | ۱۲۰$ | ۳۴۸$ | +۲۲۸$ | ۱۹۰٪ | ۲ | ۶۲۰ |
| GBPNZD (تنها) | ۱۲۰$ | ۴۷۷$ | +۳۵۷$ | ۲۹۸٪ | ۲ | ۴۸۱ |
| **EURGBP (تنها)** | ۴۰$ | ۷۲$ | +۳۲$ | **۸۱٪** | **۰** | ۱۱۵ |
| EURNZD | ۱۲۰$ | ۲۲۵$ | +۱۰۵$ | ۸۷٪ | ۲ | ۴۳۳ |
| AUDNZD | ۲۰۰$ | ۷۰$ | −۱۳۰$ | −۶۵٪ | ۴ | ۲۰۳ |
| EURCHF | ۸۰$ | ۱۱۴$ | +۳۴$ | ۴۲٪ | ۱ | ۱۶۰ |
| AUDCHF | ۴۰$ | ۵۸$ | +۱۸$ | ۴۵٪ | ۰ | ۱۸۸ |

### ۱۲.۳ تفسیر نتایج

- **ترکیب GBPNZD+EURGBP با Extend TP بهترین تعادل** بین سوددهی (۲۲۶٪) و ثبات روانی را دارد. EURGBP در دوره‌هایی که GBPNZD گیر کرده، همچنان سود می‌دهد — این Buffer روانی ارزشمند است.
- **EURGBP تنها** با ۸۱٪ ROI و **صفر کال**، امن‌ترین گزینه است ولی سود کمتری دارد.
- **AUDNZD** با ۴ کال و ROI منفی ۶۵٪، نماد یک فاجعه است — و دقیقاً نشان می‌دهد چرا انتخاب ارز حیاتی است.

---

## ۱۳. تحلیل فاندامنتال بحران‌های ۲۰۲۶

### ۱۳.۱ بحران اول — ۱۵ ژانویه ۲۰۲۶: «سونامی NZD»

**علت:** NZD یک حرکت قوی صعودی داشت (احتمالاً RBNZ هاوکیش، دادهٔ اقتصادی قوی، یا Risk-On global).

**تأثیر:**
- GBPNZD 🔴 کال (۱۱۱ ساعت)
- EURNZD 🔴 کال (۱۱۷ ساعت)
- AUDNZD 🔴 کال (۱۰۴ ساعت)
- EURGBP ✅ سالم ماند

**درس:** وقتی NZD حرکت بزرگ می‌کند، **همهٔ ارزهای NZD-based همزمان** کال می‌خورند. Flow چراغ این هم‌زمانی را تشخیص می‌دهد.

### ۱۳.۲ بحران دوم — فوریه ۲۰۲۶: «ضعف AUD + قدرت CHF»

**علت:** AUD ضعیف (China slowdown) + CHF قوی (safe-haven). Xmoon در اوج CHF و کف AUD سیگنال Long روی AUDCHF داد — **۱۳۲ روز طول کشید!**

**درس:** GBPNZD و EURGBP در همین دوره سالم بودند — انتخاب ارز، همه چیز است.

### ۱۳.۳ بحران سوم — ژوئن ۲۰۲۶: «دومین موج NZD»

**علت:** GBP ضعیف + NZD قوی. یک Long GBPNZD از ۱ ژوئن باز شد و **۲۵ روز بعد** کال خورد. حساب از ۷۰,۶۹۰ سنت به نزدیک صفر رسید.

**درس:** حتی با بالانس ۷ برابر شروع، یک روند بد می‌تواند همه را ببرد. اگر HelpMe در آن زمان وجود داشت، قانون CLOSE (Crisis قرمز برای ۶ ساعت متوالی) ضرر ۳۸,۶۶۰ سنتی را به ۳,۰۰۰–۵,۰۰۰ سنت کاهش می‌داد.

---

# بخش سوم: تولد و تکامل HelpMe

## ۱۴. چرا HelpMe ساخته شد — فلسفهٔ نگهبان

Xmoon یک مشکل اساسی دارد: **روند را نمی‌فهمد.**

فقط الگوی 3Drive را می‌بیند و بر اساس آن سیگنال می‌دهد. اگر یک روند شارپ ۶۰۰۰ پوینتی شروع شود، Xmoon همچنان سیگنال خلاف روند می‌دهد، پله‌ها را فعال می‌کند، و به سمت کال پیش می‌رود — بی‌خبر از اینکه بازار دیگر «رنج» نیست.

### ۱۴.۱ سه سؤال اساسی HelpMe

```
۱. آیا الان وضعیت برای ورود جدید خطرناک است؟   →  STOP NEW SIGNAL
۲. آیا معاملهٔ باز باید بسته شود؟               →  CLOSE WITH LOSS
۳. آیا وضعیت به حالت عادی برگشته؟               →  RESTART
```

HelpMe یک **اکسپرت دستیار** است، نه معامله‌گر. خودش هیچ معامله‌ای باز یا بسته نمی‌کند. فقط **می‌گوید** چه زمانی باید Xmoon را متوقف کرد، چه زمانی باید با ضرر خارج شد، و چه زمانی می‌توان دوباره شروع کرد.

---

## ۱۵. تاریخچهٔ توسعه — از v1 تا v14.20

این پروژه حاصل **۶ ماه تلاش شبانه‌روزی یک‌نفره** است. تمام کدنویسی با کمک مدل‌های هوش مصنوعی (Claude, DeepSeek, Gemini, GLM, Qwen, GPT-4o, Codex, Grok, Kimi) انجام شده، با مدیریت و خلاقیت شخصی. نکتهٔ قابل توجه: حتی یک خط از این کد را یک برنامه‌نویس حرفه‌ای ننوشته — همه با AI و با هدایت یک «متفکر» ساخته شده.

### ۱۵.۱ جدول تکامل نسخه‌ها — از v1 تا v14.20

#### v1.0 (اوایل ۲۰۲۴) — نسخهٔ پایه
اولین نسخهٔ HelpMe — یک **اندیکاتور** ترکیبی (نه EA) با سیستم سیگنال‌دهی مبتنی بر فلش‌های Buy/Sell در سه سطح قدرت (Weak/Medium/Strong). موتور امتیازدهی با حداقل نمره، داشبورد اطلاعاتی (Gate Monitor، Session Info، Stats)، سیستم Preset خودکار برای تایم‌فریم‌های مختلف (M1 تا H1 و Custom)، و Filter Mode سه‌گانه (Relaxed/Balanced/Strict). فیلترهای پایه (RSI, ADX, Bollinger Bands, ATR Intensity, Wick Filter) و نمایش خطوط خبری عمودی با رنگ‌بندی High/Medium/Low Impact.

#### v2.0 (اوایل ۲۰۲۴) — هوش مصنوعی و فیلترهای پیشرفته
افزودن گروه AI Settings با تاگل‌های قابل کنترل: Market Regime Detection (تشخیص Trending/Ranging/Volatile/Quiet با Efficiency Ratio و Hurst Exponent)، Smart News Filter، Multi-Timeframe Confluence (تأیید H1 و H4 با EMA 200)، و Price Action Patterns (PinBar، Engulfing، Doji، Hammer). داشبورد پیشرفته‌تر با نمایش Regime Info و MTF Trend.

#### v3.0 (اواسط ۲۰۲۴) — بهینه‌سازی و پاکسازی
حذف AI Learning ناپایدار، اضافه شدن جدول کامل سیمبل‌ها با max_percent برای بیش از ۴۰ جفت‌ارز، بهبود News System با ادغام تکراری‌های همزمان و tooltip چندخبره، تنظیمات بصری خبرها در Inputs، و یکپارچه‌سازی BaseRequiredScore برای Buy و Sell.

#### v4.0 (اواخر ۲۰۲۴) — Smart Volume و بهینه‌سازی
اضافه شدن Smart Volume Filter (مقایسه حجم Tick با میانگین — آستانه ۶۰٪)، بهبود سیستم Alert با کنترل جداگانه برای خبرها، و بهبود پارس کردن اعداد خبری.

#### v5.0 (اوایل ۲۰۲۵) — تبدیل به Expert Advisor
**تغییر بنیادین معماری:** تبدیل از Indicator به Expert Advisor (EA) با OnTick/OnInit/OnDeinit و دکمه‌های داشبورد کلیک‌پذیر (OnChartEvent). رفع باگ لیبل خبری (زاویه ۹۰ درجه، خوانایی بهتر)، و جداسازی SmartVolumeThresholdPct به عنوان input مستقل.

#### v6.0 (اواسط ۲۰۲۵) — فیلترهای نسل جدید
اضافه شدن **FVG (Fair Value Gap)**، **LiqSwp (Liquidity Sweep)** و **RTM (Return to Mean)** به عنوان دکمه‌های ردیف سوم داشبورد. بهبود VolPro با scoring برای momentum volume، و یکپارچگی با Xmoon: هشدار RTM Danger، سیگنال LiqSwp تازه برای TP، و هشدار FVG خلاف معامله.

#### v7.0 (اواخر ۲۰۲۵) — Traffic Lights و S/R Levels
**بازنویسی بنیادین سیستم هشدار:** اضافه شدن سه چراغ اصلی (RTM، ADX، STRUCT) مستقل از تایم‌فریم چارت — فقط وقتی پوزیشن باز است رنگ می‌گیرند. RTM فاصله از EMA200 روی H1 را می‌سنجد، ADX قدرت روند روی H4 را، و STRUCT یکپارچگی ساختار D1 را. **S/R Levels** همیشه بر اساس D1 (مستقل از TF جاری) با swingها و clusterThreshold از ATR_D1.

#### v7.2 (اواخر ۲۰۲۵) — رفع باگ‌های حیاتی
**بازنویسی منطق چراغ RTM:** کشف از تحلیل بکتست یک‌ساله AUDCAD — حالت Counter-Trend با عدم برگشت = ۱۰۰٪ معاملات فاجعه‌بار. منطق جدید: سبز = کشیده + در حال برگشت، قرمز = CT+دور شدن یا ثابت. بهبود خروجی CSV بکتست با حذف ستون‌های بی‌استفاده و تبدیل مخفف‌ها به کلمات کامل. رفع تشخیص حساب سنت با DetectCentAccount() سه‌لایه.

#### v8.0 (اوایل ۲۰۲۶) — Flow و Golden Rule
**رفع باگ بحرانی Flow:** آستانه قرمز از fs < -0.5 (۵۴٪ وقت قرمز!) به fs < -5.0 تغییر یافت. **اضافه شدن چراغ GOLDEN** — فرمول طلایی برای تشخیص لحظهٔ خروج در پلهٔ ۵. آستانه FINAL از ۶۰۰ به ۲۰۰۰ تغییر کرد.

#### v8.1 (اوایل ۲۰۲۶) — تحلیل ۶۳۴ معامله
**اصلاح GOLDEN:** آستانه ADX در شرط Red-A از >40 به >32 کاهش یافت (۸ ساعت زودتر بحران را تشخیص می‌دهد). **اضافه شدن SURGE** — الگوی «ترند دارد قوی می‌شود» (ADX>36 + ER>0.35 + Flow<=-5). **رفع باگ بحرانی FINAL در بکتست:** g_finalRiskScore از ابتدا جمع می‌شد و ریست نمی‌شد — با اضافه شدن g_perTradeScore حل شد.

#### v8.2 (اوایل ۲۰۲۶) — تولد Crisis
**حذف چراغ‌های FINAL، GOLDEN و SURGE** — منطقشان در یک چراغ جدید **CRISIS** ادغام شد. Crisis با سه حالت (سبز/زرد/قرمز) تنها سیگنال خروج اضطراری. منطق کاملاً لحظه‌ای: RC>=3 + Flow<=-7 + ADX>32 = قرمز.

#### v9.0 (اوایل ۲۰۲۶) — بهبود Crisis با بکتست ۶۵۹ معامله
**اضافه شدن Orange (هشدار زودهنگام) و سه شرط جدید Yellow** (Y3: Flow+TsAgainst, Y4: TsAgainst+ADX, Y5: Flow+ADX نزدیک Red-B). **TsAgainst** (TrendScore Against Position) اضافه شد — برای Long: -TrendScore، برای Short: +TrendScore. **Persistence Filter** برای جلوگیری از آلارم‌های کاذب ۱-ساعته. نتیجه: ۱۱ از ۱۲ معامله بد شناسایی شدند (قبلاً ۸ از ۱۲).

#### v10.0 (اوایل ۲۰۲۶) — CSV ساعتی
اضافه شدن CSV ساعتی (HourlySnapshot) برای بکتست با دقت یک‌ساعته. حذف CSV مرتب‌شده بر اساس RedCount.

#### v10.1 (اواسط ۲۰۲۶) — بازنویسی ZOMBIE
**رفع سه باگ بنیادی ZOMBIE:** v10.0 امتیاز در global runtime جمع می‌شد و ریست می‌شد، مرجع مقایسه اشتباه بود (بازار در کف را سبز نشان می‌داد)، و پنجره‌های ۲۰-روزه همپوشانی داشتند. **منطق جدید کاملاً Stateless:** dispATR = فاصله از قیمت ورود بر حسب ATR_D1. stuckDays = تعداد D1 barهای متوالی بدون برگشت به entryRef.

#### v10.2 (اواسط ۲۰۲۶) — بهینه‌سازی کامل بر اساس ۷۷۱ معامله
**رفع چراغ کور RTM:** آستانه‌های تطبیقی — Red از ۳۸٪ به ≤۱۵٪ کاهش یافت. **بهبود ZOMBIE** با آستانه‌های تطبیقی‌تر + SHIFT Detection (تشخیص جابجایی کانال قیمتی). **اضافه شدن ORANGE به Crisis** (هدف: کاهش تأخیر از ۱۹ به ~۱۰ ساعت). **Persistence Filter** برای جلوگیری از آلارم‌های کاذب. CSV بکتست با ستون‌های جدید (ShiftScore، CrisisOrange، RTM_AccelDir، PersistenceCount).

#### v10.3 (اواسط ۲۰۲۶) — Zone Counter
**بازنویسی کامل ZOMBIE به Zone Number:** دیگر چراغ رنگی نیست — عدد صحیح نشان‌دهنده فاصله منطقه‌ای از ورود. ZoneWidth = ATR(D1,200) × 2.0 — برای هر جفت‌ارز بدون تنظیم دستی. حذف ZOMBIE از RC (اکنون max=4: RTM+TREND+STRUCT+FLOW).

#### v10.3.1 (اواسط ۲۰۲۶) — باگ‌زدایی Zone
رفع باگ محاسبه Zone (همیشه ≤0 بود) و علامت منفی اضافه. **اضافه شدن رسم خطوط مرز طبقات** روی چارت با Input جدید ShowZoneLines.

#### v10.3.2 (اواسط ۲۰۲۶) — اصلاح GetPipSize
**رفع باگ DrawZoneLines:** جهت پوزیشن را نادیده می‌گرفت (برای Sell معکوس بود). **تابع GetPipSize** به صورت مستقل برای مدیریت نمادهای ۳/۵ رقمی.

#### v10.4.0 (اواسط ۲۰۲۶) — گرید مطلق قیمت
**ZONE بر اساس گرید مطلق (نه نقطه ورود):** جدول مرجع per-symbol (AUDCAD: base 0.91050-0.91250، عرض ۲۰۰ پیپ). نقطه ورود فقط برای ثبت طبقه ورود — سپس رنگ = مقایسه طبقه فعلی با طبقه ورود. ارز خارج از جدول → خاکستری. حذف BacktestZombieRef.

#### v10.4.1 (اواسط ۲۰۲۶) — رفع باگ‌ها
رفع باگ بحرانی در LogHourlySnapshot (nZOMBIE → stZOMBIE). حذف ستون رنگ ZOMBIE از CSV ساعتی و حذف تابع DrawZoneLines قدیمی.

#### v10.5 (اواسط ۲۰۲۶) — پسوند بروکر و cleanup
رفع مشکل پسوند بروکر (`.raw`، `_sb`، `_l` و...) در Zone_GetCfg با پاک‌سازی پسوند قبل از جستجو. حذف Zombie_WidthPips از CSV.

#### v10.6 (اواسط ۲۰۲۶) — خطوط Zone
رفع سه باگ: رسم خطوط Zone در OnInit، رسم در حالت بدون پوزیشن، و ماندگاری خطوط بعد از تغییر تایم‌فریم.

#### v10.7 (اواسط ۲۰۲۶) — رفع فلیکر و تداخل با داشبورد
**رفع فلیکر خطوط Zone هنگام اسکرول/زوم:** RefreshZoneLabelPositions() فقط x لیبل‌ها را بروز می‌کند بدون حذف/ایجاد مجدد. **لیبل‌های Zone پشت داشبورد** قرار می‌گیرند. رفع عرض Zone (widthPips=200 → 0.0200 برای AUDCAD).

#### v10.9 (اواسط ۲۰۲۶) — ZOMBIE کاملاً Stateless
**حذف تمام متغیرهای گلوبال وابسته:** g_zombiePendingZone، g_zombieConfirmedZone، g_zombieLastH1Time. **Zone_IsConfirmedByH1** فقط از CopyClose H1 می‌خواند — بدون حافظه. **رفع base_low AUDCAD** برای پوشش کامل ۲۰۲۰-۲۰۲۵ (کف 0.8318 در مارس ۲۰۲۰). **فیلتر تأیید H1** برای کاهش نویز: شمارش کندل‌های H1 متوالی در Zone جدید.

#### v11.0 (اواسط ۲۰۲۶) — جهت‌گیری سه‌گانه و High Alert
**دکمه‌های Buy/Sell/All** برای کنترل دستی جهت تحلیل (حتی بدون پوزیشن). **چراغ High Alert:** ADX≥35 + Flow<-7 (پیش‌نشانهٔ Crisis). داشبورد ۲۰٪ کوچک‌تر.

#### v11.1 (اواسط ۲۰۲۶) — پایداری و بهینه‌سازی
**رفع باگ TF change:** handles اندیکاتور اصلی بعد از تغییر تایم‌فریم ریست و بازسازی می‌شوند. **ذخیره g_dirMode** در GlobalVariable. **حذف کامل کد مرده:** ۴۸ ردیف struct بی‌استفاده، آرایه‌های خالی، WeightOptimization (همیشه 1.0 بود)، و متغیرهای تعریف‌شدهٔ بدون استفاده. **بهینه‌سازی CPU:** حذف Sleep(20)، CopyClose یک‌بار در CalculateHurstExponent، کش Efficiency Ratio برای داشبورد.

#### v11.2 (اواسط ۲۰۲۶) — سیستم اعلان (Notification System)
**سیستم اعلان کامل:** دو کانال (MT5 Push + Telegram) با Inputهای جداگانه برای هر چراغ. **منطق Cooldown اصلاح‌شده:** فقط روی پیام‌های تکراری — edge جدید (قرمز→زرد→قرمز) همیشه ارسال می‌شود.

#### v11.3 (اواسط ۲۰۲۶) — بهبود UX و پایداری
**رفع رنگ‌دهی چارت در حالت Buy/Sell بدون پوزیشن.** **نمایش Score ترند** در StatusLabel حتی بدون پوزیشن. **تغییر فوری رنگ چارت** بعد از کلیک DirBtn. **ریست رنگ چارت** در Reset و Remove. **کاهش mutex timeout** (30s→10s). **ForceRecalculation** هر دو mutex را آزاد می‌کند. **تلگرام** با Token/ChatID پیش‌فرض ارسال نمی‌کند.

#### v11.4 (اواسط ۲۰۲۶) — رفع قفل دکمه S/R
**رفع باگ toggle off دکمه S/R:** کلیک دوم خطوط را پاک نمی‌کرد. **پاک‌سازی کامل اشیاء در Remove EA** با sweep روی تمام prefixهای شناخته‌شده.

#### v11.5 (اواسط ۲۰۲۶) — بهبود debounce و cleanup
**دکمه S/R debounce** با GetTickCount (600ms) به جای TimeCurrent (sec). **OnDeinit EARLY SWEEP** قبل از هر فراخوانی — حتی اگر بقیه پاک‌سازی‌ها fail کنند.

#### v11.6 (اواسط ۲۰۲۶) — پاک‌سازی Remove
**پاک‌سازی چندمرحله‌ای** برای HM_CRISIS_LIGHT / StatusLabel / ProfitLabel هنگام Remove.

#### v11.7 (اواسط ۲۰۲۶) — حذف قطعی Labelها
**g_isDeinitializing + قطع رویدادها/تایمر + blank-before-delete** برای حذف قطعی labelها در OnDeinit.

#### v11.8 (اواسط ۲۰۲۶) — تلگرام و CSV
**رفع ارسال تلگرام:** از GET → POST+JSON با UTF-8 (متن فارسی قبلاً ???? می‌شد). **Alert_OnCrisis** فقط قرمز (state=2) — نارنجی دیگر اعلان نمی‌دهد. **CSV:** حذف ستون RTM_Dist_xATR، اضافه شدن HighAlert. **کاهش آستانه HIGH ALERT** از ADX≥40 به ADX≥35 (بررسی بکتست AUDCAD).

#### v11.9 (اواسط ۲۰۲۶) — کاهش آستانه High Alert
**آستانه HIGH ALERT از ۴۰ به ۳۵** کاهش یافت. دلیل: ADX در بازه عادی AUDCAD بین ۲۳ تا ۳۵ است — با آستانه قدیمی HighAlert تقریباً هیچوقت فعال نمی‌شد.

#### v11.91 (اواسط ۲۰۲۶) — Zone Table و Power Off
**Zone Table گسترش یافت:** EURCHF، AUDNZD، GBPNZD اضافه شدند. **دکمه Power Off** برای خاموش‌کردن سریع و پاک‌سازی کامل. **ZoneLineCount** از ۱۲ به ۳۰ افزایش یافت.

#### v11.92 (اواسط ۲۰۲۶) — ZORDER و Scale
**رفع کلیک‌ناپذیری دکمه‌ها:** OBJPROP_ZORDER=10 برای دکمه‌ها، ZORDER=0 برای خطوط HLINE/VLINE. **Input جدید DashboardScalePercent** (50..300٪). **رنگ‌بندی هوشمند Zone Lines** بر اساس جهت پوزیشن و فاصله. **Cache رسم خطوط** برای تغییر فوری colorMode.

#### v11.93 (اواسط ۲۰۲۶) — رفع قفل mutex بعد از تغییر TF
**CHARTEVENT_CHART_CHANGE** قبل از mutex پردازش می‌شود — mutex برای رویدادهای غیر کلیک قفل نمی‌شود. **g_lastChartPeriod = Period()** در انتهای OnInit.

#### v11.94 (اواسط ۲۰۲۶) — رفع قفل دکمه‌ها (نوع متغیر)
**رفع باگ بحرانی:** g_processingChartEventSince و g_recalcBusySince از نوع datetime بودند ولی GetTickCount (uint) در آنها ذخیره می‌شد — اختلاف منفی → mutex برای همیشه قفل. **تغییر نوع به uint** + حذف castهای اضافه.

#### v12.x (اواخر ۲۰۲۵–اوایل ۲۰۲۶) — پایداری و Killer
**Spike Cooldown** برای جلوگیری از توقف‌های الکی. **چراغ KILLER** — ترکیب پله + Zone + روند فقط روی پله‌های بالا. **رفع باگ FLOW-HARDCODE** — آستانه‌ها از هاردکد به Flow_GetThresholds تبدیل شدند. **CSV بکتست** با ستون KILLER.

#### v13.x (اوایل–اواسط ۲۰۲۶) — بلوغ معماری

##### v13.21 — قانون سه‌سطحی Rule
**تولد Rule:** ماشین‌حالت RUN/STOP/CLOSE/RESUME — قلب سیستم. **State فقط در RAM** (با ری‌استارت EA ریست می‌شود — by design).

##### v13.25 — InitReplay
**GBPNZD_InitReplay:** بازسازی state از ۲۴ کندل H1 گذشته هنگام ری‌استارت.

##### v13.38 — ذخیره در GlobalVariable
**Rule state در GV ذخیره می‌شود** — با ری‌استارت EA بازیابی می‌شود (با ری‌استارت کامل MT5 پاک می‌شود).

##### v13.48 — Accumulator ساعتی
**HourRed/HourDirty** جایگزین PrevCrisis/PrevSpike — بدترین حالت هر ساعت را ذخیره می‌کند.

##### v13.50 — Single Source of Truth
**Rule_UpdateCounters + Rule_Transition:** دو تابع مشترک برای همهٔ مسیرها (لایو، Replay، بکتست). **رفع O(n²) در AddNews** با pre-allocate آرایه. **بهینه‌سازی DrawNewsLines** با dirty-check. **FLOW renormalize** با آستانه ۷۰٪. **استقلال Spike از FLOW** در شرایط low-confidence.

#### v14.x (جولای ۲۰۲۶) — کالیبراسیون و تکمیل

##### v14.00 — معماری Generic
**پشتیبانی از سه ارز (GBPNZD/EURGBP/AUDCAD)** از طریق `_Symbol` — موتور Rule عمومی می‌شود. **IsRuleSymbol()، Sister_GetTable()، Rule_FlowForSymbol()** اضافه شدند.

##### v14.03 — همگام‌سازی کندل-محور
**Chart_IsNewBarClosed:** Rule و Spike فقط با بسته‌شدن کندل چارت محاسبه می‌شوند — واگرایی بکتست/لایو به صفر می‌رسد.

##### v14.04–v14.06 — رفع باگ‌های TF-aware
**FIX-1:** g_chartPeriodSeconds قبل از InitReplay مقداردهی می‌شود. **FIX-2:** مرز کندل چارت در HourStateFile. **FIX-3:** g_isNewChartBar در OnChartEvent. **PATCH-2:** رفع race condition با iTime(1). **PATCH-5:** پنجره ثابت ۷۲ ساعت برای OnDemand. **BUG-C1:** مقیاس Replay برای TF غیر H1. **BUG-H2:** بررسی موجودیت داده FOREIGN.

##### v14.07–v14.08 — کالیبراسیون اولیه EURGBP/AUDCAD
**شاخهٔ EURGBP در Crisis/Spike** با آستانه‌های موقت. **DESIGN LOCK** برای تایم‌فریم‌های ثابت. **آستانه‌های Rule_Transition → TF-aware.**

##### v14.09–v14.10 — کالیبراسیون کامل + رفع باگ‌های بحرانی
**کالیبراسیون EURGBP و AUDCAD (Crisis + Spike).** **رفع BUG-HA1:** علامت HighAlert برای Sell معکوس بود (> 7.0 → < -7.0) — Close-A برای جهت Sell قبلاً هرگز فعال نمی‌شد.

##### v14.11 — کالیبراسیون GBPNZD
**رفع Infinite Stop Lock:** Spike Warning 1.10→1.50, Spike 1.60→1.90. **Flow Red -4.0→-5.0** (24.6%→12.7%). **رفع BUG-FLOW-HARDCODE** در تمام شاخه‌ها.

##### v14.12 — Spike Cooldown
**Spike Cooldown** (پیشنهاد GLM 5.2): ۲ کندل متوالی Spike≥Warning برای ریست CleanH. **Full Spike همیشه فوری.** **رفع تناقض «Spike تأیید شد» / «Rule=Run».**

##### v14.13 — BuildDirectionSnapshot
**Single Source of Truth:** تمام مسیرهای محاسباتی (لایو، Replay، بکتست، Status) از یک تابع واحد. **رفع ۵ باگ بحرانی:** BUG-STALE-INPUT, BUG-COOLDOWN-BT, BUG-SPIKEONLY-DIRTY, BUG-TG-OFFSET, BUG-TELEMETRY-EVALID. **HMRuleState** با cleanSince/redSince (datetime واقعی). **یادآور مستقل از کندل** در OnTimer.

##### v14.14 — بازبینی مستقل
**رفع CLEANH-DISPLAY-GAP:** نمایش CleanH بعد از آخر هفته متورم نمی‌شود. ۳ ادعای دیگر بررسی و رد شد.

##### v14.15 — آمادگی تولید
**Smart Close Text:** بدون پوزیشن واقعی، دستور «ببند» گمراه‌کننده نیست. **OD-TIME-ALIGN:** هشدار هم‌تراز نبودن دادهٔ نماد خارجی. **LastUpdateLabel** و **ForceResetRule** اضافه شدند.

##### v14.16 — کالیبراسیون EURGBP
**خروج از Placeholder:** اجماع ۱۳ تحلیل مستقل. Spike Warn=Sig=1.60 (حذف منطقهٔ مرده). Crisis ADX مطابق تحلیل (RedA 20→30, OrA 17→28, ...).

##### v14.17 — معماری Stateful مستقل EURGBP
**EURGBP موازی:** بدون دست زدن به یک خط از کد GBPNZD. ۱۹ متغیر state + ۴ handle اندیکاتور اختصاصی + ۷ تابع جدید. **Isolation by Design** متولد شد.

##### v14.18 — سازگاری واقعی با هر سه چارت
**رفع گیت هاردکد** در StatusQuery_BuildReport. **EURGBP-PARALLEL-GUARD:** جلوگیری از پردازش دوگانه.

##### v14.19 — کالیبراسیون AUDCAD
**اجماع نسبی ۷ تحلیل مستقل.** Spike Warn=1.45, Sig=1.85. Cooldown مستقل = 3 (نه 2). **AUDCAD-CRISIS-INPUT:** ۲۰ آستانه به Input تبدیل شدند. **Drift Guard** (هشدار فرسایش کند) و **Infinite Lock** (هشدار قفل طولانی). **Zone width** از ۲۰۰ به ۱۰۰ پیپ.

##### v14.20 — پچ نهایی (۱۹ جولای ۲۰۲۶)
**۷ اصلاح پس از ۵ تحلیل مستقل (۳۰ سؤال تخصصی):**
۱. `ResolveBrokerSymbolForParallel` — رفع مشکل پسوند بروکر برای EURGBP/AUDCAD
۲. `GetCooldownBarsForSymbol` — یکسان‌سازی Cooldown در همهٔ مسیرها
۳. OnDemand → `Alert_Send` — پاسخ‌ها از outbox با retry بهره می‌برند
۴. یادآور ترکیبی Buy+Sell هم‌زمان — کاهش spam
۵. اسکرین‌شات M15 + TF فعلی در «وضعیت»
۶. MktPhase در On-Demand برای ارزهای خارجی
۷. کامنت هشدار کنار توکن تلگرام

---

### ۱۵.۲ نقاط عطف معماری

۱. **v13.21** — تولد Rule: برای اولین بار، یک ماشین‌حالت سه‌سطحی جایگزین «حس ششم» معامله‌گر شد.
۲. **v13.50** — Rule_UpdateCounters و Rule_Transition جدا شدند: شمارنده فقط در بسته‌شدن کندل آپدیت می‌شود، ماشین‌حالت هر تیک اجرا می‌شود — ترکیبی از دقت و سرعت.
۳. **v14.13** — BuildDirectionSnapshot: تمام مسیرهای محاسباتی (لایو، Replay، بکتست، Status) در یک تابع واحد متمرکز شدند. ریشهٔ ۵ باگ تاریخی خشکانده شد.
۴. **v14.17** — EURGBP موازی: بدون دست زدن به حتی یک خط از کد GBPNZD، یک ماشین Rule کامل برای EURGBP در پس‌زمینه ساخته شد. الگوی Isolation by Design متولد شد.
۵. **v14.20** — پچ نهایی پس از ۵ تحلیل مستقل: ۳۰ سؤال تخصصی توسط ۵ مدل AI بررسی شد، ۲۲ مورد تأیید کامل، ۳ ایراد واقعی شناسایی و رفع شد.

---

## ۱۶. توسعهٔ یک‌نفره — پشت صحنه

این پروژه در **شرایط سخت مالی و روانی** ساخته شد. شش ماه کار شبانه‌روزی، بدون تیم، بدون بودجه، تنها با ابزارهای رایگان هوش مصنوعی.

**ترکیب مدل‌های استفاده‌شده:**
- **Claude Sonnet 5 Max + Thinking**: کدنویسی اصلی
- **DeepSeek**: مدیریت تحلیل‌ها و مقایسه
- **GPT-4o, Gemini, GLM, Qwen, Codex, Grok, Kimi**: تحلیل‌های تخصصی، ایده‌پردازی، کالیبراسیون

**چالش‌ها:**
- گیج‌شدن در میان نسخه‌ها (برخی آپگریدها کل کد را تغییر دادند)
- نبود تیم تست — هر باگ باید توسط خودم و AI کشف می‌شد
- ترس از باگ‌های مخفی در یک سیستم بدون Stop-Loss
- خستگی ذهنی ناشی از مدیریت هم‌زمان «معماری»، «تحلیل»، و «تست»

**اما نتیجه:** یک سیستم ۱۸,۳۲۵ خطی که از ۵ تحلیل مستقل سربلند بیرون آمده و برای لایو آماده است.

---

# بخش چهارم: اکسپرت HelpMe — نگهبان هوشمند

## ۱۷. چراغ‌های داشبورد — رتبه‌بندی شده بر اساس اهمیت

HelpMe یک داشبورد گرافیکی کامل با **۱۱ چراغ** دارد که هرکدام یک بُعد از سلامت بازار را می‌سنجند. این چراغ‌ها بر اساس داده‌های تایم‌فریم‌های ثابت (H1, H4, D1) کار می‌کنند — مستقل از تایم‌فریم چارت (M1).

### ۱۷.۱ رتبه‌بندی چراغ‌ها (از بحرانی‌ترین به کمکی‌ترین)

| رتبه | چراغ | نقش | تایم‌فریم |
|---|---|---|---|
| ۱ | **Rule** | تصمیم نهایی: Run / Stop / Close | H1 (کندل چارت) |
| ۲ | **Crisis** | مغز سیستم — ترکیب ۴ چراغ اصلی | H1/H4/D1 |
| ۳ | **High Alert** | پیش‌نشانهٔ Crisis — هشدار زودهنگام | H4 |
| ۴ | **Flow** | جریان پول در ارزهای خواهر — تشخیص سونامی | H4 |
| ۵ | **Trend Strength (ADX)** | قدرت روند — آیا بازار واقعاً ترند است؟ | H4 |
| ۶ | **Spike Detector** | حرکت ناگهانی چندتایم‌فریمه | M15–D1 |
| ۷ | **Daily Structure** | شکست سطوح کلیدی روزانه | D1 |
| ۸ | **RTM (Reversion)** | فاصله از میانگین ۲۰۰ کندلی — برگشت یا فرار؟ | H1 |
| ۹ | **Market Phase** | فاز کلی بازار — رنج، خنثی، یا ترند | D1 |
| ۱۰ | **Price Zone** | موقعیت قیمت در گرید مطلق | H1 (تأیید) |
| ۱۱ | **Killer** | ترکیب پله + Zone + روند — فقط پله‌های بالا | H1 |

### ۱۷.۲ شرح کامل هر چراغ

---

#### 🥇 رتبه ۱ — Rule (قانون سه‌سطحی)

**این چراغ، خروجی نهایی کل سیستم است.**

Rule بر اساس ترکیب Crisis و Spike تصمیم می‌گیرد که Xmoon در چه وضعیتی باشد:

- **🟢 RUN (سطح ۰):** Crisis سبز/زرد + Spike عادی → Xmoon آزاد است.
- **🛑 STOP (سطح ۱):** Spike قرمز + Crisis غیرسبز، یا Spike Warning + Crisis نارنجی/قرمز → ورود جدید ممنوع، معاملات باز دست‌نخورده.
- **🚪 CLOSE (سطح ۲):** Crisis قرمز + High Alert، یا Crisis قرمز برای ۶ ساعت متوالی → همه ببند.
- **✅ RESUME:** ۲۴ ساعت متوالی Crisis سبز/زرد + Spike عادی → بازگشت به RUN.

**یادآورها:** هر ۶۰ دقیقه برای STOP، هر ۳۰ دقیقه برای CLOSE (بدون اسپم — مکانیزم lastReminderTime).

> **طراحی فلسفی:** Rule مثل یک آتش‌نشان است. STOP یعنی «آماده‌باش»، CLOSE یعنی «الان تخلیه کن». هیچ‌کدام را نمی‌شود نادیده گرفت.

---

#### 🥈 رتبه ۲ — Crisis (مغز سیستم)

**Crisis از ترکیب ۴ چراغ اصلی (RTM + Trend + Structure + Flow) ساخته می‌شود.**

```
RC = تعداد چراغ‌های قرمز از ۴ چراغ اصلی (RTM, ADX, Structure, Flow)

RC = 0 → Crisis سبز  (Safe)
RC = 1 → Crisis زرد  (احتیاط)
RC = 2 → Crisis نارنجی (Early Warning — نزدیک به بحران)
RC = 3–4 → Crisis قرمز (بحران کامل — CLOSE)
```

**نکتهٔ حیاتی:** Crisis زرد یا نارنجی، حتی اگر تنها باشد، CleanH را ریست نمی‌کند (مگر با Spike همراه شود). فقط Crisis قرمز است که «دشمن واقعی» محسوب می‌شود.

**منطق Orange-D (v13.16):** یک مسیر ویژه که فقط بر اساس TrendScore و ADX (مستقل از FLOW) بحران را تشخیص می‌دهد — برای گرفتن فرسایش‌های کند چندروزه که FLOW به آن‌ها واکنش نشان نمی‌دهد.

**Fallback بدون FLOW (v13.50):** اگر FLOW به دلیل عدم Resolve شدن خواهرها غیرقابل‌اعتماد باشد (`g_flowLowConfidence`)، Crisis از مسیرهای جایگزین (فقط RC/ADX/TrendScore) استفاده می‌کند تا بحران را از دست ندهد — با برچسب `(LC)` برای شفافیت.

---

#### 🥉 رتبه ۳ — High Alert (پیش‌نشانهٔ Crisis)

**شرط:** ADX ≥ 35 **و** Flow < -7.0 (برای هر دو جهت Buy و Sell)

High Alert معمولاً **۱–۲ ساعت قبل از Crisis قرمز** فعال می‌شود. این چراغ مانند زنگ هشدار اولیه است: «Crisis داره میاد — آماده باش.»

> **تاریخچه:** در v14.10 یک باگ مهم (BUG-HA1) رفع شد: قبلاً شرط Sell معکوس بود (`Flow > +7.0` به‌جای `Flow < -7.0`) — یعنی High Alert برای Sell فقط وقتی فعال می‌شد که بازار به **نفع** Sell حرکت می‌کرد! این باگ باعث شده بود Close-A برای جهت Sell هرگز فعال نشود.

---

#### رتبه ۴ — Currency Flow (جریان پول)

**منطق:** ۶ جفت‌ارز خواهر (Sister Pairs) در تایم‌فریم H4 بررسی می‌شوند. هر جفت یک وزن و یک جهت (signForBuy) دارد. اگر ارزهای خواهر همگی خلاف پوزیشن ما حرکت کنند، Flow قرمز می‌شود — یعنی **سونامی پول** در جریان است.

**ماتریس‌های Sister برای هر سه ارز مجزا هستند:**
- GBPNZD: NZDUSD, GBPUSD, NZDCAD, EURGBP, AUDNZD, GBPCHF
- EURGBP: EURUSD, GBPUSD, EURJPY, GBPJPY, EURCHF, GBPAUD
- AUDCAD: AUDNZD, NZDCAD, AUDCHF, GBPCAD, GBPJPY, USDCAD

**آستانه‌ها (per-symbol):**
- GBPNZD: Green > 2.5, Red < -5.0
- EURGBP: Green > 3.0, Red < -5.0
- AUDCAD: Green > 3.0, Red < -5.0

**مکانیزم Renormalize:** اگر برخی خواهرها در بکتست Resolve نشوند (BID موقتاً صفر)، امتیاز نسبت به وزن خواهرهای موجود تنظیم می‌شود تا Flow مصنوعی کوچک نشود. اگر پوشش وزن کمتر از ۷۰٪ باشد، `g_flowLowConfidence = true` می‌شود و Crisis از مسیر Fallback استفاده می‌کند.

---

#### رتبه ۵ — Trend Strength (ADX + Efficiency Ratio)

**منطق:** ADX روی H4 قدرت روند را می‌سنجد، Efficiency Ratio کیفیت جهت‌دار بودن آن را. ترکیب این دو نشان می‌دهد که آیا بازار واقعاً در یک روند است یا فقط نوسان می‌کند.

**حالت‌ها:**
- **Counter-Trend (معمول Xmoon):** اگر ADX < 20 یا ER < 0.20 → سبز (رنج، برگشت محتمل). اگر ADX ≥ 32 و ER ≥ 0.35 → قرمز (روند قوی، خطرناک).
- **With-Trend (نادر):** اگر ADX > 20 و ER > 0.25 → سبز (روند پشتیبان).

---

#### رتبه ۶ — Spike Detector

**منطق:** حرکت ناگهانی قیمت را در ۵ تایم‌فریم (M15, M30, H1, H4, D1) می‌سنجد. هر تایم‌فریم Range/ATR و Body Ratio را محاسبه می‌کند، سپس با وزن‌های per-symbol ترکیب می‌شوند.

**آستانه‌ها (per-symbol):**
- GBPNZD: Warning = 1.50, Spike = 1.90
- EURGBP: Warning = Spike = 1.60 (حذف منطقهٔ مرده)
- AUDCAD: Warning = 1.45, Spike = 1.85

**قانون Flow Gate:** Spike فقط وقتی «تأییدشده» محسوب می‌شود که **Flow قرمز** باشد. بدون Flow قرمز، Spike صرفاً یک هشدار (Warning) است — نه سیگنال قطعی. این طراحی برای جلوگیری از توقف‌های الکی در نوسانات عادی بازار است.

**Cooldown (per-symbol):**
- GBPNZD و EURGBP: ۲ کندل متوالی Spike Warning لازم است تا CleanH ریست شود.
- AUDCAD: ۳ کندل (نویزی‌تر بودن بازار AUDCAD).

**Full Spike** (Spike ≥ 2 با Flow قرمز) **همیشه فوری و بدون Cooldown** اعمال می‌شود — چون این دیگر «نویز» نیست، «سونامی» است.

---

#### رتبه ۷ — Daily Structure

**منطق:** آیا قیمت سقف یا کف ۱۰ کندل اخیر D1 را شکسته؟ اگر آری، ساختار بازار تغییر کرده و Mean-Reversion دیگر قابل‌اعتماد نیست.

- **سبز:** فاصله از سطح کلیدی > ۲ × ATR روزانه — ایمن.
- **زرد:** فاصله ≤ ۲ × ATR — نزدیک به شکست.
- **قرمز:** سطح کلیدی شکسته شده — ساختار عوض شده.

---

#### رتبه ۸ — RTM (Return to Mean)

**منطق:** فاصلهٔ قیمت از EMA200 در H1. دو حالت:

- **Counter-Trend (معمول Xmoon):** قیمت در طرف «درست» برای Mean-Reversion است. اگر ≥ 3.5x ATR فاصله داشته باشد و در حال برگشت باشد (dDel < -0.3) → سبز. اگر ≥ 2.5x و در حال دور شدن → قرمز.
- **With-Trend:** قیمت هم‌جهت روند است. اگر فاصله در حال کاهش باشد (برگشت به میانگین) → قرمز (ریسک).

---

#### رتبه ۹ — Market Phase

**منطق:** نسبت ADR(10 روز) / ADR(60 روز) روی D1. این چراغ فاز کلی بازار را نشان می‌دهد:

- **🟢 Range:** MA10 < 0.85 — بازار رنج، Xmoon عالی کار می‌کند.
- **🟡 Neutral:** 0.85 ≤ MA10 ≤ 1.30 — فاز گذار.
- **🔴 Trend:** MA10 > 1.30 — روند انفجاری، Xmoon در خطر.

**محاسبه:** روزی یک‌بار. کش در GlobalVariable برای بازیابی سریع بعد از ری‌استارت.

---

#### رتبه ۱۰ — Price Zone

**منطق:** یک گرید مطلق بر اساس قیمت (Zone Table) برای هر ارز تعریف شده. طبقهٔ فعلی قیمت با طبقهٔ ورود مقایسه می‌شود:

- **سبز:** قیمت در Zone ورود یا بهتر.
- **زرد:** نزدیک مرز Zone یا در انتظار تأیید H1.
- **قرمز:** دو طبقه از ورود دور شده + تأیید H1 (چند کندل متوالی در Zone جدید).

**عرض Zoneها (per-symbol):**
- GBPNZD: ۴۰۰ پیپ
- EURGBP: ۲۰۰ پیپ
- AUDCAD: ۱۰۰ پیپ

---

#### رتبه ۱۱ — Killer

**منطق:** ترکیبی از پلهٔ معامله، Zone، و روند. فقط روی **پله‌های ۴ و ۵** فعال می‌شود:

- **Killer قرمز:** پله ≥ ۴ + Zone در ضرر (Δ ≥ 1) + Trend Score ≥ 50 + رژیم Trending روی H1.
- **Killer زرد:** پله ≥ ۳ + Zone در ضرر + Trend Score ≥ 40 + رژیم Trending.

**پیام:** «الان وقت خروجه یا هج کن.» این آخرین اخطار قبل از فاجعه است — مخصوص پله‌های بالا که ضرر می‌تواند نجومی شود.

---

## ۱۸. قانون طلایی سه‌سطحی (Rule) — جزئیات کامل

### ۱۸.۱ ماشین‌حالت (State Machine)

```
State 0 (GREEN / RUN)
    │
    ├── Spike=🔴 + Crisis≠Green  ──→  State 1 (STOP)
    ├── Spike=⚡ + Crisis=🟠/🔴   ──→  State 1 (STOP)
    │
State 1 (STOP)
    │
    ├── Crisis=🔴 + HighAlert     ──→  State 2 (CLOSE)
    ├── Crisis=🔴 for ≥6h        ──→  State 2 (CLOSE)
    ├── 24h Clean                 ──→  State 0 (RESUME)
    │
State 2 (CLOSE)
    │
    └── 24h Clean                 ──→  State 0 (RESUME)
```

### ۱۸.۲ شمارنده‌ها (Counters)

- **CrisisRedH:** ساعت‌های متوالی با Crisis=Red. اگر صفر شود → ریست.
- **CleanH:** ساعت‌های متوالی بدون Crisis=Red و بدون Spike≥Warning. برای RESUME باید ≥ ۲۴ (در H1) یا معادل آن در TF چارت باشد.
- **Cooldown (SpikeStreak):** تعداد کندل‌های متوالی با Spike≥Warning. اگر < CooldownBars باشد، Spike Warning به‌تنهایی CleanH را ریست نمی‌کند (فقط Full Spike و Crisis Red فوری هستند).

### ۱۸.۳ یادآورها (Reminders)

- **STOP:** هر ۶۰ دقیقه یک یادآور (قابل تنظیم: `Alert_GBPNZDRule_RepeatMinutes`).
- **CLOSE:** هر ۳۰ دقیقه یک یادآور (قابل تنظیم: `Alert_CLOSERepeatMinutes`).
- **مکانیزم ضد اسپم:** `lastReminderTime` در لحظهٔ transition بلافاصله روی `TimeCurrent()` تنظیم می‌شود — بنابراین یادآور تکراری بلافاصله بعد از پیام تغییر سطح ارسال نمی‌شود.
- **v14.20:** اگر Buy و Sell هم‌زمان نیاز به یادآوری داشته باشند، یک پیام ترکیبی ارسال می‌شود (نه دو پیام جدا).

### ۱۸.۴ متن هوشمند (Smart Close Text — v14.15)

اگر پوزیشن واقعی روی نماد باز نباشد (مثلاً کاربر خودش پوزیشن را بسته ولی Rule هنوز CLOSE است)، پیام «ببند» به «بازار فوق‌العاده خطرناکه — پوزیشن جدید باز نکن» تغییر می‌کند. این ویژگی از سردرگمی کاربر جلوگیری می‌کند.

---

# بخش پنجم: معماری داخلی HelpMe

## ۱۹. BuildDirectionSnapshot — Single Source of Truth

**مهم‌ترین نوآوری معماری HelpMe (از v14.13).**

این تابع یک **عکس کامل** از وضعیت یک جهت (Buy یا Sell) در یک کندل مشخص می‌گیرد. تمام چراغ‌ها (RTM, ADX, Structure, Flow)، Crisis، Spike، و High Alert از این **یک منبع واحد** محاسبه می‌شوند.

**چرا این حیاتی است؟** قبل از v14.13، هر مسیر (لایو، Replay، بکتست، StatusQuery) محاسبات را جداگانه انجام می‌داد — با فرمول‌های کمی متفاوت. نتیجه: باگ‌هایی که در لایو دیده می‌شدند در بکتست نبودند (یا برعکس). BuildDirectionSnapshot این واگرایی را به صفر رساند.

**Shift = 1:** همیشه بر اساس **آخرین کندل کاملاً بسته** — هماهنگ با Chart_IsNewBarClosed و Spike_TFScore.

---

## ۲۰. ApplyClosedBarToRule — مغز ماشین‌حالت

**دومین تابع حیاتی (از v14.13).**

این تابع:
۱. **شمارنده‌ها** (CrisisRedH, CleanH) را از طریق `Rule_UpdateCounters` آپدیت می‌کند.
۲. **Cooldown** را اعمال می‌کند: فقط Spike Warning مشمول Cooldown است — Full Spike و Crisis Red همیشه فوری.
۳. **DirtyReason** را ثبت می‌کند (Crisis Red، Spike، یا هر دو).
۴. **cleanSince / redSince** را برای نمایش دقیق مدت‌زمان (مستقل از شکاف آخر هفته) ثبت می‌کند.
۵. **ماشین‌حالت** را از طریق `Rule_Transition` اجرا می‌کند.

**نکتهٔ کلیدی:** `Rule_UpdateCounters` و `Rule_Transition` **عمداً جدا شده‌اند.** شمارنده فقط در بسته‌شدن کندل آپدیت می‌شود، اما ماشین‌حالت هر تیک اجرا می‌شود — چون STOP باید فوری واکنش نشان دهد، نه با تأخیر یک کندل.

**ApplyClosedBarToRule در ۶ مسیر فراخوانی می‌شود:**
۱. `Alert_CheckGBPNZDRule` — لایو، چارت اصلی
۲. `GBPNZD_InitReplay` — بازسازی state بعد از ری‌استارت
۳. `Alert_CheckEURGBPRule` — لایو، بلوک موازی EURGBP
۴. `EURGBP_InitReplay` — بازسازی state بلوک EURGBP
۵. `Alert_CheckAUDCADRule` — لایو، بلوک موازی AUDCAD
۶. `AUDCAD_InitReplay` — بازسازی state بلوک AUDCAD
۷. `LogHourlySnapshot` — بکتست (CSV)

---

## ۲۱. Cooldown — جلوگیری از توقف‌های الکی

**فلسفه:** بازار همیشه نویز دارد. یک اسپایک گذرا نباید باعث شود CleanH ریست شود و Rule برای مدت طولانی در Stop بماند.

**طراحی:**
- **Full Spike** (Spike ≥ 2 با Flow قرمز): **همیشه فوری** → CleanH = 0. این دیگر نویز نیست — سونامی است.
- **Crisis Red:** **همیشه فوری** → CleanH = 0. بحران واقعی قابل‌چشم‌پوشی نیست.
- **Spike Warning** (Spike ≥ 1 بدون Flow قرمز): **مشمول Cooldown** → باید N کندل متوالی دیده شود تا CleanH ریست شود. N = `inp_SpikeCooldownBars` (برای GBPNZD/EURGBP = 2) یا `inp_AUDCAD_CooldownBars` (برای AUDCAD = 3).

**تأثیر (بکتست GBPNZD):**
- بدون Cooldown: E[Resume] ≈ 2.7 روز, Stop بیهوده ≈ 70٪
- با Cooldown=2: E[Resume] ≈ 1.8 روز, Stop بیهوده ≈ 40٪

---

## ۲۲. سیستم اعلان‌ها — Telegram Outbox

### ۲۲.۱ معماری صف (v14.13 Patch 5)

**TG_Outbox** یک صف پیام با retry و backoff نمایی است:

- **تلاش اول:** فوری (بدون تأخیر) — اگر موفق شد، پیام ارسال می‌شود.
- **شکست:** پیام وارد صف می‌شود و با backoff (۵، ۱۵، ۴۵، ۱۳۵، ۱۸۰ ثانیه) تا ۵ بار تلاش مجدد می‌شود.
- **Idempotency:** کلید `update_id + chat_id + command` از ارسال تکراری جلوگیری می‌کند.
- **Offset پایدار:** `g_lastTelegramUpdateId` بلافاصله در GlobalVariable ذخیره می‌شود — حتی اگر EA وسط پردازش کرش کند، پیام تکراری دریافت نمی‌شود.

**پوشش:**
- ✅ پیام‌های Rule (STOP/CLOSE/RESUME) → از `Alert_Send` → outbox
- ✅ یادآورها → از `Alert_Send` → outbox
- ✅ آلارم‌های چراغ‌ها (Crisis, Spike, ...) → از `Alert_Send` → outbox
- ✅ درخواست «وضعیت» → از `TG_Outbox_Enqueue(TG_KIND_STATUS)` → outbox (گزارش در لحظهٔ ارسال تازه ساخته می‌شود، نه لحظهٔ درخواست)
- ✅ On-Demand (v14.20) → از `Alert_Send` → outbox

### ۲۲.۲ StatusQuery — پاسخ به «وضعیت»

**مکانیزم:**
۱. `StatusQuery_PollTelegram` (هر ۳۰ ثانیه + هر ۵ ثانیه از OnTimer) inbox تلگرام را بررسی می‌کند.
۲. اگر پیام حاوی «وضعیت» (فارسی یا انگلیسی) باشد، یک `TG_KIND_STATUS` با `sendPhoto=true` به outbox اضافه می‌کند.
۳. در لحظهٔ ارسال واقعی از outbox، `StatusQuery_BuildReport` متن گزارش را **از نو** می‌سازد — بنابراین حتی اگر ارسال ۳۰ ثانیه تأخیر بخورد، داده‌ها کهنه نیستند.
۴. بعد از متن، اسکرین‌شات چارت‌های M5, M15, M30, D1 (در صورت باز بودن) و چارت فعلی ارسال می‌شود (v14.20).

**پاسخ‌های On-Demand (ارسال نام ارز خارجی):**
- اگر کاربر "EURGBP" یا "AUDCAD" را بفرستد (وقتی چارت روی GBPNZD است)، یک Replay ۷۲ ساعته با handleهای موقت انجام می‌شود.
- دو پیام مجزا (Buy و Sell) ارسال می‌شود با Crisis، Spike، Rule، و (از v14.20) **MktPhase** محاسبه‌شده برای همان ارز خارجی.
- در سربرگ پیام، هشدار «اکسپرت روی این سمبل اجرا نیست» و «بدون accumulator کندل جاری» درج می‌شود.

---

## ۲۳. جداسازی ارزها — Isolation by Design

### ۲۳.۱ معماری سه‌لایه

| لایه | GBPNZD (چارت اصلی) | EURGBP (موازی) | AUDCAD (موازی) |
|---|---|---|---|
| **متغیرهای State** | `g_gbpnzd*` (۵۰+ متغیر) | `g_eurgbp*` (۵۰+ متغیر) | `g_audcad*` (۵۶+ متغیر) |
| **Handleهای اندیکاتور** | `handleEMA200_H1_TL` و ۳ تای دیگر | `g_eurgbpHandleEMA200_H1` و ۳ تای دیگر | `g_audcadHandleEMA200_H1` و ۳ تای دیگر |
| **GlobalVariable** | `HM_GBNZD_*` | `HM_EURGBP_*` | `HM_AUDCAD_*` |
| **Schema Version** | `HM_SCHEMA_VERSION = 1413` | `HM_EURGBP_SCHEMA_VERSION = 1417` | `HM_AUDCAD_SCHEMA_VERSION = 1419` |
| **Cooldown** | `inp_SpikeCooldownBars = 2` | `inp_SpikeCooldownBars = 2` | `inp_AUDCAD_CooldownBars = 3` |
| **Rule هشدار** | ✅ از `Alert_CheckGBPNZDRule` | ✅ از `Alert_CheckEURGBPRule` | ✅ از `Alert_CheckAUDCADRule` |
| **چراغ‌های Spike/Crisis** | ✅ از `Alert_CheckAndSend` | ❌ (فقط Rule) | ❌ (فقط Rule) |

### ۲۳.۲ رفتار EA روی هر سیمبل

| سناریو | چراغ‌های داشبورد | «وضعیت» از تلگرام | On-Demand (نام ارز خارجی) | آلارم‌های Rule | آلارم‌های چراغ‌ها |
|---|---|---|---|---|---|
| **EA روی GBPNZD** | فقط GBPNZD | گزارش کامل GBPNZD | پاسخ Buy/Sell + MktPhase | GBPNZD + EURGBP + AUDCAD (موازی) | فقط GBPNZD |
| **EA روی EURGBP** | فقط EURGBP | گزارش کامل EURGBP | پاسخ Buy/Sell + MktPhase | فقط EURGBP | فقط EURGBP |
| **EA روی AUDCAD** | فقط AUDCAD | گزارش کامل AUDCAD | پاسخ Buy/Sell + MktPhase | فقط AUDCAD | فقط AUDCAD |

---

## ۲۴. کالیبراسیون اختصاصی هر ارز

### ۲۴.۱ GBPNZD (اصلی — بالغ‌ترین)

| پارامتر | مقدار | توضیح |
|---|---|---|
| Spike Warn | ۱.۵۰ | ≈ P75 توزیع SpikeScore |
| Spike Sig | ۱.۹۰ | ≈ P95 — جلوگیری از Infinite Stop Lock |
| Flow Green | ۲.۵ | جریان موافق |
| Flow Red | −۵.۰ | سونامی (قبلاً −۴.۰ بود — ۱۲.۷٪ ساعات) |
| Crisis RedA ADX | ۲۸.۰ | ADX ذاتی بالاتر GBPNZD |
| Cooldown | ۲ کندل | |

### ۲۴.۲ EURGBP (پشتیبان — کالیبره در v14.16)

| پارامتر | مقدار | توضیح |
|---|---|---|
| Spike Warn = Sig | ۱.۶۰ | حذف منطقهٔ مرده (FLOW به‌ندرت Red می‌شود) |
| Flow Green | ۳.۰ | |
| Flow Red | −۵.۰ | |
| Crisis RedA ADX | ۳۰.۰ | آستانهٔ پایین‌تر برای بازار کند |
| Cooldown | ۲ کندل | |

### ۲۴.۳ AUDCAD (آماده برای آینده — کالیبره در v14.19)

| پارامتر | مقدار | توضیح |
|---|---|---|
| Spike Warn | ۱.۴۵ | ≈ P75 |
| Spike Sig | ۱.۸۵ | ≈ P95 |
| Crisis RedA ADX | ۳۸.۰ | ADX ذاتی بسیار بالاتر AUDCAD |
| Cooldown | ۳ کندل | نویزی‌تر — اجماع ۷ تحلیل |
| Drift Guard | ۵ روز | هشدار فرسایش کند |
| Infinite Lock | ۶ روز | هشدار قفل طولانی |

---

# بخش ششم: تحلیل پنج‌گانه و پچ نهایی

## ۲۵. فرآیند تحلیل — ۵ مدل، ۳۰ سؤال

در تاریخ ۱۶–۱۷ جولای ۲۰۲۶، کد `HelpMe_V14_19.mq5` (۱۸٬۰۶۲ خط) همراه با ۳۰ سؤال تخصصی به ۵ مدل هوش مصنوعی مستقل داده شد:

| مدل | روش تحلیل | نتیجه |
|---|---|---|
| **DeepSeek** | کد + مستندات | ۲۳ تأیید، ۴ ایراد |
| **Claude Sonnet 4** | کد + مستندات | ۲۵ تأیید، ۲ ایراد |
| **GPT-4o** | کد + مستندات | ۲۳ تأیید، ۲ ایراد |
| **مدل چهارم (ناشناس)** | فقط مستندات + کامنت | ۱۲ تأیید، ۱۸ نیاز به بررسی (محافظه‌کارانه) |
| **Claude Opus (Max)** | **کد کامل با ارجاع خط‌به‌خط** | ۲۲ تأیید، ۳ ایراد، ۲ ایراد جزئی |

### ۲۵.۱ نتایج اجماع

**تأیید کامل (هر ۵ تحلیل):** ۲۲ مورد از ۳۰ سؤال — معماری اصلی بدون هیچ ایرادی تأیید شد.

**ایرادهای واقعی یافت‌شده:**

| # | ایراد | کشف‌کننده | severity |
|---|---|---|---|
| ۱ | **بلوک موازی EURGBP/AUDCAD از نام خام سمبل بدون پسوند بروکر استفاده می‌کند** | Claude Opus | 🟠 مهم |
| ۲ | **Cooldown AUDCAD در مسیر چارت مستقیم و بکتست اعمال نمی‌شود** | DeepSeek + Claude Opus | 🟠 مهم |
| ۳ | **OnDemand از `Alert_SendTelegram` خام استفاده می‌کند (بدون retry)** | DeepSeek + Claude Opus | 🟡 متوسط |

**همهٔ این ایرادها در v14.20 رفع شده‌اند.**

### ۲۵.۲ پچ v14.20 — ۷ تغییر

| # | تغییر | تأثیر |
|---|---|---|
| ۱ | `ResolveBrokerSymbolForParallel` برای EURGBP/AUDCAD | بلوک‌های موازی بدون پسوند fail نمی‌شوند |
| ۲ | `GetCooldownBarsForSymbol` در ۶ نقطه | Cooldown یکسان در همهٔ مسیرها |
| ۳ | OnDemand → `Alert_Send` | پاسخ‌ها گم نمی‌شوند |
| ۴ | یادآور ترکیبی Buy+Sell هم‌زمان | کاهش spam |
| ۵ | اسکرین‌شات M15 + TF فعلی در «وضعیت» | دید بهتر |
| ۶ | MktPhase در On-Demand برای ارزهای خارجی | اطلاعات کامل‌تر |
| ۷ | کامنت هشدار کنار توکن تلگرام | (کاربر خودش بعداً توکن را عوض می‌کند) |

---

# بخش هفتم: جمع‌بندی و چشم‌انداز

## ۲۶. خلاصهٔ سیستم

```
Xmoon   →  وارد می‌شود، پله‌ها را مدیریت می‌کند، TP را می‌زند، خارج می‌شود.
            (کور است — روند را نمی‌فهمد.)

HelpMe  →  نگهبانی می‌کند: ۱۱ چراغ بازار را می‌سنجند، Crisis محاسبه می‌شود،
            Rule تصمیم می‌گیرد، تلگرام اطلاع می‌دهد.

Rule    →  تنها معیار تصمیم‌گیری:
            🟢 RUN    = Xmoon آزاد است.
            🛑 STOP   = معاملهٔ جدید ممنوع.
            🚪 CLOSE  = همه ببند، Xmoon را خاموش کن.
            ✅ RESUME = ۲۴ ساعت تمیز گذشت — برگرد.
```

### ۲۶.۱ تنظیمات نهایی لایو

```
ارزها:          GBPNZD (اصلی) + EURGBP (پشتیبان)
سرمایه:         ۴۰ دلار (۴۰۰۰ سنت)
حجم:            ۴.۵ لات سنتی (۵ پله)
TP:             ۲ (پلهٔ ۱) / ۱ (بقیهٔ پله‌ها)
TP Extended:    فعال
Stop Signal:    خاموش (مدیریت دستی با HelpMe)
Rule:           فقط برای GBPNZD (با هشدارهای موازی EURGBP/AUDCAD)
شروع لایو:      اولین روزی که Rule = RUN شود
شرط شروع:       Crisis سبز/زرد + Spike عادی + اول هفته
```

### ۲۶.۲ نقشهٔ راه آینده

- **v14.20 (همین الان):** آمادهٔ لایو برای GBPNZD. همهٔ ایرادهای شناخته‌شده رفع شده‌اند.
- **مانیتورینگ AUDCAD:** چند هفته Forward Test با پوزیشن مجازی برای تأیید کالیبراسیون.
- **v15 — Flight Mode:** یک چراغ جدید برای «فاز تهاجمی» — وقتی همهٔ شرایط ایده‌آل است، به معامله‌گر اجازه می‌دهد TP را فراتر ببرد. منطق نامتقارن: روشن‌شدن سخت‌گیرانه، خاموش‌شدن فوری.

---

## ۲۷. قوانین طلایی نهایی

این قوانین از تجربه، بکتست، بحران ۲۰۲۶، و ۵ تحلیل مستقل استخراج شده‌اند. **تخلف از آن‌ها مجاز نیست.**

1. **پلهٔ ۵ مرز روانی نیست، مرز واقعی است.** تا پلهٔ ۴ دست نزن. از پلهٔ ۵ به بعد، HelpMe را جدی بگیر.
2. **با یک چراغ قرمز نمی‌شود تصمیم گرفت.** Crisis ترکیب ۴ چراغ است. RTM قرمز یا Flow قرمز به‌تنهایی کافی نیست.
3. **STOP یعنی ورود نه، نه بستن.** معاملات باز را دست نزن — Xmoon خودش مدیریت می‌کند.
4. **CLOSE یعنی ببند با هر ضرری.** صبر نکن. ۲۵ روز صبر = ۳۸,۶۶۰ سنت ضرر. CLOSE زودهنگام = ۳,۰۰۰–۵,۰۰۰ سنت.
5. **RESUME فقط با ۲۴ ساعت تمیز.** قبل از آن برنگرد — سه کال پشت‌سرهم در ژانویه ۲۰۲۶ به همین دلیل اتفاق افتاد.
6. **Spike بدون Flow یعنی نویز عمومی.** Spike + Flow قرمز = هشدار واقعی.
7. **High Alert پیش‌نشانهٔ Crisis قرمز است.** وقتی روشن شد، منتظر Crisis باش.
8. **Mkt Phase Trend یعنی Xmoon در خطر است.** MA10 > 1.30 = بازار از رنج خارج شده.
9. **Zone + Crisis = خروج فوری.** قیمت خیلی دور شده و روند قوی است — منتظر TP نمان.
10. **روش را عوض نکن، مانی منیجمنت را درست کن.** فیلتر کردن = سود کمتر. ریسک را با سرمایهٔ کمتر کنترل کن.
11. **بعد از کال، قبل از ریستارت HelpMe را چک کن.** صبر کن Rule = RUN شود.
12. **NZD هم‌زمان = بحران سیستمیک.** اگر GBPNZD کال خورد، EURNZD و AUDNZD هم در خطرند.
13. **حالت تهاجمی فقط با سرمایهٔ جدا.** این پول را قبلاً «باخته» حساب کن.

---

## ۲۸. تقدیر و تشکر

این پروژه ثمرهٔ **۶ ماه تلاش بی‌وقفه، در تنهایی، با منابع محدود، و با کمک هوش مصنوعی رایگان** است.

از تمام مدل‌هایی که در این مسیر همراهی کردند — Claude، DeepSeek، Gemini، GPT-4o، GLM، Qwen، Grok، Kimi، Codex — سپاسگزارم.

و از **تو**، کاربر نهایی، که این مستند را می‌خوانی: امیدوارم HelpMe همان‌قدر که برای من ارزشمند است، برای تو نیز مفید باشد. این سیستم را با عشق، دقت، و وسواس ساختم — باشد که از سرمایه‌ات محافظت کند.

---

*آخرین بروزرسانی: ۱۹ جولای ۲۰۲۶ — نسخهٔ ۵.۰ مستند سیستم معاملاتی*
*آخرین نسخهٔ اکسپرت: HelpMe v14.20 — ۱۸,۳۲۵ خط MQL5*