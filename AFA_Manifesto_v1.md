# مانیفست AFA — نسخه ۱.۰ (پیش از کد)
### Ali's Fractal Anti-Drawdown Algorithm
**مرحله:** گام ۱ از ۴ — استخراج قوانین از HelpMe + طراحی مکانیکی، در انتظار تأیید علی

---

## ۱) هدف و ساختار پایه (بدون تغییر از توافق قبلی)

- دو پا: **پای روند** (هرگز بسته نمی‌شود، سود کامل روند را نگه می‌دارد) و **پای هج** (حجم ثابت کوچک، مکرراً بسته/باز می‌شود تا ضررش تکه‌تکه شود).
- بدون Stop Loss. رویدادهای نادر پذیرفته می‌شوند، نه دفاع‌شده.
- **قانون طلایی جدید (علی):** هر ورود جدید پای هج باید قیمتی بهتر از ورود قبلی همان پا داشته باشد (Buy بعدی حتماً پایین‌تر، Sell بعدی حتماً بالاتر). این یک **hard constraint** است، نه ترجیح — یعنی حتی اگر سیگنال باز کردن فعال شود، تا این شرط برقرار نشود ورود انجام نمی‌شود.
- **Reversal-Swap:** وقتی روند برمی‌گردد، پای «روند» بسته می‌شود (سود قفل)، پای «هج» نقش پای روند جدید را می‌گیرد، و یک پای هج تازه در جهت مخالف باز می‌شود.

---

## ۲) ابزارهای واقعی HelpMe که برای AFA قابل استفاده‌اند

بعد از گشتن کامل کد v14.35، این بلوک‌ها دقیقاً چیزی هستند که AFA لازم دارد — همه‌شان از قبل موجود و کالیبره‌شده‌اند:

| ابزار HelpMe | چیست | نقش پیشنهادی در AFA |
|---|---|---|
| **TrendScore** (`trend_score` / `g_csvScore`) | رأی‌گیری وزن‌دار فاصلهٔ قیمت از EMA در ۵ تایم‌فریم (M5×1, M30×3, H1×4, D1×2, W1×1)، خروجی پیوسته ۱۰۰± | **موتور اصلی Reversal-Swap** — پیوسته و چندتایم‌فریمی است، دقیقاً چیزی که برای تشخیص برگشت روند با تأخیر کم لازم داریم |
| **Hurst Exponent** (`CalculateHurstExponent`, R/S روی H1، پنجرهٔ غلتان با آستانهٔ صدک ۹۰) | H<0.5 = بازار میانگین‌گرا (اصلاح‌پذیر)، H>0.5 = بازار روندی خالص | **فیلتر شرایط باز کردن پای هج** — فقط وقتی H زیر آستانه‌ست اصلاح واقعی رخ می‌دهد؛ اگر H بالا باشد، «اصلاح» ممکن است در واقع ادامهٔ روند باشد و هج ضرر واقعی بدهد |
| **MktPhase** (`Calc_MktPhase`, نسبت ADR روزانه/۶۰روزه) | Range / Neutral / Trend! در سطح D1 | **فیلتر روزانهٔ انتخاب حالت معامله** — AFA در فاز Range خالص، فرصت اصلاح کم دارد (پای روند ضعیف)؛ در فاز Trend! خالص، اصلاح‌ها می‌توانند غیرمنتظره کوچک/بزرگ باشند. حالت ایده‌آل AFA به احتمال زیاد **Neutral رو به Trend خفیف** است |
| **Spike Detector** (`Spike_TFScore`, ۵ تایم‌فریم M15-D1، بر پایهٔ Range/ATR + نسبت بدنه) | Normal / Warning / Spike | **گیت زمان‌بندی ورود/خروج هج** — هرگز پای هج را دقیقاً وسط یک Spike باز یا نبند؛ نویز باعث می‌شود قیمت "بهتر" ثبت‌شده در واقع تصادفی باشد |
| **FlowEvaluate** (همبستگی جفت‌ارزهای خواهر) | تأیید/رد جهت با ارزهای همبسته | **تأیید ثانویهٔ Reversal-Swap** — قبل از سوییچ نقش، چک شود که ارزهای خواهر هم همین برگشت را نشان می‌دهند یا این فقط نویز تک‌ارزی است |
| **Crisis composite light** | Green/Yellow/Red/Orange | **کلید امن کلی (kill-switch)** — در Crisis=Red، نه باز کردن هج جدید مجاز است نه سوییچ نقش؛ فقط منتظر می‌مانیم |

نکتهٔ مهم: هیچ‌کدام از این‌ها برای AFA بازنویسی نمی‌شوند — همه‌شان به‌صورت import/فراخوانی از HelpMe استفاده می‌شوند تا کالیبراسیون چندماهه‌ای که رویشان انجام شده هدر نرود.

---

## ۳) قانون مکانیکی پیشنهادی برای پای هج (بدون پیش‌بینی، بدون look-ahead)

### باز کردن پای هج (مثال: Sell = روند، Buy = هج)
باز می‌شود وقتی **همهٔ** شرایط زیر هم‌زمان برقرار باشند:
1. قیمت به‌اندازهٔ `X × ATR(H1)` برخلاف پای روند حرکت کرده (اصلاح شروع شده — قابل‌اندازه‌گیری، نه حدسی)
2. Hurst(H1) زیر آستانهٔ میانگین‌گرایی (اصلاح واقعی محتمل‌تر از ادامهٔ روند)
3. Spike ≠ Full Spike در تایم‌فریم ورود (وارد نویز نمی‌شویم)
4. Crisis ≠ Red
5. قیمت باز شدن، از آخرین Buy بسته‌شده **پایین‌تر** باشد (قانون طلایی علی)

### بستن پای هج
بسته می‌شود وقتی **یکی** از این‌ها رخ دهد:
- قیمت به نسبت مشخصی از اندازهٔ اصلاح برگشته (مثلاً سطح فیبو یا سطح ADR-based قابل‌محاسبه از همان زیرساخت MktPhase)
- TrendScore دوباره در جهت اصلی روند مومنتوم گرفت (آستانه از تجربهٔ Killer's TrendScore قابل‌وام‌گیری)
- Spike به Full Spike رسید (خروج ایمنی، نه سودی)

### باز کردن Buy بعدی
فقط اگر قیمت از محل بسته‌شدن Buy قبلی، دوباره حداقل `Y پیپ` (یا `Z × ATR`) پایین‌تر رفت — یعنی واقعاً یک «فرکتال» جدید اصلاح، نه صرفاً نوسان کوچک.

---

## ۴) قانون مکانیکی پیشنهادی برای Reversal-Swap

سوییچ نقش (پای روند فعلی بسته می‌شود، پای هج نقش روند جدید می‌گیرد) وقتی:
1. TrendScore چندتایم‌فریمی علیه جهت فعلی پای روند، از یک آستانهٔ معنادار عبور کند (نه فقط رد شدن از صفر — باید persistence داشته باشد، مثلاً N کندل H1 پشت‌سرهم)
2. FlowEvaluate روی ارزهای خواهر هم همین جهت مخالف را تأیید کند (رد کردن نویز تک‌ارزی)
3. Crisis در وضعیت بحرانی نباشد (سوییچ وسط بحران خطرناک است)

این دقیقاً معادل منطقی همان چیزی است که در HelpMe برای Killer TrendScore و Crisis از قبل پیاده شده — فقط به‌جای صرفاً هشدار دادن، اینجا یک اکشن معاملاتی (بستن/سوییچ) واقعی است.

---

## ۵) چهار شرط طلایی نهایی AFA (به‌روزشده)

1. روند باید با TrendScore مشخص باشد؛ اگر عوض شد → Reversal-Swap
2. ورود بعدی پای هج همیشه در قیمت بهتر از ورود قبلی همان پا (hard constraint)
3. حجم پای هج ثابت و کوچک — بدون مارتینگل
4. **(جدید)** کیفیت و سرعت تشخیص Reversal مهم‌تر از هر پارامتر دیگری است — چون تنها سناریوی فاجعه‌بار AFA، تشخیص دیرهنگام یا اشتباه برگشت روند در پای «روند» (که هرگز به‌خودی‌خود بسته نمی‌شود) است

---

## ۶) پارامترهای باز برای کالیبراسیون در گام ۲ (بک‌تست)

- `X` (ضریب ATR برای شروع اصلاح / باز کردن هج)
- آستانهٔ Hurst برای فیلتر
- نسبت بازگشت برای بستن هج (فیبو یا ADR-based)
- `Y`/`Z` (حداقل فاصلهٔ ورود بعدی)
- آستانه و persistence لازم برای TrendScore جهت Reversal-Swap
- انتخاب جفت‌ارز نهایی از بین EURCHF / EURGBP / CADCHF (کدام روی دادهٔ واقعی، بیشترین تعداد «اصلاح قابل‌بهره‌برداری بدون فاجعه» را داشته)

---

## ۷) گام بعدی

اگر این چارچوب تأیید بشه، گام ۲ شروع می‌شه: انتخاب جفت‌ارز از روی دادهٔ واقعی، و نسخهٔ اول کد قابل‌بک‌تست (بدون UI، فقط منطق) با این پارامترها به‌صورت input قابل‌تغییر — تا رو دادهٔ واقعی کالیبره‌شون کنیم.
