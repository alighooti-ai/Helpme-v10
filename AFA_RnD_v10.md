# AFA — سند R&D یکپارچه، نسخهٔ ۱۰ (تاریخچهٔ کامل + مسیر تحقیق + وضعیت نسخهٔ ۵ + یافته‌های AI4 + بازخورد AI1/AI2 + حل معمای GBPNZD + نتایج واقعی MT5)

**تاریخ:** ۲۰ اوت ۲۰۲۶ · **وضعیت:** baseline واقعی MT5 منفیه (PF=۰٫۲۹) — این الان بحرانی‌ترین مسئلهٔ باز پروژه‌ست · **بسته شد:** معمای GBPNZD (در سطح پایتون) · **باز و فوری:** چرا پایتون مثبت ولی MT5 واقعی منفیه

## چطور این سند را بخوانی
- **بخش الف:** کل نسخهٔ ۴، بدون یک کلمه تغییر (مرجع تاریخی و پایهٔ همهٔ ارجاع‌ها).
- **بخش ب:** بعد از نسخهٔ ۴ چه اندازه‌گیری شد، چه چیزی شکست خورد و چرا سراغ چه چیزی رفتیم.
- **بخش ج:** سیستم فعلی (نسخهٔ ۵) — دقیقاً جایگزین بخش‌های باطل‌شدهٔ الف.
- **بخش د:** یافته‌های عددی و گلوگاه‌های واقعی.
- **بخش ه:** آخرین کاری که باید اجرا شود + دفاع از آن.
- **بخش و:** وضعیت دو پیش‌نویس MQL5 و نقشهٔ «با نسخهٔ ۵ چه چیزی ساختنی است».

---
---

# بخش الف — نسخهٔ ۴ کامل (بدون تغییر)

# تاریخچهٔ پروژهٔ AFA — از ایده تا امروز (نسخهٔ ۴ — راهنمای اجرا با تقسیم‌کار واقعی)

این سند برای اینه که هرکی بخونتش، از صفر تا الان رو بفهمه: چی می‌خواستیم، چی ساختیم، کجا گیر کردیم، چطور مسیر رو عوض کردیم، و الان دقیقاً کجای کاریم.

> **تفاوت نسخهٔ ۴ با نسخهٔ ۳:** محتوای فنی (بخش‌های ۱ تا ۷، و همهٔ معیارهای قبولی/ردی بخش ۸) دقیقاً همون نسخهٔ ۳ است — که خیلی کامل‌تر و دقیق‌تر از پیش‌نویس‌های قبلی بود. تنها تغییر: بخش ۸ در نسخهٔ ۳ طوری نوشته شده بود که انگار خودِ خواننده باید پایتون نصب کنه، اسکریپت بنویسه، تست واحد بسازه. این با نحوهٔ واقعی کار علی و دستیار هوش‌مصنوعی نمی‌خونه — علی پایتون نمی‌نویسه؛ ایده می‌ده، مدیریت می‌کنه، و کاری که فقط خودش می‌تونه انجام بده (اجرای MT5، دادهٔ OHLC، حساب دمو/واقعی) رو انجام می‌ده. **هر زیربخش گام ۰ تا ۱۰ حالا برچسب [من]/[تو] داره** تا مشخص باشه واقعاً کی چیکار می‌کنه — بدون تغییر در خودِ معیارها.

> **تفاوت نسخهٔ ۳ با نسخهٔ ۲ (خلاصهٔ تغییرات این نسخه):**
> ۱) رأی‌گیری ۶ مدل دیگر بولین نیست؛ **قدرت اجماع** به‌صورت عدد پیوسته وارد فیلتر رنج (گیت ۲) و اندازهٔ حجم پایه شد.
> ۲) `K` از عدد ثابت `1.0×ATR` به **گام تطبیقی** بر پایهٔ ER و ترکیب ATR کوتاه/بلند با clamp تبدیل شد.
> ۳) گیت ۳ یک شرط **شتاب مومنتوم** (`d(ER)/dt > 0`) گرفت تا احتمال رسیدن به `n ≥ 3` بالا برود.
> ۴) هزینهٔ **اسپرد لحظهٔ AP** جدا از اسپرد میانگین اندازه‌گیری و در فرمول فاز ۱ لحاظ شد.
> ۵) فاز ۲: **سوآپ سه‌برابری چهارشنبه‌شب** و کاهش خودکار ۱۵٪ در `T_max`.
> ۶) لایهٔ **محافظ سراسری حساب** (Daily/Weekly Max Drawdown + سقف ریسک حساب + سقف همبستگی) اضافه شد (بخش ۵.۹).
> ۷) **سازگارکنندهٔ خودکار حساب‌های Netting** (پراپ‌فرم‌ها و آمریکا) رسمی شد.
> ۸) پیش از MQL5، **شبیه‌سازی ماشین‌حالت در پایتون + تست واحد هر انتقال** به مسیر اجرا اضافه شد.
> ۹) بخش جدید **۸ — راهنمای صفر تا صد ساخت EA** با گام‌های اجرایی، معیار قبولی و راه‌حل شکست هر گام.
>
> **تفاوت نسخهٔ ۲ با نسخهٔ ۱:** بخش‌های ۱ تا ۴ عیناً حفظ شده‌اند (فقط یک پانویس به بخش ۴ اضافه شده). بخش ۵ به‌طور کامل بازنویسی شده: فاز صفر حالا خودش یک فاز فعال با فیلتر رنج، کول‌داون و تأیید ورود است (فاز چهارم جداگانه‌ای وجود ندارد — آن وظیفه به فاز صفر منتقل شد)، فاز ۱ صراحتاً به «بستن کامل → فاز صفر» ختم می‌شود، فاز ۲ از «مارتینگل سطحی» به «قفل حجم خالص + گرید نوسان‌گیر + هدف صفرِ نزولی + بودجهٔ ریسک» تبدیل شده، فاز ۳ از «تأخیر زمانی چند ساعته» به «تأیید مبتنی بر فاصلهٔ قیمت برحسب ATR، به‌صورت نامتقارن» تغییر کرده، و یک جدول اولویت برای برخورد هم‌زمان رویدادها اضافه شده است.

---

## ۱. اول چی می‌خواستیم؟ (مانیفست AFA)

بعد از یک سال تجربه با یک ربات دیگه (Xmoon — یک ربات مارتینگل mean-reversion)، علی تصمیم گرفت یک سیستم جدید بسازه با این ایده‌ها:

- **دو پا:** یک پای «روند» که همیشه باز می‌مونه و سود روند رو نگه می‌داره، و یک پای «هج» که برخلاف روند بازه و از اصلاحات کوچیک سود می‌گیره.
- **قانون طلایی:** هر بار که پای هج دوباره باز می‌شه، باید توی قیمت بهتری نسبت به دفعهٔ قبل باشه.
- **هدف:** حدود ۲۰۰ معامله در ۷ ماه (نه ۵ تا)، با درادون کنترل‌شده.
- **ریسک:** فلسفهٔ اصلی این بود که «بدون Stop Loss» کار کنیم — چون تشخیص سریع و دقیق برگشت روند، خودش محافظه، نه یه استاپ ثابت.

---

## ۲. چی ساختیم؟ و چرا کار نکرد؟ (ماجرای V7)

کد اولیه (V7) برای تشخیص روند از یه چیز به اسم TrendScore استفاده می‌کرد، به‌علاوهٔ چند تا فیلتر اضافه (فیلتر ساختار قیمت، فیلتر قدرت روند). بعد از ماه‌ها بک‌تست و تنظیم پارامتر، این‌ها رو فهمیدیم:

1. **فیلترها همدیگه رو خفه می‌کردن.** یکی از فیلترها (شکست ساختار قیمت) فقط ۱۱٪ وقت‌ها اجازهٔ ورود می‌داد؛ به همین خاطر خیلی از پارامترهای دیگه هرچقدر عوضشون می‌کردیم، هیچ اثری نداشتن — چون گلوگاه اصلی جای دیگه‌ای بود.
2. **وقتی اون فیلتر رو برداشتیم، فرکانس معامله دو برابر شد ولی کیفیت بدتر شد** — سود کمتر، ضرر بیشتر. یعنی اون فیلتر واقعاً داشت یه کار درست انجام می‌داد (فقط بیش‌ازحد سخت‌گیر بود).
3. **کشف مهم:** نسبت سود به ضرر هر معامله خیلی کج بود — هر باخت تقریباً ۶ برابر هر بردِ. با این نسبت، حتی نرخ‌برد بالای ۸۵٪ لازم بود که فقط سربه‌سر بشیم. یه افت کوچیک تو نرخ‌برد (که با شل‌کردن فیلترها می‌اومد) کافی بود کل سیستم رو منفی کنه.
4. **کشف مهم‌تر:** با یه تست جدا (بدون هیچ منطق معامله‌ای، فقط اندازه‌گیری خالص)، فهمیدیم خودِ TrendScore رابطهٔ یک‌نواختی با آیندهٔ قیمت نداره — یه «قوز» داره. TrendScore متوسط (۲۵ تا ۶۰) واقعاً سیگنال خوبی بود، ولی TrendScore خیلی قوی (۶۰+) برعکس عمل می‌کرد — نشونهٔ خستگی/اشباع روند بود، نه ادامه‌ش. تقریباً نیمی از «سیگنال‌های معتبر» فعلی داشتن این edge رو خنثی می‌کردن.

بعد از این‌ها، چند بار سعی کردیم با تغییرات کوچیک (یکی‌یکی، طبق روش علمی) درستش کنیم — ولی هیچ‌کدوم از V7 اصلی بهتر نشد. این خودش یه علامت بود: داشتیم روی همون ۷ ماه بک‌تست overfit می‌کردیم، نه واقعاً مشکل رو حل می‌کردیم.

---

## ۳. تصمیم بزرگ: توقف AFA و ساخت جدا یه «موتور تشخیص روند»

اینجا فهمیدیم مشکل اصلی، خودِ منطق هج/روند نیست — **مشکل اینه که هیچ‌وقت یه ابزار تشخیص جهت داشتیم که واقعاً درست ثابت‌شده باشه.** پس تصمیم گرفتیم AFA رو کامل بذاریم کنار و یه ابزار جدا بسازیم که فقط یه کارو بکنه: بگه الان صعودی هستیم یا نزولی.

- ۶ روش مختلف تست کردیم (میانگین متحرک، ADX، ساختار قیمت، Supertrend، و دو تا مدل جدید طراحی‌شده).
- هر کدوم رو با یه روش عینی (نه با چشم) سنجیدیم: قیمت واقعی رو با ZigZag (فقط نوسان‌های بزرگ واقعی) مقایسه کردیم، دیدیم هر مدل چقدر دیر واکنش نشون می‌ده و چقدر بی‌خودی نوسان می‌کنه.
- ضعیف‌ترها رو حذف کردیم، بهترها رو نگه داشتیم، و از ترکیبشون یه «رأی‌گیری اکثریت» ساختیم که همیشه یه جواب مشخص می‌ده.
- روی ۸ جفت‌ارز مختلف (رنجی، رونددار، اسپایکی) و ۴.۶ سال تست شد — همه‌جا بالای خط نویز (۵۰٪)، بین ۵۶٪ تا ۷۲٪ دقت.

نتیجه: الان یه ابزار تشخیص روند داریم که واقعاً — نه با حدس، با آزمایش مستقل — اعتبارسنجی شده.

**نکتهٔ مهمی که از همین تست بیرون اومد و در بخش ۵ استفاده می‌شه:** پراکندگی دقت بین جفت‌ارزها تصادفی نیست — پایین‌ترین دقت‌ها (نزدیک ۵۶٪) مالِ جفت‌ارزهای رنجی مثل EURGBP بود و بالاترین‌ها مالِ جفت‌ارزهای رونددار. یعنی خطای موتور رژیم با «رنجی‌بودن بازار» همبستگی مستقیم داره. این دقیقاً دلیلیه که در نسخهٔ جدید، یه فیلتر رنج **قبل از** هر ورودی گذاشتیم.

---

## ۴. تست خالص: آیا خودِ این سیگنال جهت، پول درمیاره؟

قبل از این‌که بریم سراغ منطق پیچیدهٔ هج، یه تست خیلی ساده زدیم: فقط طبق جهتی که موتور رژیم می‌گه معامله باز کن، با TP/SL ثابت، هیچ منطق دیگه‌ای نه.

- اول با TP/SL درصدی امتحان کردیم — نتیجه ضعیف بود، چون درصد ثابت هیچ ربطی به نوسان واقعی بازار نداشت.
- بعد TP/SL رو بر اساس ATR واقعی (خودکار، نه دستی) ساختیم — نتیجه بهتر شد (نرخ‌برد ۴۹٪، سود کوچیک مثبت).
- فهمیدیم: معاملاتی که وسط راه رژیم عوض می‌شد، خیلی بد نتیجه می‌داد (نرخ‌برد ۳۰٪)، ولی معاملاتی که رژیم کل مدت ثابت می‌موند، خوب بود (۵۸٪ — نزدیک دقت واقعی موتور).
- سعی کردیم فیکسش کنیم: «هر وقت رژیم عوض شد، فوری ببند» — ولی این زیادی حساس شد و به نویزهای کوچیک موتور رژیم واکنش نشون داد، نتیجه بدتر شد (نرخ‌برد ۴۰٪).
- **جایی که الان هستیم:** فهمیدیم راه‌حل نه «هیچ‌وقت واکنش نشون نده» است و نه «فوری به هر تکون واکنش نشون بده» — باید یه تأخیر تأییدی قبل از بستن باشه.

> **اصلاح نسخهٔ ۲:** در نسخهٔ قبلی این تأخیر «چند ساعت» نوشته شده بود. این دقیقاً همون اشتباهیه که یک‌بار با TP/SL درصدی کردیم — یه عدد دلبخواه که هیچ ربطی به نوسان واقعی بازار نداره. «۳ ساعت» در یه بازار آروم یعنی تقریباً هیچ حرکتی، و در یه بازار پرنوسان یعنی یه حرکت عظیم. پس در نسخهٔ ۲، معیار تأیید از **زمان** به **فاصلهٔ قیمت نسبت به ATR** تغییر کرده و علاوه‌بر اون **نامتقارن** شده (جزئیات در بخش ۵ ← فاز ۳).

---

## ۵. الان کجاییم: بازگشت به AFA با منطق هج بازطراحی‌شده (نسخهٔ ۳)

با یه موتور تشخیص روند قابل‌اعتماد در دست، برگشتیم سراغ منطق هج/روند AFA.

### ۵.۰ نکتهٔ پایه‌ای: این یه ماشین‌حالته، نه یه قانون ثابت

سیستم همیشه دقیقاً در یکی از این ۴ فاز است، و **باید بدونه الان کدومه** (این باید در یک متغیر وضعیتِ ذخیره‌شده نگه‌داری بشه — و بین ری‌استارت‌های ترمینال هم پایدار بمونه، نه این‌که از روی پوزیشن‌های باز حدس زده بشه):

- **فاز صفر (خنثی / دروازهٔ ورود):** هیچ پوزیشن باز مدیریت‌شده‌ای نداریم. سیستم اینجا فقط سه کار می‌کنه: خنک شدن، فیلتر کردن بازار، و گرفتن تأیید ورود.
- **فاز ۱ (تهاجمی):** قیمت داره با روند پیش می‌ره (AP پشت AP).
- **فاز ۲ (تدافعی/بازیابی):** قیمت برخلاف روند رفته و هج به TP خورده.
- **فاز ۳ (تغییر رژیم):** یه لایهٔ روی فاز ۱ یا ۲، نه یه فاز مستقل — چون رفتارش به این بستگی داره که از کدوم فاز اومده.

> **چرا «فاز ۴» حذف شد:** در پیش‌نویس قبلی یه فاز چهارم برای «بعد از بسته‌شدن، صبر و تأیید مجدد قبل از ورود دوباره» در نظر گرفته شده بود. این دقیقاً همون کاریه که فاز صفر باید بکنه — دو تا نام برای یک وضعیت، فقط باگ و ابهام تولید می‌کنه. پس اون وظیفه کامل به داخل فاز صفر منتقل شد و فاز صفر از یه «حالت خالی منفعل» به یه **دروازهٔ فعال** تبدیل شد.

**اصل کلیدی:** هر مسیر خروج از هر فازی، **همیشه** به فاز صفر ختم می‌شه. هیچ گذر مستقیمی بین فاز ۱ و فاز ۲ وجود نداره. این عمداً است: هر بار که یه چرخه تموم می‌شه، سیستم باید از دروازه رد بشه (کول‌داون + فیلتر + تأیید) تا حلقهٔ Whipsaw شکل نگیره.

```text
                 ┌──────────────────────────────────────────┐
                 │            فاز صفر (دروازه)              │
                 │  کول‌داون → فیلتر رنج → تأیید → ورود جفت  │
                 └───────────────┬──────────────────────────┘
                                 │ باز کردن جفت (روند + هج)
                                 ▼
                        ┌────────────────┐
                        │ اولین رویداد؟  │
                        └───┬────────┬───┘
             AP (قیمت +K)   │        │  هج TP خورد (قیمت −K)
                            ▼        ▼
                     ┌──────────┐  ┌──────────┐
                     │  فاز ۱   │  │  فاز ۲   │
                     └────┬─────┘  └────┬─────┘
                          │ بستن کامل   │ بستن کامل
                          └──────┬──────┘
                                 ▼
                            فاز صفر
```

---

### ۵.۱ فاز صفر — دروازهٔ ورود (بازنویسی‌شده)

فاز صفر سه گیت پشت‌سرهم داره. **همه باید هم‌زمان پاس بشن**، و ترتیبشون مهمه (ارزان‌ترین و قطعی‌ترین اول).

#### گیت ۱: کول‌داون (Cooldown)

**چرا:** درسی که مستقیم از تست NoLogicEA گرفتیم — ورود فوری در کندل بعد از یه خروج، یه حلقهٔ Whipsaw می‌سازه: می‌بندی، فوری در جهت مخالف/همان جهت وارد می‌شی، دوباره استاپ می‌خوری. هزینهٔ اسپرد چند برابر می‌شه بدون این‌که هیچ اطلاعات جدیدی وارد تصمیم شده باشه.

**قانون:** بعد از هر بسته‌شدن چرخه، سیستم به مدت `InpCooldownBars` کندل بسته‌شده **هیچ ورودی نمی‌گیره** — بدون استثنا.

**کول‌داون تطبیقی (توصیه‌شده):** طول کول‌داون به دلیل خروج بستگی داره، چون همهٔ خروج‌ها اطلاعات یکسانی حمل نمی‌کنن:

| دلیل خروج قبلی | ضریب کول‌داون |
|---|---|
| فاز ۱ با سود بسته شد (TP هج / سقف لوپ) | `1 ×` |
| فاز ۲ با موفقیت (به هدف صفر/سود رسید) | `1.5 ×` |
| فاز ۲ با پذیرش ضرر (بودجهٔ ریسک تموم شد) | `3 ×` |
| خروج به‌خاطر فلیپ رژیم (فاز ۳) | `2 ×` |

`Cooldown_bars = InpCooldownBars × multiplier`

**علاوه بر این — کول‌داون جهت‌دار:** اگر چرخهٔ قبلی با ضرر بسته شد، ورود **در همان جهتِ** چرخهٔ قبلی تا پایان کول‌داون ممنوعه. ورود در جهت مخالف فقط بعد از `InpCooldownBars` پایه مجازه (چون فلیپ رژیم، خودش اطلاعات جدیده).

#### گیت ۲: فیلتر «بازار رنجه، معامله نکن»

**چرا:** بخش ۳ نشون داد دقت موتور رژیم روی جفت‌ارزهای رنجی (EURGBP) تا ۵۶٪ افت می‌کنه. ولی «رنجی‌بودن» یه ویژگی ثابتِ جفت‌ارز نیست — یه ویژگی لحظه‌ایِ بازاره. EURUSD هم می‌تونه دو هفته رنج بزنه. پس فیلتر باید **لحظه‌ای** باشه، نه یه لیست سیاه از جفت‌ارزها.

**چهار سنجهٔ مستقل (همه باید پاس بشن):**

1. **کارایی حرکت (Efficiency Ratio / Kaufman):**
   ```text
   ER = |Close[0] − Close[N]| / Σ|Close[i] − Close[i+1]|   ,  i = 0..N−1
   ```
   `ER` بین ۰ و ۱ است. نزدیک ۱ = حرکت مستقیم و رونددار. نزدیک ۰ = رفت‌وبرگشت بدون پیشرفت.
   **شرط:** `ER ≥ InpMinER` (پیشنهاد شروع: `N = 20`، `InpMinER = 0.30`).

2. **پایداری رأی موتور رژیم:**
   در `InpRegimeLookback` کندل اخیر (پیشنهاد: ۵۰)، تعداد دفعاتی که رأی موتور عوض شده (`flips`) رو بشمار:
   ```text
   FlipRate = flips / InpRegimeLookback
   ```
   **شرط:** `FlipRate ≤ InpMaxFlipRate` (پیشنهاد: ۰٫۱۲ — یعنی حداکثر ۶ فلیپ در ۵۰ کندل).
   این سنجه مستقیم‌ترین سنجه است، چون داره مستقیماً می‌گه «خودِ ابزار تصمیم‌گیری من الان بی‌ثباته».

3. **عرض مؤثر رنج نسبت به هزینه:**
   ```text
   K ≥ InpMinK_ATR × ATR      و     K ≥ InpMinK_Spread × spread
   ```
   اگر گام هج (`K`) نسبت به اسپرد کوچیک باشه، حتی منطق درست هم زیر هزینه دفن می‌شه. (پیشنهاد: `InpMinK_Spread = 6`).

4. **قدرت اجماع موتور رژیم (سنجهٔ پیوسته — افزودهٔ نسخهٔ ۳):**

   **چرا:** رأی‌گیری اکثریتِ ۶ مدل، یک عدد پیوسته (مثلاً ۴ از ۶ در برابر ۶ از ۶) را به یک بولین «صعودی/نزولی» تبدیل می‌کند و اطلاعات را دور می‌ریزد. اجماع قوی‌تر تقریباً همیشه با دقت بالاتر همبسته است — دقیقاً همان الگویی که در کشف «قوز TrendScore» (بخش ۲) دیدیم: شدت سیگنال، خودش اطلاعات دارد و نباید له شود.

   ```text
   Consensus = (تعداد مدل‌های موافق با جهت برنده) / 6        →  عددی بین 0.5 و 1.0
   ```

   **شرط گیت:** `Consensus ≥ InpMinConsensus` (پیشنهاد شروع: `0.67` یعنی حداقل ۴ از ۶).

   **استفادهٔ دوم — مقیاس‌دهی حجم:** همان عدد، اندازهٔ حجم پایه را هم تعیین می‌کند (به‌جای حجم ثابت):
   ```text
   V_base = V_risk × ( InpConsFloor + (1 − InpConsFloor) × (Consensus − 0.5) / 0.5 )
   ```
   با `InpConsFloor = 0.5` یعنی: اجماع ۴ از ۶ ≈ نصف حجم کامل، اجماع ۶ از ۶ = حجم کامل.
   **سقف سخت:** `V_base` هرگز نمی‌تواند از سقفی که بودجهٔ ریسک (`InpRiskBudgetPct`) اجازه می‌دهد بیشتر شود — مقیاس‌دهی اجماع فقط می‌تواند حجم را **کم** کند، هرگز از سقف بودجه بالاتر نمی‌برد.

**خروجی گیت ۲ یک بولین سخته:** پاس نشد = هیچ ورودی، دوباره کندل بعد چک کن. هیچ حالت «نصفه‌ورود» وجود نداره.

**هیسترزیس روی فیلتر رنج:** برای این‌که خودِ فیلتر دچار whipsaw نشه، برای **فعال‌شدن** فیلتر (اعلام رنج) از آستانهٔ اصلی و برای **غیرفعال‌شدن** (اعلام پایان رنج) از آستانهٔ سخت‌گیرانه‌تری استفاده می‌کنیم:
```text
ورود به وضعیت رنج:    ER < InpMinER
خروج از وضعیت رنج:    ER > InpMinER × 1.25
```

#### گیت ۳: تأیید ورود

موتور رژیم یه جهت می‌ده، ولی رأی یک کندل کافی نیست.

**شرط:** رأی موتور باید حداقل `InpEntryConfirmBars` کندل بسته‌شدهٔ پیاپی **بدون تغییر** باشه (پیشنهاد شروع: ۲ تا ۳).

**شرط دوم گیت ۳ — شتاب مومنتوم (افزودهٔ نسخهٔ ۳):**

**چرا:** طبق ریاضی فاز ۱ (بخش ۵.۲)، سیستم در `n=1` و `n=2` سربه‌سر یا کمی منفی است و سودش از `n ≥ 3` شروع می‌شود. پس ورودی که در آن بازار **در حال کند شدن** است، حتی اگر جهتش درست باشد، احتمالاً به AP سوم نمی‌رسد و فقط هزینه تولید می‌کند.

```text
dER = ER(کندل جاری) − ER(کندل قبلی)
شرط ورود:  dER > 0            (کارایی حرکت در حال افزایش است، نه کاهش)
```
برای حذف نویز، به‌جای اختلاف تک‌کندلی از شیب میانگین `InpER_SlopeBars` کندل اخیر استفاده کن (پیشنهاد: ۳).

**نکتهٔ ضدِ overfit:** این عدد باید کوچیک بمونه. `InpEntryConfirmBars` بزرگ = دیر رسیدن به روند = دقیقاً همون تلهٔ V7 که فیلتر ساختار قیمت فقط ۱۱٪ اجازهٔ ورود می‌داد.

#### اجرای ورود

اگر هر سه گیت پاس شد، در **باز شدن کندل بعدی**:
**تعیین `K` در لحظهٔ ورود — گام تطبیقی هج (بازنویسی‌شدهٔ نسخهٔ ۳):**

**مشکل نسخهٔ ۲:** `K = 1.0 × ATR` یک عدد ثابت بود و دو ایراد داشت:
- ATR با پنجرهٔ ۱۴ کندل، در یک جهش ناگهانی نوسان (خبر) چند کندل **عقب می‌ماند** و `K` نامتناسب با بازار واقعی می‌شود.
- در بازار نویزدار، `K` کوچک باعث تریگر زودهنگام AP و افت نسبت سود به کمیسیون می‌شود.

**راه‌حل — دو اصلاح روی هم:**

۱) **ترکیب ATR کوتاه و بلند + clamp** (پایداری + واکنش‌پذیری با هم):
```text
ATR_blend = w × ATR(InpATRFast)  +  (1 − w) × ATR(InpATRSlow)      (پیشنهاد: 5 و 20 ، w = 0.5)
clamp:  |ATR_blend[i] − ATR_blend[i−1]| ≤ InpATRMaxStepPct × ATR_blend[i−1]      (پیشنهاد: 0.20)
```
یعنی ATR مؤثر می‌تواند سریع واکنش نشان دهد ولی در هر کندل بیش از ۲۰٪ نمی‌پرد.

۲) **مقیاس‌دهی بر اساس کارایی حرکت (Adaptive K Scaling):**
```text
K = ATR_blend × ( 1 + λ × (1 − ER) )        (پیشنهاد: λ = InpKLambda = 0.5)
```
- بازار شفاف و رونددار (`ER` بالا) → `K` کوچک‌تر → AP سریع‌تر جمع می‌شود → به `n ≥ 3` زودتر می‌رسیم.
- بازار نویزدار (`ER` پایین) → `K` بزرگ‌تر → AP دیرتر تریگر می‌شود و هزینهٔ رفت‌وبرگشت کاهش می‌یابد.

**کف‌های سخت (همچنان برقرار):** `K ≥ InpMinK_ATR × ATR_blend` و `K ≥ InpMinK_Spread × spread`.
**قفل `K` در طول چرخه:** `K` **فقط یک‌بار، در لحظهٔ ورود** محاسبه و تا پایان همان چرخه ثابت نگه داشته می‌شود. اگر `K` وسط لوپ AP تغییر کند، فاصلهٔ سطوح ناهمگون می‌شود و ریاضیِ `n(n+1)/2` بی‌اعتبار می‌گردد.

- پای روند باز می‌شه با حجم `V_base` در جهت موتور رژیم.
- پای هج باز می‌شه با حجم `V_base` در جهت مخالف، با `TP_hedge = K` (فاصله‌ای برحسب ATR).
- متغیر وضعیت هنوز **فاز صفر** می‌مونه (وضعیت «جفت باز، فاز نامعلوم»)، چون هنوز معلوم نیست به کدوم سمت می‌ره.

#### تعیین تکلیف فاز

اولین رویدادی که رخ بده، فاز رو تعیین می‌کنه و ثبت می‌شه:

| رویداد | فاز جدید |
|---|---|
| قیمت `+K` در جهت روند رفت (اولین AP) | فاز ۱ |
| هج به `TP_hedge` خورد (قیمت `−K` رفت) | فاز ۲ |
| رژیم فلیپ کرد قبل از هر دوی این‌ها | بستن هر دو پا فوراً، برگشت به فاز صفر (هنوز هیچ سرمایه‌گذاری روی جهت نکردیم؛ اینجا تأخیر بی‌معنیه) |

---

### ۵.۲ فاز ۱ — تهاجمی: قیمت با روند پیش می‌ره (AP پشت AP)

این حالت **موتور اصلی پول‌سازی سیستمه** — نه یه سود کوچیک ثابت، بلکه رشد مرکب.

مکانیزم دقیق: پای روند **هیچ‌وقت بسته نمی‌شه** — فقط تجمیع می‌شه. فقط پای هج در هر نقطه (به اسم AP) عوض می‌شه:

- شروع: روند #۱ + هج #۱ باز می‌شه.
- قیمت K پیش می‌ره (اولین AP): هج #۱ بسته می‌شه (یه ضرر کوچیک و ثابت، چون تازه باز شده بود). روند #۱ **باز می‌مونه**. یه هج #۲ جدید (کنار روند #۲) باز می‌شه.
- قیمت K دیگه پیش می‌ره (AP دوم): هج #۲ بسته می‌شه (بازم همون ضرر کوچیک ثابت). روند #۱ و #۲ باز می‌مونن. هج #۳ باز می‌شه.
- ... و همینطور ادامه.

**ریاضی دقیق این حالت (اصلاح‌شدهٔ نسخهٔ ۲):**

بعد از `n` بار AP:
```text
سود شناور پاهای روند   = K · n(n+1)/2 · pip_value · V        ← رشد مربعی
ضرر هج‌های بسته‌شده     = n · h · pip_value · V               ← رشد خطی
که در آن   h = K + spread + commission_per_lot
```

> **تصحیح مهم نسبت به نسخهٔ ۱:** در نسخهٔ قبل، ضرر هر هج بسته‌شده «یه ضرر کوچیک» توصیف شده بود. این خوش‌بینانه است. هجی که در نقطهٔ باز شدن، `K` واحد بر خلافش حرکت می‌کنه، دقیقاً `K` ضرر می‌ده (به‌علاوهٔ هزینه). یعنی `h ≈ K`، نه یه چیز ناچیز. خبر خوب: **این نتیجهٔ نهایی رو خراب نمی‌کنه**، چون:
> ```text
> خالص ≈ K·V·pv · [ n(n+1)/2 − n·(1 + c) ]   ,   c = (spread+comm)/K
> ```
> برای `c` کوچیک، این عبارت در `n = 1` منفی، در `n = 2` تقریباً صفر، و از `n ≥ 3` به‌سرعت مثبت و شتاب‌دار می‌شه. پس **فاز ۱ در AP اول و دوم عملاً سربه‌سر یا کمی منفیه، و سودش از AP سوم به بعد شروع می‌شه.** این یه واقعیت طراحی است که باید در انتظارات و در بک‌تست دیده بشه، نه یه ایراد.

> **اصلاح نسخهٔ ۳ — اسپرد لحظهٔ AP با اسپرد میانگین یکی نیست:** فرمول بالا فرض می‌کند `c = (spread+comm)/K` برای همهٔ `n` سطح ثابت است. در عمل، AP دقیقاً در لحظه‌ای تریگر می‌شود که قیمت **سریع** حرکت کرده — و همان لحظه اسپرد بازتر است. یعنی `c` واقعی هنگام AP از `c` میانگین بزرگ‌تر است و نقطهٔ سربه‌سر ممکن است از `n=2` به `n=3` یا حتی `n=4` عقب بیفتد.
> **کاری که باید بشود:** در گام ۱ بخش ۷ (اندازه‌گیری توزیع `n`)، دو عدد جدا ثبت و گزارش شود:
> ```text
> spread_avg     = میانگین اسپرد روی کل تاریخچه
> spread_at_AP   = میانگین اسپرد فقط در کندل‌هایی که AP تریگر شده
> c_real         = (spread_at_AP + commission) / K
> ```
> و نقطهٔ سربه‌سر با `c_real` محاسبه شود، نه با `c` میانگین. اگر `spread_at_AP > 2 × spread_avg` بود، این یک هشدار جدی است: یا `K` باید بزرگ‌تر شود، یا آن جفت‌ارز/تایم‌فریم برای فاز ۱ مناسب نیست.

**نکتهٔ مهم:** معاملات این حالت **پکیج نمی‌شن** — هرکدوم جدا مدیریت می‌شه، نه با یه TP مشترک.

**قانون طلایی همچنان برقراره:** هر هج جدید در قیمتی بهتر از هج قبلی باز می‌شه (چون قیمت `K` در جهت روند جلو رفته). اگر به هر دلیلی (گپ، اسلیپیج) این شرط نقض بشه، AP اجرا **نمی‌شه** و سیستم منتظر کندل بعدی می‌مونه.

**۳ راه پایان لوپ — و در هر سه، نتیجه یکیه: بستن کامل و برگشت به فاز صفر**

1. **سقف تعداد لوپ (`InpAP_MaxLoopCount`).**
2. **فلیپ رژیم** → طبق قانون نامتقارن فاز ۳ (در فاز ۱ = **بستن فوری**، بدون تأخیر).
3. **قیمت برگرده و به TP واقعیِ هج فعلی بخوره.**

> **رفع تناقض نسخهٔ ۱:** در نسخهٔ قبلی، در شرط ۳ نوشته شده بود «این یعنی داریم می‌ریم تو حالت ۲» ولی چند خط پایین‌تر نوشته شده بود «همهٔ پوزیشن‌ها بسته می‌شن و برمی‌گردیم به فاز صفر». این دو تا با هم نمی‌خونن. **قانون قطعی نسخهٔ ۲: هر سه شرط پایان فاز ۱ = بستن کامل همهٔ پاها (روند و هج) → فاز صفر.** انتقال مستقیم فاز ۱ → فاز ۲ وجود ندارد. اگر شرایط بازار واقعاً یه چرخهٔ تدافعی جدید بطلبه، سیستم از دروازهٔ فاز صفر رد می‌شه و چرخهٔ بعدی خودش طبیعتاً وارد فاز ۲ می‌شه. این هم منطق رو ساده نگه می‌داره و هم سود انباشتهٔ فاز ۱ رو **قفل‌شده** می‌کنه به‌جای این‌که وارد یه چرخهٔ بازیابی بشه.

#### چرا یه «تریلینگ استاپ جدا روی کل سبد» لازم نیست

یه نگرانی مطرح شد: بعد از چند بار AP پشت‌سرهم، چند تا پای روند داریم که هرکدوم قیمت ورود متفاوتی دارن. اگه یه برگشت نسبتاً عمیق (ولی نه در حد تغییر رژیم) اتفاق بیفته، پاهای روندِ بالایی (که دیرتر باز شدن) ممکنه وارد ضرر بشن، درحالی‌که فقط پاهای اولیه سودن. نگرانی این بود که این باعث یه افت موقت ولی بزرگ توی Equity بشه، و شاید نیاز به یه تریلینگ استاپ جدا روی کل سبد باشه.

**جواب: این نگرانی درست دیده شده ولی راه‌حلش از قبل توی خودِ سیستم هست — سه شرط پایان لوپ.** یکی از اون سه شرط دقیقاً همینه: قیمت برگرده و به TP واقعیِ هج فعلی برسه. وقتی این اتفاق بیفته، همه‌چیز بسته می‌شه.

**یه مثال عددی (اعداد فقط برای نشون‌دادن مقیاسه):**
فرض کن تا AP4 پیش رفتیم (۵ پای روند باز، ۴ هج بسته‌شده). نه رژیم عوض شده و نه به سقف لوپ رسیدیم — قیمت فقط برمی‌گرده و به TP هج فعلی می‌خوره. اینجا همه‌چیز بسته می‌شه:

- ضرر: چهار هج قبلی، جمعاً ~۴K.
- سود: پاهای روند در سطح `K·(4+3+2+1+0) = 10K` بودن و حالا هرکدوم `K` پس داده‌ن → ~`10K − 5K = 5K`؛ به‌علاوهٔ سود هجی که TP خورد (`+K`).
- خالص: مثبت — ولی **نه به بزرگیِ ~۱۰U که در نسخهٔ ۱ ادعا شده بود**. عدد واقع‌بینانه‌تر حدود `+2K` منهای هزینه‌هاست. باز هم مثبت، ولی باید انتظار درست داشت.

**چرا این تصادفی نیست:** خودِ فاصلهٔ TP هج طوری طراحی شده که وقتی بهش می‌خوریم، دقیقاً یعنی «یه K کامل از آخرین سود پس گرفته شده» — یه سطح مشخص و از پیش تعریف‌شده، نه یه افت تصادفی Equity. یه اسپایک لحظه‌ای هم فرقی نمی‌کنه — فقط همین سه شرط تصمیم رو می‌گیرن، نه «چرا» قیمت برگشته.

**تنها استثنا — قفل سود مینیمم (اختیاری، پیشنهادی):** اگر `n ≥ InpAP_LockAfter` (پیشنهاد: ۴) و سود شناور کل سبد از `InpLockFraction × MaxFloatingProfitSeen` (پیشنهاد: ۰٫۵) پایین‌تر اومد، همه‌چیز بسته می‌شه. این یه تریلینگ نیست، یه «سقفِ پس‌دادن» است و فقط در لوپ‌های عمیق فعال می‌شه — جایی که ریاضیِ مربعی بیشترین مقدار رو روی میز گذاشته.

---

### ۵.۳ فاز ۲ — تدافعی/بازیابی (کامل بازنویسی‌شده)

نقطهٔ شروعش با فاز ۱ فرق داره: به‌جای این‌که قیمت با روند پیش بره، از همون اول برخلاف روند رفته و هج (پوزیشن مخالف روند) به هدف سودش رسیده. اینجا دیگه فرض «با خیال راحت سوار روندیم» درست نیست.

#### چرا نسخهٔ قبلی فاز ۲ خطرناک بود

نسخهٔ ۱ می‌گفت: یه جفت جدید با `a > c` باز کن، پکیج کن، و تا `InpMaxRecoveryLevels` تکرار کن.

مشکل ریاضی:
```text
D_new = L / ((V + v) · pv)      ← فاصلهٔ لازم تا صفر، با افزودن حجم v
Risk  = (V + v) · MaxAdverse    ← ریسک با افزودن حجم
```
یعنی **فایده هذلولی و اشباع‌شونده است، ولی ریسک خطی و بی‌سقف رشد می‌کنه.** هر سطح جدید، نسبت به سطح قبل کمتر کمک می‌کنه ولی همون‌قدر (یا بیشتر) ریسک اضافه می‌کنه. این تعریف ریاضیِ مارتینگل است — دقیقاً همون چیزی که از Xmoon فرار کردیم.

مشکل دوم: `InpMaxRecoveryLevels` یه سقف بر حسب **تعداد** است، نه بر حسب **پول**. سطح ۵ در بازار آروم یه ضرر کوچیکه و در بازار پرنوسان یه فاجعه. سقفِ تعداد، ریسک واقعی رو محدود نمی‌کنه.

#### فلسفهٔ جدید فاز ۲

به‌جای «بزرگ‌تر کردن پوزیشن تا زودتر به صفر برسیم»، سه اهرم دیگه:

```text
D = L / (V_net · pip_value)        ← فاصله تا سربه‌سر
```
- `L` را کم کن (درآمد از نوسان)
- `V_net` را کنترل‌شده تغییر بده (نه بی‌حساب)
- **هدف صفر را با گذشت زمان شل کن** (هدف را به سمت خودت بیاور)

و مهم‌تر از همه: **اول ریسک را فریز کن.**

##### گام ۱ — قفل کامل (Lock)

به‌محض ورود به فاز ۲، سیستم حجم خالص سبد را صفر می‌کند:
```text
V_net = Σ V_buy − Σ V_sell = 0
```
با باز کردن یک پوزیشن قفل‌کننده به اندازهٔ `|V_net|` در جهت مخالف.

**اثر:** ضرر شناور `L` در همان لحظه **فریز** می‌شود. از این به بعد هر حرکت قیمت — هر اندازه بزرگ — `L` را تغییر نمی‌دهد. سیستم دیگر در معرض ریسک جهتی نیست.

**هزینهٔ قفل:** اسپرد + سوآپ منفی روی دو سمت. این یک هزینهٔ کوچکِ ثابت در ازای حذف کاملِ ریسک دنبالهٔ چاق است. سوآپ باید در `T_max` (پایین‌تر) لحاظ شود.

**سازگارکنندهٔ خودکار حساب‌های Netting (پراپ‌فرم‌ها و بروکرهای آمریکایی — نسخهٔ ۳):**
در حساب **Netting** (حسابی که برای هر نماد فقط یک پوزیشن خالص نگه می‌دارد و پوزیشن مخالف را با پوزیشن موجود تسویه می‌کند)، باز کردن پوزیشن قفلِ معکوس اصلاً ممکن نیست.

```text
در OnInit:
   mode = AccountInfoInteger(ACCOUNT_MARGIN_MODE)
   اگر mode == ACCOUNT_MARGIN_MODE_RETAIL_NETTING  →  IsNetting = true  (یک‌بار، و لاگ شود)
```

رفتار در حالت `IsNetting = true`:
- **قفل فاز ۲** با پوزیشن معکوس اجرا نمی‌شود؛ به‌جای آن سیستم **خروج جزئی** می‌زند تا `V_net = 0` برسد (که در netting یعنی بستن کامل نماد) و `L` را به‌عنوان ضرر محقق‌شده تثبیت می‌کند.
- **گرید نوسان‌گیر** در حالت netting **غیرفعال** می‌ماند، چون خرید و فروش هم‌زمانِ کوچک، صرفاً پوزیشن خالص را جابه‌جا می‌کند و مفهوم «پوزیشن گرید مستقل با TP خودش» وجود ندارد.
- **فاز ۱** بدون تغییر کار می‌کند فقط اگر پای روند و پای هج روی یک نماد نباشند — که در AFA هستند. پس در netting، «هج» به‌صورت **کاهش حجم خالص** (خروج جزئی به اندازهٔ `V_base`) شبیه‌سازی می‌شود و «چرخش هج در AP» به‌صورت افزایش دوبارهٔ حجم خالص. اثر PnL معادل است ولی گزارش‌دهیِ per-leg از دست می‌رود؛ پس در netting باید یک **دفترکل داخلی (internal ledger)** نگه داشته شود که نقش هر جزء حجم (`trend` / `hedge`) را در حافظهٔ EA ثبت کند.
- اگر مدل داخلیِ EA و حجم واقعی حساب از هم فاصله گرفتند (اختلاف بیش از یک گام لات)، سیستم وارد **وضعیت امن** می‌شود: هیچ ورود جدید، فقط بستن.

##### گام ۲ — گرید نوسان‌گیر داخل رنج

حالا که ریسک جهتی صفر است، از نوسان درآمد می‌گیریم تا `L` را قسطی پس بدهیم.

**تعریف رنج:** با ورود به فاز ۲، مرزها ثبت می‌شوند:
```text
R_high = بالاترین High در InpRangeLookback کندل اخیر
R_low  = پایین‌ترین Low  در InpRangeLookback کندل اخیر
A      = R_high − R_low        (دامنهٔ رنج)
```

**شرط فعال‌سازی گرید (اجباری):**
```text
A > InpGridEdgeMult × spread / pip_value      (پیشنهاد: 3)
```
اگر رنج آن‌قدر باریک است که هر رفت‌وبرگشت فقط هزینهٔ اسپرد است، **گرید فعال نمی‌شود** و سیستم مستقیماً روی مسیر «هدف نزولی + T_max» می‌رود. این شرط، دقیقاً همان چیزی است که یک گرید سودده را از یک ماشین سوزاندن کمیسیون جدا می‌کند.

**قواعد گرید:**
- گام گرید: `g = A / InpGridLevels` (پیشنهاد `InpGridLevels = 4`)، با کف `g ≥ InpMinGridStep_ATR × ATR`.
- در هر سطحِ گرید که قیمت به آن می‌رسد: خرید نزدیک کف رنج، فروش نزدیک سقف رنج، هر کدام با حجم `V_grid = InpGridVolFraction × V_base` (پیشنهاد: ۰٫۲۵ — عمداً کوچک).
- هر پوزیشن گرید یک TP سخت به اندازهٔ `g` دارد و **هیچ SL جهتی ندارد**، چون قفل پایه ریسک جهتی را حذف کرده.
- **سقف اشغال هم‌زمان گرید:** حداکثر `InpGridMaxOpen` پوزیشن گرید هم‌زمان (پیشنهاد: ۴). این باعث می‌شود گرید خودش نتواند `V_net` را از صفر دور کند بیش از یک مقدار کوچک کنترل‌شده:
  ```text
  |V_net| ≤ InpGridMaxOpen × V_grid    (باید ≤ 1.0 × V_base بماند)
  ```
- هر سود محقق‌شدهٔ گرید مستقیماً از `L` کم می‌شود:
  ```text
  L_remaining = L_frozen − Σ grid_realized_profit − Σ base_realized
  ```

**افزایش حجم — محدود و مشروط:** حداکثر `InpMaxVolAdds` بار (پیشنهاد: ۲) اجازهٔ افزایش حجم خالص وجود دارد، و فقط اگر هر سه شرط برقرار باشد:
1. `A > 3 × spread / pip_value` (نوسان واقعاً وجود دارد)
2. موتور رژیم هنوز جهت اولیه را تأیید می‌کند
3. بودجهٔ ریسک باقی‌مانده بیش از `50%` است

این جایگزین `InpMaxRecoveryLevels` است — و برخلاف آن، سقفش هم بر تعداد است، هم بر بودجه، هم بر شرایط بازار.

##### گام ۳ — هدف صفرِ نزولی (Decaying Target)

منتظر ماندن برای «دقیقاً صفر» می‌تواند بی‌نهایت طول بکشد. پس هدف را به‌مرور شل می‌کنیم:

```text
Target(t) = −L_frozen · (1 − exp(−t / τ))
```
یا در شکل خطیِ ساده‌تر برای پیاده‌سازی:
```text
Target(t) = −L_frozen · max(0, 1 − t / T_max)
```
که `t` = تعداد کندل‌های سپری‌شده از ورود به فاز ۲، و `T_max` = افق حداکثر.

**تفسیر:** در ابتدا سیستم دنبال بازیابی کامل است (`Target = −L_frozen`، یعنی «صفر شدن»). هرچه زمان می‌گذرد، سیستم به یک بازیابی جزئی هم راضی می‌شود. در `t = T_max` هدف به صفرِ نسبی می‌رسد یعنی «هر چه در دست است بردار و برو».

**شرط خروج موفق:**
```text
اگر  PnL_کل_سبد ≥ Target(t)   →   بستن کامل → فاز صفر
```

**انتخاب `T_max` (نه دلبخواه):** `T_max` بر اساس هزینهٔ نگهداری تعیین می‌شود، نه سلیقه:
```text
T_max = min( InpMaxRecoveryBars ,  |L_frozen| × InpSwapBudgetFraction / daily_swap_cost )
```
یعنی «تا وقتی که هزینهٔ سوآپِ انتظار، کسر معناداری از خودِ ضرر نشده».

**تصحیح سوآپ سه‌برابری (افزودهٔ نسخهٔ ۳):** در فاز ۲ هر دو سمت Buy و Sell هم‌زمان باز هستند، پس سوآپ منفی روی حداقل یک سمت قطعی است. اکثر بروکرها **سوآپ سه‌برابر** را در شب چهارشنبه (برای تسویهٔ آخر هفته) اعمال می‌کنند.

```text
daily_swap_cost = Σ (swap_long + swap_short) روی همهٔ پوزیشن‌های باز چرخه   (عدد واقعی، از SYMBOL_SWAP_LONG / SHORT)
اگر روزِ رول‌اوور سه‌برابری نزدیک است (کمتر از ۲۴ ساعت):
        T_max ← T_max × (1 − InpTripleSwapCut)          (پیشنهاد: 0.15)
```
و مهم‌تر: **سوآپ شناور به‌صورت زنده در `Consumed` جمع می‌شود** (نه فقط در پایان چرخه)، وگرنه بودجهٔ ریسک بی‌صدا نشت می‌کند. روز سه‌برابری باید از تقویم واقعی بروکر خوانده شود (`SYMBOL_SWAP_ROLLOVER3DAYS`)، نه از فرض ثابت «چهارشنبه».

##### گام ۴ — بودجهٔ ریسک (جایگزین سقف تعداد سطح)

```text
RiskBudget = InpRiskBudgetPct × Equity_at_cycle_start
```
پیشنهاد شروع: `2%`.

```text
Consumed = |L_frozen| + هزینهٔ قفل + Σ زیان محقق‌شدهٔ گرید + سوآپ انباشته
```

**شرط توقف سخت:**
```text
اگر Consumed ≥ RiskBudget   →   بستن کامل با پذیرش ضرر → فاز صفر (با کول‌داون ۳×)
```

این سقف، برخلاف سقف تعدادِ سطح، مستقیماً همان چیزی را محدود می‌کند که واقعاً اهمیت دارد: **پول**.

##### گام ۵ — شکست رنج

اگر قیمت `R_high + InpBreakBuffer × ATR` را بالا یا `R_low − InpBreakBuffer × ATR` را پایین بشکند (پیشنهاد بافر: ۰٫۵):
- تمام پوزیشن‌های گرید فوراً بسته می‌شوند (فرض رنج باطل شد).
- قفل پایه دست‌نخورده می‌ماند (`L` هنوز فریز است).
- سیستم `InpPostBreakWait` کندل صبر می‌کند:
  - اگر قیمت به داخل رنج برگشت → رنج بازمحاسبه، گرید از سر گرفته می‌شود.
  - اگر بیرون ماند → بستن کامل با پذیرش ضرر → فاز صفر.

**در حالت شکست رنج، هرگز گرید را در جهت شکست ادامه نده.** این تنها راهی است که یک گرید قفل‌شده می‌تواند به یک ضرر بی‌سقف تبدیل شود.

#### الگوریتم خلاصهٔ فاز ۲

```text
ورود به فاز ۲
 ├─ 1) قفل کامل: V_net → 0 ، L فریز می‌شود
 ├─ 2) ثبت R_high / R_low / A / RiskBudget / T_max
 ├─ 3) اگر A > 3·spread → گرید نوسان‌گیر روشن (حجم کوچک، TP=g، سقف اشغال)
 ├─ 4) هر کندل: L_remaining به‌روزرسانی، Target(t) محاسبه
 ├─ 5) حداکثر ۲ افزایش حجم، فقط با ۳ شرط
 ├─ 6) خروج موفق:  PnL ≥ Target(t)        → بستن کامل → فاز صفر
 ├─ 7) خروج ریسکی: Consumed ≥ RiskBudget  → بستن با ضرر → فاز صفر
 ├─ 8) خروج زمانی: t ≥ T_max              → بستن کامل  → فاز صفر
 └─ 9) شکست رنج → گرید خاموش → صبر → یا از سر گیری یا بستن کامل
```

**نکتهٔ صادقانه:** این طراحی از نسخهٔ سند ۷ به‌مراتب پیچیده‌تر است. پیچیدگی، خودش یک ریسک است (باگ، overfit، سخت‌شدن دیباگ). به همین دلیل **پیشنهاد پیاده‌سازی مرحله‌ای** در بخش ۷ آمده: اول فاز ۲ را فقط با «قفل + هدف نزولی + بودجهٔ ریسک» (بدون گرید) بک‌تست کنیم، بعد گرید را به‌عنوان یک لایهٔ جدا با فلگ روشن/خاموش اضافه کنیم و اثر خالصش را اندازه بگیریم. اگر گرید سود خالص اضافه نکرد، خاموش می‌ماند.

---

### ۵.۴ فاز ۳ — تغییر رژیم وسط معامله (نامتقارن، مبتنی بر ATR)

فاز ۳ یک فاز مستقل نیست — یک **لایهٔ روی فاز ۱ یا ۲** است.

#### چرا تأخیر زمانی غلط بود

«چند ساعت صبر کن» یک عدد دلبخواه است، دقیقاً هم‌جنس با TP/SL درصدی که یک‌بار شکست خورد. سه ساعت در بازار آسیا ≈ هیچ حرکتی؛ سه ساعت در همپوشانی لندن-نیویورک ≈ یک حرکت کامل. معیار باید **حرکت قیمت** باشد، نه ساعت.

#### معیار جدید: تأیید مبتنی بر فاصله

```text
ConfirmDistance = InpFlipConfirm_ATR × ATR
```
فلیپ رژیم تنها زمانی «تأییدشده» است که پس از فلیپ، قیمت به اندازهٔ `ConfirmDistance` در جهت جدید حرکت کند. (پیشنهاد شروع: `0.5`.)

اختیاری و توصیه‌شده: علاوه بر فاصله، یک حداقل تعداد کندل هم به‌عنوان کف بگذاریم (`InpFlipConfirmBars`، پیشنهاد ۱ یا ۲) تا یک تیک اسپایکی به‌تنهایی تأیید نسازد. شرط تأیید = **هم فاصله، هم کندل**.

#### قانون نامتقارن (هستهٔ اصلاح)

تأخیر رایگان نیست. اما هزینهٔ تأخیر در دو حالت **کاملاً متفاوت** است:

| وضعیت سبد | رفتار در فلیپ رژیم | چرا |
|---|---|---|
| **در فاز ۱ (سبد در سود)** | **بستن فوری و کامل** — هیچ تأخیری | سبد سود شناور دارد. تأخیر فقط سود موجود را می‌خورد. اگر فلیپ نویز بوده باشد، بدترین اتفاق این است که یک ورود مجدد با هزینهٔ یک اسپرد داشته باشیم — بسیار ارزان‌تر از پس‌دادن سود انباشتهٔ مربعی. |
| **در فاز ۲ (سبد در ضرر)** | **فوراً نبند** — فقط «فریز افزودن»: هیچ حجم جدید، هیچ سطح جدید، گرید فقط پوزیشن‌های موجود را می‌بندد | بستن فوری یعنی تثبیت ضرر بر اساس یک سیگنالی که ۳۰-۴۰٪ اوقات اشتباه است. اینجا تأخیر واقعاً ارزش دارد. |
| **در فاز صفر (جفت باز، فاز نامعلوم)** | بستن فوری هر دو پا | هنوز هیچ چیزی روی جهت سرمایه‌گذاری نشده. |

**در فاز ۲، بعد از فریز:**
- اگر فلیپ **تأیید شد** (`ConfirmDistance` + `ConfirmBars`) → بستن کامل سبد → فاز صفر (کول‌داون ۲×).
- اگر رژیم قبل از تأیید به جهت اولیه **برگشت** → فریز برداشته می‌شود، فاز ۲ عادی ادامه می‌یابد.
- توجه: در طول فریز، قفلِ گام ۱ برقرار است، پس صبر کردن **ریسک اضافه نمی‌کند** — فقط هزینهٔ سوآپ دارد که در `T_max` حساب شده. این دقیقاً دلیلی است که تأخیر در فاز ۲ منطقی و در فاز ۱ غیرمنطقی است.

**نمودار حالت فاز ۳:**

```text
فلیپ رژیم رخ داد
   ├─ سبد در فاز ۱ (سود) ──────────────────► بستن فوری کامل ──► فاز صفر
   ├─ سبد در فاز صفر (جفت باز) ────────────► بستن فوری کامل ──► فاز صفر
   └─ سبد در فاز ۲ (ضرر) ──► فریز افزودن
                               ├─ تأیید (0.5·ATR + N کندل) ──► بستن کامل ──► فاز صفر
                               └─ رژیم برگشت ──────────────► ادامهٔ فاز ۲
```

---

### ۵.۵ ترتیب اولویت وقتی چند رویداد هم‌زمان می‌افتد

این بخش در نسخهٔ قبل کاملاً جا افتاده بود و منبع مستقیم باگ است: در یک کندل ممکن است هم‌زمان AP فعال شود، هم TP هج بخورد، هم رژیم فلیپ کند. بدون ترتیب صریح، رفتار سیستم به ترتیب تصادفی کد بستگی پیدا می‌کند.

**قانون: در هر کندل، دقیقاً یک تصمیمِ سطح‌بالا اجرا می‌شود. بالاترین اولویتِ فعال برنده است و بقیه در آن کندل نادیده گرفته می‌شوند.**

| اولویت | رویداد | اقدام |
|---|---|---|
| **۱** | نقض ریسک: `Consumed ≥ RiskBudget` یا مارجین بحرانی | بستن کامل فوری → فاز صفر (کول‌داون ۳×) |
| **۲** | `t ≥ T_max` در فاز ۲ | بستن کامل → فاز صفر |
| **۳** | شکست رنج در فاز ۲ | خاموشی گرید + شمارش `InpPostBreakWait` |
| **۴** | فلیپ رژیم | طبق جدول نامتقارن فاز ۳ |
| **۵** | خروج موفق: TP هج (فاز ۱) یا `PnL ≥ Target(t)` (فاز ۲) | بستن کامل → فاز صفر |
| **۶** | سقف لوپ `InpAP_MaxLoopCount` | بستن کامل → فاز صفر |
| **۷** | تریگر AP (فاز ۱) | چرخش هج، افزودن پای روند |
| **۸** | افزودن حجم مشروط (فاز ۲) | فقط با هر سه شرط |
| **۹** | پر/بازکردن سطح گرید (فاز ۲) | اجرا |
| **۱۰** | ورود جدید از فاز صفر | فقط اگر هر سه گیت پاس شده باشند |

**قواعد تکمیلی حل تعارض:**
- **بستن همیشه بر بازکردن مقدم است.** اگر در یک کندل هم شرط بستن و هم شرط بازکردن فعال است، فقط بستن اجرا می‌شود؛ بازکردن به کندل‌های بعد و از مسیر فاز صفر موکول می‌شود.
- **در همان کندلی که چرخه بسته می‌شود، هیچ ورودی جدیدی مجاز نیست** (کول‌داون از کندل بعد شمرده می‌شود).
- اگر هم‌زمان AP و TP هج تریگر شوند (کندلی که هر دو سمت را لمس کرده)، **TP هج برنده است** (اولویت ۵ > ۷) — یعنی محافظه‌کارانه‌ترین تفسیر.
- در داده‌های کندلی که ترتیب لمس High و Low نامعلوم است، همیشه **بدترین ترتیب ممکن** فرض می‌شود. این جلوی خوش‌بینیِ کاذبِ بک‌تست را می‌گیرد.

---

### ۵.۵.۱ لایهٔ محافظ سراسری حساب (Global Safety Layer — افزودهٔ نسخهٔ ۳)

**چرا لازم است:** بودجهٔ ریسک (`InpRiskBudgetPct`) فقط در سطح **یک چرخه روی یک نماد** سقف می‌گذارد. اگر پنج چرخهٔ بدشانس هم‌زمان روی پنج نماد اجرا شوند، هیچ قانونِ تک‌چرخه‌ای نقض نشده ولی درادون کل حساب می‌تواند ۱۰٪+ شود. این لایه **بالاتر از منطق فاز** می‌نشیند و مستقل از آن عمل می‌کند.

**سه سقف مستقل، هر سه هم‌زمان فعال:**

۱) **سقف ریسک هم‌زمان حساب:**
```text
Σ (RiskBudget همهٔ چرخه‌های فعال روی همهٔ نمادها)  ≤  InpMaxAccountRiskPct × Equity        (پیشنهاد: 5.0%)
```
اگر باز کردن یک چرخهٔ جدید این سقف را بشکند، آن چرخه **باز نمی‌شود** (نه با حجم کمتر — اصلاً باز نمی‌شود، تا منطق ساده بماند).

۲) **سقف همبستگی (Correlated Exposure Cap):**
جمع سادهٔ ریسک کافی نیست: EURUSD و GBPUSD در یک حرکت دلاری **هم‌زمان** ضرر می‌دهند، پس ریسکشان جمع‌پذیر نیست بلکه تقریباً تکراری است.
- **راه ساده و توصیه‌شده برای شروع:** گروه‌بندی ثابت ارزی، و **حداکثر یک چرخهٔ فعال از هر گروه**:
  ```text
  گروه USD:  EURUSD, GBPUSD, AUDUSD, NZDUSD, USDCHF, USDCAD, USDJPY
  گروه EUR:  EURGBP, EURJPY, EURCHF, EURAUD
  گروه JPY:  USDJPY, EURJPY, GBPJPY, AUDJPY
  قانون: حداکثر InpMaxPerGroup چرخهٔ هم‌زمان از هر گروه       (پیشنهاد: 1)
  ```
- **راه دقیق‌تر (فاز بعدی):** ماتریس همبستگی متحرک روی `InpCorrLookback` کندل اخیر (پیشنهاد: ۲۰۰). ریسک مؤثر:
  ```text
  Risk_effective = sqrt( Σᵢ Σⱼ  ρᵢⱼ · Rᵢ · Rⱼ )        ≤  InpMaxAccountRiskPct × Equity
  ```
  که `ρᵢⱼ` همبستگی بازده دو نماد و `Rᵢ` بودجهٔ ریسک تخصیص‌یافته به نماد `i` است. اگر `ρ = 1` برای همه، این فرمول به جمع ساده برمی‌گردد؛ اگر مستقل باشند، خیلی کمتر می‌شود.
  **قانون عملی:** اگر `|ρᵢⱼ| > InpCorrThreshold` (پیشنهاد: ۰٫۷)، آن دو نماد یک نماد شمرده می‌شوند.

۳) **توقف کامل بر اساس درادون (Daily / Weekly Kill-Switch):**
```text
DD_daily  = (Equity_شروع_روز  − Equity_فعلی) / Equity_شروع_روز
DD_weekly = (Equity_شروع_هفته − Equity_فعلی) / Equity_شروع_هفته

اگر DD_daily  ≥ InpMaxDailyDD   (پیشنهاد: 3%)   →  حالت امن سراسری تا شروع روز بعد
اگر DD_weekly ≥ InpMaxWeeklyDD  (پیشنهاد: 6%)   →  حالت امن سراسری تا شروع هفتهٔ بعد
```

**تعریف دقیق «حالت امن سراسری»:**
- همهٔ نمادها **فوراً** همهٔ پوزیشن‌های مدیریت‌شده را می‌بندند و به فاز صفر می‌روند (این معادل اولویت ۱ در جدول اولویت است و بر همه‌چیز مقدم است).
- هیچ ورود جدیدی روی **هیچ نمادی** تا پایان دورهٔ توقف مجاز نیست.
- یک لاگ صریح با دلیل، مقدار درادون و لیست پوزیشن‌های بسته‌شده ثبت می‌شود.
- خروج از حالت امن **خودکار و زمانی** است (شروع روز/هفتهٔ بعد)، نه با برگشت اکوییتی — چون برگشت اکوییتی می‌تواند خودش نتیجهٔ یک پوزیشن باز خطرناک باشد.

**نکتهٔ پیاده‌سازی چندنمادی:** این سقف‌ها باید در یک **حافظهٔ مشترک بین نمونه‌های EA** نگه‌داری شوند (Global Variables ترمینال با پیشوند یکتا، مثل `AFA_ACC_RISK_USED`)، چون هر نماد یک نمونهٔ جدا از EA است و از پوزیشن‌های بقیه بی‌خبر است. نوشتن/خواندن باید با یک قفل ساده (نوبت‌دهی بر اساس timestamp) انجام شود تا دو نمونه هم‌زمان بودجه را دوبار خرج نکنند.

---

### ۵.۶ قانون مشترک همه‌جا

- هر تصمیمی (باز کردن، بستن) فقط بعد از **بسته‌شدن کندل** گرفته می‌شود و در ابتدای کندل بعدی اجرا می‌شود. این فقط برای کاهش باگ اجراست، نه یک قانون مالی — ولی همهٔ فازها باید رعایتش کنند.
- **استثنای منطقی:** TP‌های سخت (TP هج، TP گرید) روی سرور بروکر می‌نشینند و درون‌کندلی اجرا می‌شوند. این خللی در قانون بالا ایجاد نمی‌کند، چون **واکنش** سیستم به آن TP (یعنی چرخش هج یا بستن سبد) باز هم در کندل بعد ارزیابی می‌شود.
- متغیر وضعیت (`phase`) باید پایدار ذخیره شود (Global Variable یا فایل) و در هر `OnInit` با پوزیشن‌های واقعی حساب **آشتی داده شود** (reconciliation). اگر ناسازگاری دیده شد: سیستم وارد یک وضعیت امن می‌شود (هیچ ورود جدید، فقط مدیریت پوزیشن‌های موجود) و لاگ می‌کند.
- هر پوزیشن با یک `magic number` و یک کامنت شامل `cycle_id` و `role` (`trend` / `hedge` / `lock` / `grid`) تگ می‌شود تا بازسازی وضعیت بعد از ری‌استارت قطعی باشد.

---

### ۵.۷ جدول کامل پارامترها

| پارامتر | نقش | مقدار پیشنهادی شروع |
|---|---|---|
| `InpCooldownBars` | کول‌داون پایه بعد از هر چرخه | 5 |
| `InpMinER` | حداقل Efficiency Ratio | 0.30 |
| `InpER_Lookback` | پنجرهٔ ER | 20 |
| `InpRegimeLookback` | پنجرهٔ سنجش پایداری رأی رژیم | 50 |
| `InpMaxFlipRate` | حداکثر نرخ فلیپ مجاز | 0.12 |
| `InpMinK_ATR` | حداقل K بر حسب ATR | 0.5 |
| `InpMinK_Spread` | حداقل K بر حسب اسپرد | 6 |
| `InpEntryConfirmBars` | کندل تأیید ورود | 2 |
| `V_base` | حجم پایهٔ هر پا | بر اساس ریسک حساب |
| `K` | گام هج / گام AP (بر حسب ATR) | 1.0 × ATR |
| `InpAP_MaxLoopCount` | سقف تعداد AP در فاز ۱ | 8 |
| `InpAP_LockAfter` | از چندمین AP قفل سود فعال شود | 4 |
| `InpLockFraction` | حداکثر کسر قابل پس‌دادن از اوج سود | 0.5 |
| `InpRangeLookback` | پنجرهٔ تعریف رنج فاز ۲ | 50 |
| `InpGridLevels` | تعداد سطح گرید | 4 |
| `InpGridVolFraction` | حجم هر پوزیشن گرید نسبت به پایه | 0.25 |
| `InpGridMaxOpen` | حداکثر پوزیشن گرید هم‌زمان | 4 |
| `InpGridEdgeMult` | حداقل نسبت دامنهٔ رنج به اسپرد | 3 |
| `InpMinGridStep_ATR` | کف گام گرید | 0.25 × ATR |
| `InpMaxVolAdds` | حداکثر افزایش حجم در فاز ۲ | 2 |
| `InpRiskBudgetPct` | بودجهٔ ریسک هر چرخه | 2% |
| `InpMaxRecoveryBars` | سقف مطلق افق فاز ۲ | 500 |
| `InpSwapBudgetFraction` | سهم مجاز سوآپ از L | 0.15 |
| `InpBreakBuffer` | بافر شکست رنج | 0.5 × ATR |
| `InpPostBreakWait` | صبر بعد از شکست رنج | 10 |
| `InpFlipConfirm_ATR` | فاصلهٔ تأیید فلیپ رژیم | 0.5 |
| `InpFlipConfirmBars` | کف کندلی تأیید فلیپ | 2 |
| `InpMaxSpread` | حداکثر اسپرد مجاز برای بازکردن | 2.5 × میانگین اسپرد نماد |
| **افزوده‌های نسخهٔ ۳** | | |
| `InpMinConsensus` | حداقل قدرت اجماع ۶ مدل (گیت ۲) | 0.67 (۴ از ۶) |
| `InpConsFloor` | کف ضریب حجم در اجماع حداقلی | 0.5 |
| `InpER_SlopeBars` | پنجرهٔ شیب ER برای شرط شتاب | 3 |
| `InpATRFast` | ATR کوتاه‌مدت | 5 |
| `InpATRSlow` | ATR بلندمدت | 20 |
| `InpATRMaxStepPct` | حداکثر تغییر ATR مؤثر در هر کندل | 0.20 |
| `InpKLambda` | ضریب λ در گام تطبیقی K | 0.5 |
| `InpTripleSwapCut` | کاهش T_max نزدیک سوآپ سه‌برابری | 0.15 |
| `InpMaxAccountRiskPct` | سقف ریسک هم‌زمان کل حساب | 5.0% |
| `InpMaxPerGroup` | حداکثر چرخهٔ فعال از هر گروه ارزی | 1 |
| `InpCorrLookback` | پنجرهٔ ماتریس همبستگی | 200 |
| `InpCorrThreshold` | آستانهٔ «هم‌ارز شمردن» دو نماد | 0.70 |
| `InpMaxDailyDD` | سقف درادون روزانه (کیل‌سوئیچ) | 3% |
| `InpMaxWeeklyDD` | سقف درادون هفتگی (کیل‌سوئیچ) | 6% |

---

### ۵.۸ موارد لبه‌ای که باید صریحاً در کد پوشش داده شوند

هیچ‌کدام از این‌ها «بعداً درستش می‌کنیم» نیست — هرکدام یک‌بار در Xmoon یا V7 باعث زیان واقعی شده‌اند:

1. **گپ آخر هفته:** اگر با گپ، قیمت از چند سطح AP یا گرید رد شود — هیچ‌وقت چند سطح را عطف‌به‌ماسبق اجرا نکن. فقط یک سطح، در قیمت واقعی بازار.
2. **گپی که از TP هج رد شود:** پر شدن در قیمت بدتر، پذیرفته می‌شود و PnL واقعی ثبت می‌شود؛ سیستم بلافاصله وارد مسیر «بستن کامل → فاز صفر» می‌شود.
3. **حساب netting در برابر hedging:** در حالت netting، قفل فاز ۲ ممکن نیست — مسیر جایگزین (گام ۱) اجرا شود.
4. **حداقل/حداکثر لات و گام لات بروکر:** `V_grid` می‌تواند زیر حداقل لات بیفتد. اگر افتاد، گرید غیرفعال می‌شود (نه گرد شدن به بالا — گرد کردن به بالا سقف حجم را می‌شکند).
5. **StopLevel / FreezeLevel:** اگر TP محاسبه‌شده نزدیک‌تر از حد مجاز بروکر باشد، به حداقل مجاز منتقل شود و لاگ گردد.
6. **مارجین ناکافی:** قبل از هر بازکردن، `OrderCalcMargin` چک شود. اگر ناکافی بود، عمل انجام نمی‌شود و **این یک خطای سکوت نیست** — لاگ می‌شود و اگر تکرار شد، سیستم وارد وضعیت امن می‌شود.
7. **ATR صفر یا نامعتبر** (شروع تاریخچه، بازار بسته): هیچ تصمیمی گرفته نمی‌شود.
8. **گسترش شدید اسپرد** (اخبار، پایان روز): اگر `spread > InpMaxSpread`، هیچ عمل بازکردنی انجام نمی‌شود؛ عملیات بستنِ اضطراری (اولویت ۱) همچنان اجرا می‌شود.
9. **قطع ارتباط وسط چرخه:** بازسازی وضعیت از روی تگ‌ها در `OnInit`؛ اگر پوزیشن یتیم پیدا شد، به چرخهٔ فعلی الحاق یا بسته شود — هرگز نادیده گرفته نشود.
10. **سوآپ منفی انباشته در فاز ۲:** باید در `Consumed` لحاظ شود، وگرنه بودجهٔ ریسک بی‌سر و صدا نشت می‌کند.
11. **چند نماد هم‌زمان:** بودجهٔ ریسک باید هم در سطح نماد و هم در سطح حساب سقف داشته باشد، وگرنه ۵ نماد × ۲٪ = ۱۰٪ ریسک هم‌زمان.
12. **تغییر ساعت تابستانی و ناسازگاری تایم‌فریم بروکر:** همهٔ محاسبات کندلی باید بر پایهٔ **اندیس کندل** باشد، نه زمان ساعتی. هیچ‌جا از «۳ ساعت» یا «ساعت ۲۲:۰۰» به‌عنوان شرط منطقی استفاده نشود؛ فقط `iTime`/شمارندهٔ کندل.
13. **همبستگی بین نمادها:** جمع سادهٔ بودجهٔ ریسک کافی نیست (بخش ۵.۵.۱) — سقف گروهی یا ریسک مؤثر همبسته اعمال شود.
14. **سوآپ سه‌برابری:** روز رول‌اوور از `SYMBOL_SWAP_ROLLOVER3DAYS` خوانده شود، نه فرض «چهارشنبه»؛ و `T_max` قبل از آن کاهش یابد.
15. **درادون تجمعی حساب:** کیل‌سوئیچ روزانه/هفتگی مستقل از منطق فاز، همهٔ نمادها را هم‌زمان به حالت امن ببرد.
16. **رقابت بین نمونه‌های EA روی چند نماد:** بودجهٔ مشترک حساب باید با Global Variable و نوبت‌دهی نوشته شود؛ دو نمونه نباید هم‌زمان یک بودجه را خرج کنند (race condition).

---

## ۶. نظر من: آیا این نسبت به AFA اولیه بهتره؟

**بله، به‌طور معناداری بهتره — ولی هنوز «موفق» نیست، چون هنوز تست واقعی نشده.**

چرا بهتره:
- موتور تشخیص روند الان واقعاً و مستقل اعتبارسنجی شده (۸ جفت‌ارز، ۴.۶ سال) — چیزی که TrendScore اولیه هیچ‌وقت نداشت.
- منطق هج الان با فرمول ریاضی دقیق بررسی می‌شه (نه فقط حدس)، و باگ‌های واقعی (مثل a=c که خودشون رو خنثی می‌کنن) قبل از کدنویسی پیدا و رفع می‌شن.
- الان داده‌های واقعی داریم (تست NoLogic) که نشون می‌ده واکنش به تغییر رژیم چقدر حساسه — این چیزی بود که قبلاً فقط حدس می‌زدیم.
- **(نسخهٔ ۲)** ریسک دیگه با «تعداد» محدود نمی‌شه، با **پول** محدود می‌شه. این تنها سقفیه که واقعاً معنی داره.
- **(نسخهٔ ۲)** هیچ پارامتر زمانیِ دلبخواهی در تصمیم‌های اصلی نمونده — همه‌چیز به ATR، اسپرد یا بودجهٔ حساب نرمال شده.
- **(نسخهٔ ۲)** رفتار در برخورد هم‌زمان رویدادها صریح تعریف شده، پس نتیجهٔ بک‌تست قابل بازتولیده.

چرا هنوز نمی‌شه گفت «حل شد»:
- منطق ماشین‌حالت هنوز هیچ‌وقت روی داده‌های واقعی بک‌تست نشده — همه‌ش فعلاً روی کاغذ و فرمول.
- موتور تشخیص روند با همهٔ اعتبارسنجی‌هاش، بازم فقط ۶۰-۷۰٪ دقیقه — یعنی هنوز ۳۰-۴۰٪ وقت‌ها جهت اشتباه می‌گه، و هیچ فرمول هجی این رو کامل جبران نمی‌کنه، فقط ضررش رو کوچیک‌تر می‌کنه.
- **(نسخهٔ ۲)** فاز ۲ الان به‌مراتب پیچیده‌تر از سند ۷ است. پیچیدگی خودش ریسک است. تنها دفاع ما در برابرش، پیاده‌سازی مرحله‌ای با فلگ و اندازه‌گیری اثر خالص هر لایه است.
- فاز ۱ در AP اول و دوم سربه‌سر یا کمی منفیه؛ کل سودآوری سیستم به این وابسته‌ست که چند وقت یک‌بار به `n ≥ 3` می‌رسیم. این عدد هنوز اندازه‌گیری نشده و **مهم‌ترین عدد ناشناختهٔ پروژه است.**

جمع‌بندی: **مسیر قبلی (AFA V7 با گیت‌های سخت‌گیر) بن‌بست بود — درست تشخیص دادیم که باید عوضش کنیم.** مسیر جدید پایه‌های به‌مراتب محکم‌تری داره، ولی هنوز باید ثابت کنه که واقعاً روی داده‌های واقعی هم جواب می‌ده.

---

## ۷. قدم بعدی مشخص (به‌روزشده)

مرحله‌ای، و هر مرحله با یک سؤال قابل اندازه‌گیری:

0. **شبیه‌سازی آفلاین ماشین‌حالت در پایتون + تست واحد (افزودهٔ نسخهٔ ۳ — قبل از هر خط MQL5).** با این حجم از شرط و اولویت، نوشتن مستقیم در MQL5 ریسک باگ بالایی دارد که بک‌تست معمولی هم لزوماً نشانش نمی‌دهد (مثلاً race condition بین تیک‌ها یا اولویت اشتباه در یک کندلِ پرحادثه). منطق فاز باید اول روی داده M1/تیک در پایتون شبیه‌سازی شود و برای **هر انتقال حالت** یک تست واحد نوشته شود. جزئیات کامل در بخش ۸، گام ۴.
1. **اندازه‌گیری توزیع `n`.** قبل از هر کدنویسی کامل، فقط با یک اسکریپت اندازه‌گیری: در تاریخچه، بعد از یک سیگنال معتبر رژیم، توزیع «چند بار قیمت `K` پشت `K` در جهت روند پیش می‌ره» چیست؟ اگر `P(n ≥ 3)` خیلی پایین باشد، کل فرض فاز ۱ زیر سؤال می‌رود و باید `K` را تنظیم کنیم. **این ارزان‌ترین و تعیین‌کننده‌ترین تست است — اول این.**
2. **پیاده‌سازی فاز صفر + فاز ۱ تنها.** فاز ۲ فعلاً = «ببند و برو فاز صفر». این یک خط پایهٔ (baseline) قابل اندازه‌گیری می‌دهد.
3. **افزودن فاز ۳ نامتقارن.** با فلگ روشن/خاموش. اندازه‌گیری اثر خالص روی همان دورهٔ قبل.
4. **افزودن فاز ۲ سادهٔ (قفل + هدف نزولی + بودجهٔ ریسک، بدون گرید).** مقایسه با خط پایه.
5. **افزودن لایهٔ گرید با فلگ جدا.** فقط اگر سود خالص اضافه کرد، روشن می‌ماند.
6. **تست ترکیبی مرز رنج/روند (افزودهٔ نسخهٔ ۳).** فیلتر رنج (ER + هیسترزیس ۱٫۲۵×) و فلیپ رژیم (فاصلهٔ ATR + کف کندلی) دو مکانیزم مستقل‌اند و در نزدیکی مرز می‌توانند هم‌زمان و متناقض فعال شوند. اندازه بگیر: چند درصد از ورودها در کندل‌های «نزدیک مرز خروج از رنج» (یعنی `InpMinER ≤ ER ≤ InpMinER × 1.25`) رخ می‌دهند، و **نرخ‌برد این زیرگروه را جدا** گزارش کن. اگر نرخ‌بردشان به‌طور معنادار پایین‌تر از بقیه بود، `InpMinER` را بالا ببر یا یک بافر ورود اضافه کن.
7. **بک‌تست کامل روی همان ۸ جفت‌ارز و ۴.۶ سال** — نه فقط دورهٔ ۷ ماههٔ قبلی، تا دوباره overfit نکنیم. جداسازی صریح دورهٔ توسعه از دورهٔ تأیید (out-of-sample).

**قاعدهٔ روش:** هر مرحله فقط **یک** تغییر. اگر یک مرحله نتیجه را بهتر نکرد، برمی‌گردد — نه این‌که با تنظیم پارامتر نجاتش بدهیم. این دقیقاً درسی است که V7 به ما داد.


---

## ۸. [حذف‌شده از این سند]

> راهنمای گام‌به‌گامِ «چطور بسازیم» (که این‌جا بود) عمداً از این فایل حذف شد — این سند فقط برای تجمیع **یافته‌های R&D** است، نه راهنمای اجرا. اگر لازم شد، نسخهٔ کامل آن (گام ۰ تا ۱۰، جدول زمانی، واژه‌نامه) در تاریخچهٔ گفتگو با AI4 موجود است. تنها چیزی که از آن بخش واقعاً یک «یافته» بود — نه یک گام اجرا — به بخش ب (ب.۸) منتقل شد.

---

---

# بخش ب — مسیر تحقیق بعد از نسخهٔ ۴: چه چیزی شکست خورد، چرا، و چرا سراغ چه چیزی رفتیم

> این بخش «ادامهٔ زمانی» سند بالاست. هیچ‌چیزی از بخش الف پاک نشده؛ هرجا عددی یا قانونی عوض شده، همین‌جا صریح گفته می‌شود که کدام بند از بخش الف باطل است و جایگزینش چیست.

## ب.۰ نقطهٔ شروع: ایراد AI3

نسخهٔ ۴ یک معماری کامل بود که هنوز **یک عدد** پشتش نبود. AI3 دقیقاً همین را گرفت و گفت:

> «قبل از رفتن به v5 و ساختن معماری بازیابی، باید اقتصاد فاز ۱ را با عدد بسنجی. حدس من این است که `P(n≥3)` حدود ۱۲٪ است؛ اگر این‌طور باشد، هج چرخشی خودش موتور ضرر است و کل بحث فاز ۲ بی‌موضوع می‌شود.»

جوابِ حرف او: **درست می‌گفت، فقط توصیه‌اش («فعلاً صبر کنیم») غلط بود** — چون داده (۶ نماد × ۶٫۶ سال H1) از قبل موجود بود و همان لحظه قابل اندازه‌گیری بود. اندازه گرفتیم. بقیهٔ این بخش، نتیجهٔ آن اندازه‌گیری است.

## ب.۱ آزمایش ۱ — توزیع `n` (مهم‌ترین عدد پروژه)

روی هر ۶ نماد، با گیت‌های فاز صفرِ بخش ۵.۱، هر چرخه تا انتها دنبال شد و شمرده شد که به چند AP رسید:

| `n` (تعداد AP) | سهم چرخه‌ها |
|---|---|
| ۰ | **۵۲٪** |
| ۱ | ۲۳٪ |
| ۲ | ۱۴٪ |
| **≥ ۳** | **۱۱٪** |

حدس AI3 (۱۲٪) با اندازه‌گیری (۱۱٪) تقریباً یکی درآمد.

## ب.۲ شکست ۱ — هج چرخشی: بیمه نبود، موتور ضرر بود

ریاضی بند ۵.۲ بخش الف را تا آخر باز کردیم. با «بستن هج قبلی و باز کردن هج جدید در هر AP»، سود خالص چرخه‌ای که در سطح `n` بسته شود برابر است با:

```
PnL(n) = K · ( n²/2 − 3n/2 )
```

یعنی: `n=1 → −1K` ، `n=2 → −1K` ، `n=3 → 0` (سربه‌سر) ، و تازه از `n=4` به بعد مثبت.

حالا این را کنار جدول ب.۱ بگذارید: **۸۹٪ چرخه‌ها زیر نقطهٔ سربه‌سرِ خودِ مکانیزم تمام می‌شوند.** نتیجهٔ عددی روی ۶ نماد: میانگین **−۰٫۳۶R در هر چرخه**، و منحنی اکوییتی که در ۶ سال به صفر می‌رسد — مستقل از این‌که موتور رژیم چقدر خوب باشد.

**علت ریشه‌ای (این جمله را حفظ کن):** هج در هر AP، هزینهٔ اجرا (اسپرد کامل + کمیسیون + اسلیپیج، حدود ۱٫۳ تا ۱٫۵ پیپ در هر پا) را در فرکانس بالا تکرار می‌کند، در حالی که فایده‌اش فقط در ۱۱٪ دنبالهٔ توزیع ظاهر می‌شود. هر مکانیزمی با این شکل — هزینهٔ همیشگی، فایدهٔ دُمی — قبل از این‌که به دُم برسد می‌میرد.

**نتیجه: هج چرخشی از این‌جا به بعد حذف است.** بند ۵.۲ بخش الف در قسمت هج باطل است.

## ب.۳ شکست ۲ — کل ماشین بازیابی فاز ۲

فاز ۲ نسخهٔ ۴ (گرید، قفل `V_net`، سقف پله، آزادسازی تدریجی) حدود ۶۰٪ پیچیدگی سند و کد بود. اما ب.۱ می‌گوید **۵۲٪ چرخه‌ها اصلاً به AP اول نمی‌رسند** — یعنی مکانیزم نجات در نیمی از چرخه‌ها فعال می‌شود. هر راه‌حلی که گران‌تر از «یک استاپ کوچک» باشد، در ۵۲٪ مواقع پرداخت می‌شود.

تست‌شده و رد شده:
- گرید بازیابی با قفل `V_net` → افت میانگین `R` و انفجار درادون در بازارهای رنجی.
- DCA / افزایش حجم در ضرر → همان الگو، بدتر.
- «مبدل موج» فاز ۳ (نگه‌داشتن بخشی از پوزیشن بعد از فلیپ) → در هر تنظیمی بدتر از بستن کامل.

**جایگزین: فاز ۲ = خروج. تمام.** این تنها جایی است که ساده‌سازی، هم سود را بالا برد و هم کد را کوچک کرد.

## ب.۴ شکست ۳ — پارامترهای بزرگ نسخهٔ ۴ (`K=1.0×ATR`، سقف لوپ ۸، TP ثابت)

جاروی پارامتری نشان داد جهت انتخاب‌های نسخهٔ ۴ **برعکسِ درست** بود:

| ضریب `K` | ۰٫۴ | ۰٫۷ | ۱٫۰ (نسخهٔ ۴) |
|---|---|---|---|
| میانگین چرخه، بازهٔ کالیبراسیون | +۰٫۳۳R | +۰٫۰۷R | +۰٫۰۴R |
| میانگین چرخه، بازهٔ OOS | **+۰٫۲۳R** | +۰٫۰۱R | −۰٫۰۷R |

| فاصلهٔ تریل | ۱٫۰×K (معادل قانون v4) | ۰٫۵×K | **۰٫۲×ATR** |
|---|---|---|---|
| میانگین هر چرخه | −۰٫۰۲R | +۰٫۱۱R | **+۰٫۲۵R** |

و مهم‌تر از همه: **بستنِ برنده در سقف لوپ ۸، دقیقاً همان کاری است که تنها منبع سود سیستم را می‌کشد.**

## ب.۵ چیزی که پیدا شد: تمام سود در «تحدب پیرامید» است

اجرای همان موتور، یک‌بار با پیرامید و یک‌بار بدون آن:

| ساختار | میانگین هر چرخه |
|---|---|
| بدون پیرامید (تک‌پا، استاپ/تریل) | ۰٫۰۰R |
| پیرامید + تریل تنگ، بدون هج | **+۰٫۲۵R** |

یعنی موتور رژیم به‌تنهایی «صفر» است — دقیقاً همان چیزی که بخش ۴ سند بالا هم گفته بود. سودِ سیستم از **شکل توزیع بازده** می‌آید، نه از دقت جهت: بازنده‌ها را در `1R` می‌کشیم و روی برنده‌ها پا اضافه می‌کنیم تا دُم را باز بگذاریم (بزرگ‌ترین چرخهٔ ثبت‌شده: **+۹۹R**).

## ب.۶ کشف دوم: انتخاب نماد از کل معماری مهم‌تر است

با همان تنظیمات:

| نماد | میانگین چرخه |
|---|---|
| USDJPY | **+۰٫۷۸R** |
| XAUUSD | مثبت و بزرگ |
| AUDCAD / EURCHF / EURGBP / GBPNZD | منفی (مثلاً EURCHF: **−۰٫۱۷R**) |

اما «فقط ین و طلا را معامله کن» یعنی cherry-picking روی گذشته. راه‌حل بدون نگاه به آینده، **لایهٔ خودانتخابی نماد** است (بخش ج.۵).

> ### ⚠️ تناقض حل‌نشده — قبل از هر تصمیمی دربارهٔ لیست نمادها این را بخوان
> AI4 (Claude، به‌صورت مستقل، با معماری پیرامید مشابه ولی نه لزوماً یکسان) دقیقاً **نتیجهٔ برعکس برای GBPNZD** گرفت: در جاروی حساسیت هزینهٔ اجرا روی ۶ نماد (۰ تا ۲٫۵ پیپ هزینهٔ اضافه)، **GBPNZD و XAUUSD تنها دو نمادی بودند که حتی زیر بدترین سناریوی هزینه (۲٫۵ پیپ) هم مثبت ماندند** — GBPNZD حدود +۵۵۱R جمعی روی ۶٫۶ سال. در همان تست، EURGBP و EURCHF (که این جدول هم به‌عنوان منفی نشونشون می‌ده) زیر هزینهٔ واقع‌گرایانه (۱٫۵ پیپ) منفی شدند — این بخش با این جدول هم‌خوانی داره. **پس فقط GBPNZD مغایرت مستقیم داره.**
> این می‌تونه از چند جا اومده باشه: تفاوت دقیق فرمول K/تریل بین دو تست، تفاوت در نحوهٔ محاسبهٔ هزینه (AI4 هزینهٔ اضافه را جدا از اسپرد تاریخی مدل کرد)، یا یه باگ در یکی از دو پیاده‌سازی. **قبل از این‌که GBPNZD رسماً از لیست نمادها کنار گذاشته بشه، این تناقض باید حل بشه** — پیشنهاد: هر دو تست (این و تست AI4) روی دقیقاً همون پنجرهٔ زمانی و همون فرمول هزینه دوباره اجرا بشن و نتیجه مو‌به‌مو مقایسه بشه.

## ب.۷ خلاصهٔ یک‌خطیِ مسیر

> از «معماری نجات» به «معماری تحدب» رفتیم. دو سال از این پروژه صرف مکانیزم‌های نجات (هج، گرید، مبدل موج) شد؛ اندازه‌گیری می‌گوید سود جای دیگری بود: **دُم برنده‌ها را باز بگذار، بازنده‌ها را سریع بکش، و روی نمادی که خودش ثابت کرده معامله کن.**

## ب.۸ سه اشتباه روشی که این پروژه قبلاً کرده و نباید تکرار شوند

(منتقل‌شده از راهنمای اجرای حذف‌شدهٔ بخش ۸ — چون خودش یک یافتهٔ R&D است، نه یک گام اجرا)

1. **تنظیم پارامتر به‌جای فهمیدن علت.** در V7 ماه‌ها پارامتر عوض شد بدون اثر، چون گلوگاه واقعی (فیلتر ۱۱٪) جای دیگری بود. **درمان:** هر بار قبل از تغییر پارامتر، اول اندازه بگیر که آن پارامتر اصلاً چند درصد مواقع فعال است.
2. **اضافه‌کردن لایه روی لایه بدون اندازه‌گیری اثر خالص هرکدام.** **درمان:** هر لایه یک فلگ روشن/خاموش دارد و اثرش جدا اندازه‌گیری می‌شود.
3. **اعداد دلبخواه زمانی یا درصدی (مثل «۳ ساعت» یا «TP درصدی»).** **درمان:** هر عدد در سیستم باید به یکی از این‌ها نرمال شده باشد: ATR، اسپرد، بودجهٔ حساب، یا تعداد کندل. اگر عددی به هیچ‌کدام وصل نیست، مشکوک است.

---
---

# بخش ج — سیستمِ فعلی (نسخهٔ ۵): آن‌چه جایگزین بخش‌های باطل‌شدهٔ بخش الف است

> **از بخش الف بدون تغییر معتبر می‌ماند:** بخش‌های ۱ تا ۴، ۵.۰، ۵.۱ (گیت‌های فاز صفر)، ۵.۵ (ترتیب اولویت و قانون بدترین ترتیب درون‌کندلی)، ۵.۵.۱ (لایهٔ محافظ سراسری)، ۵.۶، ۵.۸ (موارد لبه‌ای)، و کل بخش ۸ (راهنمای صفر تا صد) با اصلاح ترتیب گام‌ها در بخش د.
> **باطل و جایگزین‌شده:** ۵.۲ (فاز ۱)، ۵.۳ (فاز ۲)، ۵.۴ (فاز ۳)، ۵.۷ (جدول پارامترها).

## ج.۱ فاز ۱ — پیرامید تحدبی با تریل تنگ (جایگزین ۵.۲)

**فلسفهٔ نسخهٔ ۴:** «با روند رفت → AP بزن → هم‌زمان هج مخالف باز کن.»
**فلسفهٔ فعلی:** «با روند رفت → AP بزن. هیچ هجی نیست. برگشت = خروج فوری کل چرخه با تریل تنگ.»

1. **ورود پایه:** بعد از عبور از گیت‌های ۵.۱، یک پا در جهت رژیم.
2. **گام پیرامید:** `K = 0.4 × ATR_blend` ، `ATR_blend = میانگین(ATR(14), ATR(50))`.
3. **کف اجرایی K:** `K ≥ 3 × اسپرد جاری`. اگر کف فعال شد و `K > 0.8×ATR_blend` → آن نماد در آن ساعت اصلاً معامله نمی‌شود.
4. **افزودن (AP):** هر بار قیمت `K` **از آخرین پا** (نه از ورود اول) به نفع ما پیش رفت، یک پا با **همان حجم پایه**. مارتینگل ممنوع.
5. **سقف افزودن:** `maxAdds = 18` — **سقف ایمنی است، نه شرط بستن.** قانون «بستن در سقف لوپ ۸» حذف شد.
6. **استاپ اولیه:** `S₀ = 1.5 × K` با کف `max(4×اسپرد, 0.25×ATR_blend)`. این همان **واحد `1R`** و مبنای حجم‌دهی است.
7. **تریل مشترک بعد از اولین AP:** `trail = max(0.2 × ATR_blend, 3 × اسپرد)` از بیشینه/کمینهٔ ثبت‌شدهٔ چرخه. فقط به نفع ما حرکت می‌کند (ratchet)، هرگز شل نمی‌شود.
8. **خروج:** فقط تریل/استاپ، یا فاز ۳، یا لایهٔ محافظ سراسری. **هیچ TP ثابتی وجود ندارد.**
9. **قانون درون‌کندلی:** اگر در یک کندل هم سطح AP و هم استاپ لمس شد، فرض می‌کنیم **اول استاپ خورده** (بدون تغییر از ۵.۵).

## ج.۲ فاز ۲ — خروج (جایگزین ۵.۳)

> اگر قیمت قبل از اولین AP به `S₀` برسد، چرخه با `−1R` بسته می‌شود. هیچ هج، گرید، DCA یا افزایش حجمی وجود ندارد.
> **کول‌داون:** `C = 3` کندل روی همان نماد/جهت؛ بعد از دو خروج زیان‌ده پیاپی `C = 8`؛ بعد از یک چرخهٔ برنده به ۳ برمی‌گردد.

## ج.۳ فاز ۳ — فلیپ رژیم وسط چرخه (جایگزین ۵.۴)

1. فلیپ در حالی که چرخه **در سود** است → بستن کامل و فوری با قیمت بازار (بدون مبدل موج، بدون نگه‌داشتن باقی‌مانده).
2. فلیپ در حالی که چرخه **در ضرر** است → همان `S₀`/تریل معتبر است؛ کار اضافه‌ای نمی‌کنیم.
3. تأیید فلیپ: **۲ کندل پیاپی** رژیم مخالف با اجماع ≥ ۴.

## ج.۴ جدول کامل پارامترها (جایگزین ۵.۷)

| پارامتر | مقدار | تغییر نسبت به بخش الف |
|---|---|---|
| تایم‌فریم اجرا | H1 | — |
| `K` | `0.4 × ATR_blend` | **۱٫۰ → ۰٫۴** (مهم‌ترین پارامتر) |
| کف `K` | `3 × spread` | جدید |
| `S₀` | `1.5 × K` (= `1R`) | بازتعریف |
| کف `S₀` | `max(4×spread, 0.25×ATR)` | جدید |
| تریل | `max(0.2×ATR, 3×spread)` | جدید — منبع اصلی بهبود |
| `maxAdds` | ۱۸ (سقف ایمنی) | ۸ → ۱۸، و دیگر «بستن در سقف» نیست |
| حجم هر AP | ثابت = حجم پایه | — |
| هج / گرید فاز ۲ | **حذف** | ❌ |
| کول‌داون | ۳ کندل (۸ بعد از دو باخت) | ساده‌سازی |
| گیت ER | `ER(20) ≥ 0.35` | تأیید شد |
| گیت اجماع | `≥ 4` رأی هم‌جهت | تأیید شد |
| گیت نرخ فلیپ | حداکثر ۲ فلیپ در ۲۰ کندل | تأیید شد |
| گیت شتاب ER | `ΔER(5) > 0` | تأیید شد |
| ریسک هر چرخه | **۰٫۲۵٪ تا ۰٫۵٪ اکوییتی** | صریح شد |
| لایهٔ خودانتخابی نماد | ۳۶۵ روز / ≥۴۰ چرخه / `ΣR > 0` | جدید (ج.۵) |
| کیل‌سوییچ سراسری | درادون ۲۵٪ → توقف ۱ هفته | از ۵.۵.۱ بدون تغییر |

## ج.۵ لایهٔ خودانتخابی نماد (Walk-Forward Symbol Gate) — بخش کاملاً جدید

هر نماد **همیشه** کاغذی (paper) در موتور اجرا می‌شود، ولی سفارش واقعی فقط وقتی ارسال می‌شود که:

```
Σ R(همان نماد، ۳۶۵ روز گذشته) > 0   و   تعداد چرخهٔ آن پنجره ≥ 40
```

هر دو شرط فقط از **گذشته** حساب می‌شوند. نماد خاموش، خاموش نمی‌ماند: چون کاغذی ادامه می‌دهد، اگر رفتارش عوض شود دوباره روشن می‌شود.

| هزینه به‌ازای هر پا | بدون لایه | با لایه |
|---|---|---|
| ۰٫۷ پیپ | CAGR ۱۳۹٪ ، DD −۶۱٪ | CAGR ۱۸۹٪ ، DD **−۳۳٪** |
| ۱٫۵ پیپ | CAGR ۴۲٪ ، DD −۸۷٪ | CAGR ۱۴۵٪ ، DD **−۳۲٪** |
| ۲٫۵ پیپ | **CAGR −۲۶٪ (سوختن حساب)** | CAGR ۸۱٪ ، DD −۳۲٪ |

این یک بهینه‌سازی نیست؛ **شرط بقای سیستم در برابر بدتر شدن شرایط اجراست.**

---
---

# بخش د — یافته‌های عددی (نتیجهٔ اندازه‌گیری، نه فرض)

پیکربندی بخش ج، ۶ نماد، ۲۰۲۰-۰۱ تا ۲۰۲۶-۰۸، هزینهٔ واقع‌گرای ۱٫۵ پیپ در هر پا:

| ریسک هر چرخه | میانهٔ ماهانه | میانگین ماهانه | ماه مثبت | حداکثر درادون | CAGR |
|---|---|---|---|---|---|
| ۰٫۲۵٪ | **+۱٫۰٪** | +۴٫۴٪ | ۵۵٪ | −۱۷٫۵٪ | +۶۲٪ |
| ۰٫۵۰٪ | **+۱٫۹٪** | +۹٫۰٪ | ۵۵٪ | −۳۲٪ | +۱۴۵٪ |
| ۰٫۷۵٪ | +۲٫۶٪ | +۱۳٫۹٪ | ۵۵٪ | −۴۴٪ | +۲۵۰٪ |

**آمار رفتاری:** نرخ برد **۲۹٪** ، میانگین چرخه **+۰٫۵۱R** ، میانگین نگه‌داری **~۲ ساعت** ، **~۳۳ چرخه در ماه** روی کل سبد ، بزرگ‌ترین چرخه **+۹۹R** ، بدترین چرخه **−۶٫۷R** (گپ).

**سه هشدار که همیشه باید کنار این جدول بماند:**
1. **توزیع دُم‌کلفت است.** ۲۰۲۲ و ۲۰۲۴ (روند بزرگ ین و طلا) بیشتر سود را ساختند؛ سال‌های عادی ۲۵-۶۰٪. **۴۵٪ ماه‌ها منفی است.** عددی که باید رویش برنامه بریزی **میانهٔ ماهانه (۱ تا ۲٪)** است، نه میانگین.
2. **خوش‌بینی‌های باقی‌ماندهٔ شبیه‌سازی:** پرشدن دقیق روی سطح، بدون سوآپ، بدون گپ آخر هفته، بدون سقف مارجین، بدون محدودیت هم‌بستگی. انتظار داشته باش MT5 با تیک واقعی **۳۰ تا ۵۰٪ بدتر** دربیاید.
3. **قانون توقف:** اگر در فوروارد دمو میانگین `R` هر چرخه زیر **+۰٫۱۵** ماند، سیستم به حساب واقعی نمی‌رود — بدون بحث.

## د.۱ دو گلوگاه واقعی (و چیزهایی که گلوگاه نیستند)

**گلوگاه ۱ — هزینهٔ اجرا.** با ۱٫۵ پیپ در هر پا، سود نصف حالت ۰٫۷ پیپ است؛ با ۲٫۵ پیپ، بدون لایهٔ ج.۵ حساب می‌سوزد. **اهرم اصلی بهبود: بروکر با اسپرد تنگ‌تر، اجرای بهتر، و کاهش تعداد پاها — نه پیچیده‌تر کردن منطق.**

**گلوگاه ۲ — انتخاب نماد.** حل‌شده با لایهٔ ج.۵؛ باید روی نمادهای جدید و ناآزموده هم آزموده شود.

**چیزهایی که گلوگاه نیستند (پول و وقت اینجا نگذار):** موتور تشخیص روند دقیق‌تر (بدون پیرامید، همین موتور صفر است؛ سود از شکل توزیع می‌آید نه دقت جهت)، فیلترهای لحظه‌ای بیشتر، و هر مکانیزم «نجات» دیگری.

---
---

# بخش ه — وضعیت فعلی تصمیم، و پاسخ به سؤالات باز

> **یادداشت AI4:** این بخش در نسخهٔ اولیهٔ این سند «دفاع از گام بعدی» نام داشت — لحنی که می‌تونه به‌جای گزارش یافته، شبیه متقاعدکردن به نظر برسه. محتوا دست‌نخورده، فقط لحن و عنوان خنثی شد؛ این هنوز یک **فرضیهٔ در حال آزمایش** است، نه یک نتیجهٔ نهایی.

## ه.۱ تصمیم

> **کاری که الان باید انجام شود: پورت دقیق و کوچکِ بخش ج به MQL5، اجرای بک‌تست MT5 با «Every tick based on real ticks»، مقایسهٔ عدد‌به‌عدد با پیاده‌سازی مرجع پایتون، و بعد ۸ هفته فوروارد دمو با ریسک ۰٫۲۵٪.**
> هیچ قانون جدیدی به سیستم اضافه نمی‌شود تا وقتی این حلقه بسته شود.

(ترتیب اجرایی گام‌به‌گام از این سند حذف شد — این‌جا فقط یافته‌ها جمع می‌شوند، نه راهنمای اجرا. مهم‌ترین شرط توقف/ادامه‌اش: **اگر در جاروی هزینه، عدد در ۲٫۵ پیپ مثبت نماند، پورت به MQL5 معنی ندارد.**)

## ه.۲ پاسخ به پنج سؤال متداول

**«نرخ برد ۲۹٪ خیلی پایین است.»** نرخ برد در سیستم تحدبی بی‌معنی است. میانگین چرخه **+۰٫۵۱R** است چون برنده‌ها چند ده برابر بازنده‌ها هستند. بالا بردن نرخ برد فقط با بستن زودهنگام برنده‌ها ممکن است — دقیقاً همان اشتباه نسخهٔ ۴.

**«چرا بیمه (هج) را کامل حذف کردیم؟ ریسک بالا نمی‌رود؟»** نه. بیشینهٔ ضرر هر چرخه در نسخهٔ ۴ نامحدود-به-شرط-گرید بود؛ حالا `1R` سقف تعریف‌شده است (به‌جز گپ). هج نه ریسک را کم می‌کرد، نه سود می‌ساخت — فقط هزینه تولید می‌کرد (ب.۲).

**«این overfit است.»** سه سپر: (الف) تأیید در بازهٔ OOS (`+۰٫۲۳R` در بازهٔ دوم)، (ب) جاروی هزینه در سه سناریو، (ج) لایهٔ ج.۵ که فقط از گذشته تصمیم می‌گیرد و نماد را دستی انتخاب نمی‌کند. علاوه بر این، سیستم فقط **۵ عدد آزاد** دارد (`K`، `S₀`، تریل، `maxAdds`، کول‌داون) — سطح آزادی کم، ریسک overfit کم. **یادداشت AI4:** این سه سپر مفیدن ولی کافی نیستن — تناقض GBPNZD در ب.۶ نشون می‌ده حتی روی همین معماری، جمع‌بندی «کدوم نماد کار می‌کنه» هنوز بین دو تست مستقل یکی نشده؛ اثبات overfit-نبودن باید شامل حل این تناقض هم بشه.

**«CAGR ۱۴۵٪ باورکردنی نیست.»** درست است، و ما هم رویش برنامه نمی‌ریزیم. عدد قابل اتکا **میانهٔ ماهانه ۱ تا ۲٪** است، آن هم قبل از جریمهٔ ۳۰-۵۰٪ بابت تیک واقعی. جدول‌های بزرگ فقط نشان می‌دهند لبه وجود دارد، نه این‌که چقدر پول درمی‌آید.

**«چرا حالا کد بزنیم و باز تحقیق نکنیم؟»** چون اشتباه شمارهٔ ۱ همین پروژه (ب.۸) «فرض به‌جای اندازه‌گیری» بود، و تنها اندازه‌گیری باقی‌مانده‌ای که هنوز انجام نشده، **اجرا روی موتور واقعی MT5 با تیک واقعی** است. هر قانون جدیدی قبل از آن، دوباره همان اشتباه است. (با این حال، تناقض GBPNZD یک استثناست: قبل از کدنویسی هم قابل بررسی است — چون فقط مقایسهٔ دو نتیجهٔ پایتونیه، نه نیازمند MT5.)

## ه.۳ قوانین توقف (غیرقابل مذاکره)

- میانگین `R` زیر `+0.15` در دمو → حساب واقعی ممنوع.
- درادون ۲۵٪ حساب → کیل‌سوییچ، یک هفته توقف کامل.
- اختلاف > ۲۵٪ بین پایتون و MT5 → هیچ‌کدام قابل اعتماد نیست تا وقتی علتش پیدا شود.
- حجم هرگز به بالا گرد نمی‌شود: اگر حجم محاسبه‌شده از حداقل لات بروکر کمتر است، معامله **رد** می‌شود.

---
---

# بخش و — وضعیت دو کد MQL5 موجود: با نسخهٔ ۵ دقیقاً چه چیزی ساختنی است

> این بخش **ادعای «ساخته شد» ندارد.** هیچ‌کدام از این دو فایل کامپایل یا بک‌تست نشده‌اند. هدف این بخش این است که بگوید کدام قسمت‌های نسخهٔ ۵ در عمل قابل پیاده‌سازی‌اند، کدام قسمت‌ها در MQL5 دردسر دارند، و این دو پیش‌نویس چه چیزی را روشن کردند.

## و.۱ آن‌چه این دو پیش‌نویس نشان می‌دهند

| فایل | چه چیزی را پوشش داده |
|---|---|
| `AFA_v5_EA_AI1.mq5` | موتور رژیم پورت‌شده از v2.11، گیت‌های فاز صفر (اجماع، ER، نرخ فلیپ، ΔER)، پیرامید با `K=0.4×ATR` و کف اسپردی، `S₀=1.5K` با کف، تریل ratchet، `maxAdds=18`، سایزینگ ۰٫۵٪ با قانون «هرگز گرد به بالا نکن»، لایهٔ خودانتخابی نماد با تاریخچهٔ CSV، کیل‌سوییچ سراسری با GlobalVariable، فاز ۳ با تأیید ۲ کندلی |
| `AFA_v3.00_AI2.mq5` | همان موتور رژیم + همان مکانیزم پیرامید، به‌علاوهٔ سایزینگ دقیق‌تر بر پایهٔ `TICK_VALUE/TICK_SIZE` واقعی بروکر و یک دروازهٔ Walk-Forward نماد |

**نتیجهٔ مهم:** دو پیاده‌سازی مستقل، از دو مسیر جدا، به همان هستهٔ نسخهٔ ۵ رسیدند (پیرامید بدون هج، `K` کوچک، تریل با کف اسپردی، سایزینگ محافظه‌کار). این خودش یک cross-check است که خودِ **معماری** ابهام ندارد.

## و.۲ ایرادهایی که باید قبل از هر بک‌تستی رفع شوند

1. **عدم تطابق موتور رأی‌گیری با شبیه‌سازی.** شبیه‌سازی پایتون از ۶ رأی جانشین روی H1 استفاده کرد؛ هر دو EA از موتور واقعی ۵-رأیی روی H4 استفاده می‌کنند. **پس اعداد بخش د مستقیماً به این کد منتقل نمی‌شوند** — باید اول همان موتور EA در پایتون بازتولید شود، وگرنه مقایسهٔ عدد‌به‌عدد بی‌معنی است. این مهم‌ترین ایراد است.
2. **ریسک پیش‌فرض.** پیش‌فرض ۳٪ در پیش‌نویس دوم با بند ج.۴ (۰٫۲۵-۰٫۵٪) در تضاد است و با درادون‌های اندازه‌گیری‌شده حساب را می‌سوزاند. باید ۰٫۲۵٪ شود.
3. **سقف زمانی (`TimeCapBars`).** هر خروج زمانی، دُم سود را می‌کشد — همان اشتباه «سقف لوپ ۸». باید حذف یا خاموش شود.
4. **مشترک نبودن وضعیت بین چارت‌ها.** لایهٔ ج.۵، محدودیت هم‌بستگی، و درادون سراسری همه **سبد-محور** هستند، ولی هر EA روی چارت خودش زندگی می‌کند. مسیر GlobalVariable/فایل مشترک باید صریح و اتمیک باشد، وگرنه سه چارت هم‌زمان ریسک را سه برابر می‌کنند.
5. **حساب Netting.** روی حساب نتینگ، پاها در یک پوزیشن نت می‌شوند و کل منطق `n_adds`/تریل بی‌معنی می‌شود. باید در `OnInit` تشخیص داده شود و EA خودش را غیرفعال کند.
6. **قانون بدترین ترتیب درون‌کندلی.** در MT5 فقط با مدل «Every tick based on real ticks» معنی دارد؛ با مدل‌های سبک‌تر، نتیجه به‌طور مصنوعی بهتر درمی‌آید.
7. **سوآپ، کمیسیون و گپ آخر هفته** باید در تنظیمات تستر واقعی باشند، نه صفر.
8. **(افزودهٔ AI4) حداقل لات با ریسک پیش‌فرض پایین، طلا را کامل حذف می‌کند — دیدم، نه حدس.** روی یه تست واقعیِ حساب ۱۰۰ دلاری با کد خواهر همین دو فایل، با ریسک پیش‌فرض ۰٫۵٪، **۱۰۰٪ سیگنال‌های معتبر طلا** به حجمی کمتر از حداقل لات نیاز داشتن — یعنی طلا صفر معامله گرفت، نه چون رد بود، چون هیچ‌وقت فرصت داده نشد. این مستقیماً مورد ۲ (ریسک پیش‌فرض) رو تشدید می‌کنه: با ۰٫۲۵٪ این مشکل برای طلا **بدتر** می‌شه، نه بهتر. باید `AllowMinLotWithActualRiskCap` (که خودِ سند مقایسه هم پیشنهاد داده) قبل از هر بک‌تست چندنمادی پیاده بشه، وگرنه لایهٔ خودانتخابی نماد (ج.۵) هیچ‌وقت فرصت نمی‌کنه دربارهٔ طلا قضاوت کنه.

## و.۳ نقشهٔ «چه چیزی با نسخهٔ ۵ ساختنی است»

- ✅ **کاملاً ساختنی و ساده:** ورود تک‌پا با گیت‌ها، پیرامید با حجم ثابت، تریل ratchet با کف اسپردی، کول‌داون، فاز ۳، رد کردن معامله وقتی حداقل لات از بودجهٔ ریسک بزرگ‌تر است.
- ⚠️ **ساختنی ولی نیازمند دقت:** لایهٔ خودانتخابی نماد (نیاز به تاریخچهٔ پایدار و مشترک)، کیل‌سوییچ و درادون سراسری، محدودیت هم‌بستگی بین نمادها.
- ❌ **ساختنی نیست / نباید ساخته شود:** هج چرخشی، گرید فاز ۲، مبدل موج، هر TP ثابت، هر سقف زمانی.

**تعریف «تمام شد» برای این مرحله:** یک EA که روی حساب هجینگ کامپایل شود، روی هر ۶ نماد با تیک واقعی اجرا شود، میانگین `R` هر چرخه‌اش با پیاده‌سازی مرجع پایتون کمتر از ۲۵٪ اختلاف داشته باشد، و در تست فوروارد دمو `R̄ ≥ +0.15` بدهد. تا آن لحظه، این پروژه **هنوز یک فرضیهٔ اندازه‌گیری‌شده است، نه یک سیستم اثبات‌شده.**

---
---

# بخش ز — یافته‌های AI4 (Claude، در گفتگوی موازی با علی)

> **✅ معمای GBPNZD حل شد (بخش ز.۷، AI4 — بدون نیاز به AI3، چون کاملاً پایتونی بود).** خلاصه: «صفر چرخه» = همون باگ حداقل‌لات طلا، این‌بار روی GBPNZD. «منفی در برابر مثبت» = فاصلهٔ کیفیت واقعی بین موتور پروکسیِ ساده (Lovable) و موتور واقعیِ ۵رأیی (AI4/AI1/AI2) — اندازه‌گیری شد: توافق فقط ۶۷٪، فاصلهٔ دقت ۵ واحد در برابر ZigZag. جزئیات در ز.۷.

> این بخش مستقل از کاری که Lovable انجام داد نوشته شده — روی همون OHLC خام (۶ نماد، H1، ۲۰۲۰ تا ۲۰۲۶)، با یک پیاده‌سازی پایتونیِ جدا از قوانین نسخهٔ ۵. جاهایی که با بخش‌های بالا هم‌خونی داره، تأییده؛ جاهایی که تناقض داره، صریح علامت خورده (نه پنهان یا حذف).

## ز.۱ باگ K حین شوک نوسان — پیدا و رفع شد

اولین اجرای پیرامید (بدون فیکس) نتایج غیرواقعی داد: چرخه‌هایی با ۱۷-۱۸ بار افزودن پشت‌سرهم در کمتر از ۳ روز، بعضی‌هاشون تا +۴۰۸R. علتش: `K = 0.4×ATR_blend` وقتی نوسان ناگهانی (مثلاً کرش کووید فوریهٔ ۲۰۲۰) رخ می‌ده، چند ساعت عقب می‌مونه چون ATR کند میانگین می‌گیره — در همون فاصله، K بی‌خودی کوچیک می‌مونه و پیرامید منفجر می‌شه. **فیکس:** یه `ATR(3)` سریع‌واکنش هم به بلند اضافه شد: `ATR_blend = max(میانگین(ATR14,ATR50), ATR3)`. بعد از فیکس، بزرگ‌ترین چرخه از ۴۰۸R به ۲۸۵R اومد پایین و کل نتیجه واقع‌بینانه‌تر شد. **این دقیقاً همون خطریه که سند ب.۴ هم به‌صورت نظری ("K چند کندل عقب می‌مونه") پیش‌بینی کرده بود — اینجا با داده دیده شد.**

## ز.۲ حساسیت شدید به هزینهٔ اجرا — روی هر ۶ نماد

| نماد | ۰ پیپ | ۱٫۰ پیپ | ۱٫۵ پیپ | ۲٫۵ پیپ (بدترین) |
|---|---|---|---|---|
| XAUUSD | +2001 | +1441 | +1399 | **+1149 ✅** |
| GBPNZD | +1328 | +939 | +774 | **+551 ✅** |
| USDJPY | +2497 | +1183 | +802 | +101 ⚠️ |
| AUDCAD | +1159 | +403 | +176 | −389 ❌ |
| EURCHF | +993 | +137 | −354 | −847 ❌ |
| EURGBP | +772 | +99 | −213 | −814 ❌ |

(اعداد = جمع R روی کل بازه با ریسک ثابت هر پا؛ عدد مطلق قابل‌مقایسهٔ مستقیم با جدول‌های بخش د نیست چون فرمول متفاوته — ولی **رتبه‌بندی نسبی** نمادها قابل‌استفاده‌ست.)

این جدول با ب.۶/د یک تناقض (GBPNZD، در بخش ب.۶ برجسته شد) و بقیه‌ش هم‌خونی داره: EURGBP/EURCHF شکننده، XAUUSD قوی.

## ز.۳ باگ حداقل‌لات در تست حساب ۱۰۰ دلاری — قبلاً در بخش و.۲(۸) اضافه شد

خلاصه: با ریسک پیش‌فرض ۰٫۵٪، طلا در تست AI1/AI2 صفر معامله گرفت چون ۱۰۰٪ سیگنال‌هاش زیر حداقل لات بودن — نه چون رد شدن.

## ز.۴ سؤال باز برای AI1/AI2/AI3 — نه یه نتیجه، یه درخواست

سه‌تا چیز هست که AI4 نمی‌تونه به‌تنهایی حلش کنه، چون نیاز به مقایسهٔ مستقیم کد به کد داره:
1. **چرا GBPNZD بین دو تست مستقل برعکس شد؟** (ب.۶ در برابر ز.۲) — پیشنهاد: هر دو تیم فرمول دقیق K/تریل/هزینهٔ خودشون رو کنار هم بذارن.
2. **آیا معماری «پیرامید محض بدون هج» (بخش ج) واقعاً بهترین شکل حالت ۱ سند اصلیه، یا فقط بهترین چیزیه که تا الان تست شده؟** AI4 فقط دو نسخه (با/بدون فیکس K) رو تست کرده، نه کل فضای پارامتری.
3. **لایهٔ خودانتخابی نماد (ج.۵) با ریسک پیش‌فرض چقدر باید باشه که طلا رو ناعادلانه حذف نکنه؟** (وصل به و.۲.۸)

---

## ز.۵ بازخورد AI1 (دور اول بازبینی، ۱۵ اوت ۲۰۲۶)

**دربارهٔ GBPNZD (ز.۴.۱):** AI1 دیتای GBPNZD رو نداشت، پس عدد سوم نداد؛ ولی دو سرنخ منطقی داد. **سرنخِ محتمل‌تر:** باگ ATR-lag (ز.۱) احتمالاً روی نمادِ پرنوسان‌تر و کم‌نقدشونده‌تر (GBPNZD نسبت به EURGBP/EURCHF) اثر نامتناسب‌تری می‌ذاره — چون تست ب.۶ (Lovable) احتمالاً این فیکس رو نداشته. سرنخ دوم (تفاوت موتور رژیم) رو هم محتمل ولی کم‌وزن‌تر دونست. **یه فرضیه رو صریح رد کرد:** تفاوتِ صرفِ مدل‌سازیِ هزینه نمی‌تونه توضیح باشه — چون اگه ب.۶ هزینهٔ کمتری فرض کرده بود، باید GBPNZD رو *بهتر* نشون می‌داد، نه بدتر؛ جهتِ قضیه برعکسه.

**اعتراف مهم:** کد AI1 (`AFA_v5_EA_AI1.mq5`) و پیاده‌سازی پایتونی قبلیِ خودش هم فیکس ATR3 رو نداشتن. یعنی اعداد قبلی AI1 برای EURGBP (+۰٫۳۲R) و USDJPY (+۰٫۵۵R) هم به همین باگ آسیب‌پذیرن. **باید قبل از هر بک‌تست رسمی این فیکس به همهٔ پیاده‌سازی‌ها (پایتون و MQL5) اضافه بشه.**

**دربارهٔ ز.۴.۲ (آیا پیرامید محض بهترینه یا فقط بهترینِ‌تست‌شده):** «بهترینِ تست‌شده، نه سقف نظری» — فقط یه جدول ۳×۳ (K × تریل) جارو شده، نه بهینه‌سازی واقعی. جهتِ یافته قابل‌اتکاست، نقطهٔ دقیق بهینه نه.

**دربارهٔ ز.۴.۳ (ریسک پیش‌فرض برای طلا):** رابطهٔ کیفی داد، نه عدد ثابت: مشکل وقتی رخ می‌ده که `حداقل‌لات × S₀ × ارزش‌هرپیپ > ریسک‌هدف` — و چون S₀ طلا بزرگ‌تره، زودتر فعال می‌شه. راه‌حل باید ساختاری باشه (ریسک واقعی محاسبه و لاگ بشه)، نه یه درصد ثابت حدسی.

**سه تناقض/نقص جدید که AI1 پیدا کرد:**
- **لایهٔ همبستگی سراسری** توی بخش الف بود ولی جدول ج.۴ («جایگزین ۵.۷») ردیفی براش نداره — مشخص نیست هنوز جزو نسخهٔ ۵ هست یا حذف شده.
- **«پیاده‌سازی مرجع پایتون» دیگه یکی نیست** (توضیح داده شد، بالای همین بخش).
- **و.۲.۴ («وضعیت مشترک بین چارت‌ها») دو چیز متفاوت رو قاطی کرده:** لایهٔ خودانتخابی نماد (ج.۵) ذاتاً تک‌نمادیه و نیازی به هم‌اشتراکی بین چارت‌ها نداره؛ فقط کیل‌سوییچ سراسری و همبستگی واقعاً سبد-محورن. راه‌حل فنی‌شون فرق می‌کنه (فایل per-symbol برای ج.۵، GlobalVariable برای بقیه).

**بازبینی موارد و.۲ (ایرادهای کد) توسط AI1:**
- **#۱ (عدم تطابق موتور رأی‌گیری):** موافق، تا حدی حل‌شده — AI1 موتور واقعی ۵-رأی رو پورت و روی EURGBP/USDJPY اجرا کرده (نه پروکسی)، ولی هنوز فیکس ATR3 رو نداره.
- **#۲ (ریسک ۳٪):** موافق، ولی مال فایل AI2ه — پیش‌فرض خودِ AI1 از اول ۰٫۵٪ بوده.
- **#۳ (TimeCapBars):** موافق در اصل، ولی مال فایل AI1 نیست — AI1 هیچ سقف زمانی نداره.
- **#۴ (وضعیت مشترک):** جزئی مخالف — توضیح بالا (تفکیک ج.۵ از سبد-محورها).
- **#۵ (Netting باید INIT_FAILED بشه):** **مخالفِ مستدل** — نسخهٔ ۵ برخلاف نسخهٔ ۴ هیچ هج هم‌زمان دوجهته نداره (همهٔ پاها هم‌جهتن) و ورودی هر پا از خودِ EA خونده می‌شه نه از پوزیشن؛ پس منطقاً باید حتی روی Netting هم درست کار کنه. **این تحلیله، نه تست** — AI1 خودش می‌گه رد کردن کامل Netting بدون تست، خودش یه فرض بدون اندازه‌گیریه (اشتباه شمارهٔ ۱ در ب.۸). **وضعیت: مورد مناقشه، نیاز به تست واقعی روی حساب Netting دارد.**
- **#۶ (بدترین‌ترتیب فقط با Every Tick):** کاملاً موافق.
- **#۷ (سوآپ/کمیسیون واقعی):** موافق.
- **#۸ (حداقل‌لات طلا):** کاملاً موافق و از قبل در کد خودش رفع‌شده (`InpAllowMinLotWithActualRiskCap`) — قبل از دیدن این سند، مستقل از AI2 و AI4 به همین نتیجه رسیده بود. **سه منبع مستقل روی یه یافته همگرا شدن — اطمینان بالا.**

**جمع‌بندی AI1:** بزرگ‌ترین کمکِ این دور، خودِ فیکس ATR3 (ز.۱) بود — هم توضیح محتمل تناقض GBPNZD، هم یه گپ واقعی در کد AI1 که باید بسته بشه.

---

## ز.۶ بازخورد AI2 (دور دوم بازبینی، ۱۵ اوت ۲۰۲۶) — فرضیهٔ AI1 عملاً تست و رد شد

**نکتهٔ روش‌شناختی اول:** AI2 فرمول‌های K/S₀/تریلش رو دقیقاً از `afa_v5_backtest.py` (همون اسکریپتی که علی مستقیم به هر سه تیم داد) گرفته — و همین اسکریپت رو مستقیم اجرا کرده. نتیجه: **در `afa_v5_backtest.py`، GBPNZD صفر چرخه داشت** — نه مثبت، نه منفی، هیچ. یعنی معما ۳ حالت نبود، ۴ حالته:

| منبع | نتیجهٔ GBPNZD |
|---|---|
| `afa_v5_backtest.py` (منبع مشترک هر سه تیم) | **صفر چرخه** |
| ب.۶ (Lovable) | منفی |
| ز.۲ (AI4) | +۵۵۱R (بدترین سناریوی هزینه) |
| شبیه‌سازی ترکیبی AI2 (موتور واقعی + پیرامید) | +۰٫۳۷R (معتدل، هم‌ردیف بقیهٔ نمادها) |

**فرضیهٔ AI1 (ز.۵) — باگ ATR-شوک (ز.۱) علت اصلیه — با تست واقعی رد شد:** AI2 دقیقاً همون تست رو با و بدون فیکس ATR(3) روی هر ۶ نماد اجرا کرد. برای GBPNZD، اختلاف ناچیز بود (+۰٫۳۷۱ در برابر +۰٫۳۶۵ — عملاً یکسان). **این فرضیه دیگه توضیح اصلی نیست.**

**مظنون جدید:** جزئیات گیت ورود و/یا تأیید فلیپ‌سودده — نه فرمول K/تریل. AI2 صراحتاً اعتراف کرد شبیه‌سازی خودش شرط «تأیید دو کندل متوالی» که `afa_v5_backtest.py` برای فلیپ‌سودده داره رو پیاده نکرده بود (فقط تک‌کندلی). **پیشنهاد AI2 (به‌جای فرض یک متغیر): یک جدول مقایسهٔ گیت‌به‌گیت (اجماع، ER، شیب ER، نرخ فلیپ، تأیید فلیپ‌سودده، مدل اسپرد) بین هر چهار پیاده‌سازی.**

### چهار باگ/نقص جدید که AI2 پیدا کرد

- **ناهم‌خوانی آستانه/بازنشانیِ کیل‌سوئیچ در کد خودش:** آستانهٔ ۲۵٪ (که باید طبق ه.۳ یک هفته توقف بده) با منطق بازنشانیِ «روز بعد» (که مال آستانهٔ روزانهٔ ۳٪ است) جفت شده — خودِ کد AI2 بین این دو ناهم‌خوانه.
- **سقف همبستگی نه در کد AI1 هست، نه در کد AI2.** بخش الف/۵.۵.۱ این لایه رو داشت، ولی هیچ‌کدوم از دو پیش‌نویس MQL5 پیاده‌ش نکردن.
- **GlobalVariable در کد AI2 اتمیک نیست** — race condition واقعی (نه فرضی): چون MT5 هر نماد رو در ترد جدا اجرا می‌کنه، دو نماد می‌تونن هم‌زمان بودجهٔ ریسک سراسری رو چک کنن، هر دو «مجاز» ببینن، و هر دو وارد بشن.
- **Netting در کد AI2 فقط هشدار می‌ده، معامله رو بلاک نمی‌کنه** — دقیقاً برخلاف چیزی که و.۲.۵ می‌خواد.

### به‌روزرسانی و.۲ با پاسخ AI2

- **#۳ (TimeCapBars باید حذف بشه):** **مخالفت جزئی، با شاهد.** از ۲۹۱۲ معاملهٔ `trades_proxy.csv`، **صفر تا** با سقف زمانی بسته شدن (همه با استاپ/تریل). برخلاف سقف لوپ ۸ نسخهٔ ۴ (که طبق ب.۴ مرتب فعال می‌شد و دُم رو می‌کشید)، این سقف در دادهٔ واقعی فعلاً هیچ‌وقت لمس نشده. پیشنهاد: به‌جای حذف، اثرش با فلگ روشن/خاموش جدا اندازه‌گیری بشه.
- **#۶، #۷:** موافق، ولی یادآوری می‌کنه این‌ها تنظیمات Strategy Tester‌ان، نه چیزی که کد می‌تونه تضمین کنه.
- بقیهٔ موارد (#۱، #۲، #۵، #۸) را کاملاً پذیرفت و به‌عنوان یافته ثبت کرد (بدون اجرا).

## ز.۷ حل معمای چهاروجهیِ GBPNZD (AI4، بدون نیاز به AI3)

AI3 در اجرا کند/گیر افتاده بود؛ چون کل این بررسی صرفاً پایتونی بود (نیازی به کد MQL5 یا داوری بیرونی نداشت)، AI4 مستقیم انجامش داد.

**علت #۱ («صفر چرخه» در `afa_v5_backtest.py`):** تأیید شد — **دقیقاً همون باگ حداقل‌لات طلا (و.۲.۸)، این‌بار روی GBPNZD.** با ریسک پیش‌فرض ۰٫۵٪ روی حساب ۱۰۰ دلاری، ۱۰۰٪ سیگنال‌های معتبر GBPNZD به حجمی کمتر از ۰٫۰۱ لات نیاز داشتن. یه باگ، نه یک پدیدهٔ جدا.

**علت #۲ («منفی» ب.۶ در برابر «مثبت» ز.۲/ز.۶):** `afa_v5_backtest.py` از یک موتور پروکسیِ عمومی (کراس SMA/EMA، شکست Donchian، علامت ROC، RSI، قیمت-نسبت-به-میانه — ۶ اندیکاتور کلاسیک ساده) استفاده می‌کند، **نه** موتور واقعی و اعتبارسنجی‌شدهٔ نسخهٔ ۵ (ADX-تأییدی + دو Supertrend + ساختار Swing). مقایسهٔ مستقیم روی GBPNZD:

| سنجه | مقدار |
|---|---|
| توافق جهت بین دو موتور | فقط ۶۷٪ |
| دقت موتور پروکسی در برابر ZigZag(۳٪) | ۶۰٫۷٪ |
| دقت موتور واقعیِ ۵رأیی در برابر همان ZigZag | ۶۵٫۵٪ |

فاصلهٔ دقت ۵ واحدی + عدم‌توافق ۳۳٪ برای یک سیستم پیرامیدیِ حساس به کیفیت لحظهٔ ورود (طبق ز.۱) کاملاً کافی‌ست که علامت نتیجه عوض بشه. فاصلهٔ کوچک باقی‌مانده بین ز.۲ (AI4) و ز.۶ (AI2 — که هردو از موتور واقعی استفاده کردن ولی هنوز کمی فرق دارن) به جزئیات گیت ورود/تأیید فلیپ برمی‌گرده، همون‌طور که AI2 حدس زده بود — این باقیمانده کوچیکه و نیاز به پیگیری فوری نداره.

**نتیجهٔ عملی (مهم‌تر از خودِ عدد):** هر تحلیل بعدی — از جمله جدول احتمال ۲۰۰/۳۰۰/۵۰۰ دلاریِ علی — باید حتماً از موتور واقعی ۵رأیی استفاده کنه، نه پروکسی؛ نشون داده شد پروکسی به‌طور قابل‌اندازه‌گیری ضعیف‌تره.

**وضعیت این مسیر تحقیق: بسته.** نیازی به بازخورد بیشتر از AI3 دربارهٔ GBPNZD نیست.

---
---

# بخش ح — نتایج واقعی MT5 (نه شبیه‌سازی پایتون) — علی + AI2، پورت و تست شده تا v5.07

> **⚠️ این مهم‌ترین به‌روزرسانی این سند تا امروزه.** همهٔ نتایج مثبت بخش‌های قبلی (ز.۲، ز.۶، جدول ب.۶) از شبیه‌سازی پایتون بودن. این بخش اولین **اجرای واقعی MT5** با تیک واقعی است — و نتیجه منفیه. این باید اولویت پروژه رو عوض کنه: قبل از هر بهبود دیگه، باید فهمید چرا پایتون مثبت و MT5 واقعی منفی می‌ده.

## ح.۱ باگ‌های اجرا که پیدا و رفع شدن (v5.00 تا v5.07)

کد از v5.00 تا v5.07 رفت جلو چون این باگ‌های واقعی (نه فرضی) پیدا شدن:
- **Kill Switch مدیریت پوزیشن باز رو متوقف می‌کرد** (v5.01) — رفع شد.
- **بن‌بست Cooldown دائمی:** `g_cooldownLeft` موقع بستن چرخه مقداردهی می‌شد ولی هیچ‌وقت هر کندل کم نمی‌شد → قفل همیشگی بعد اولین چرخهٔ زیان‌ده (v5.02) — رفع شد.
- **ارزش تیک اشتباه در ارزهای متقاطع:** `TickValue/TickSize` برای USDJPY درست تبدیل به دلار نمی‌کرد؛ حجم واقعی خیلی کوچیک‌تر از هدف بود (v5.03) — با `OrderCalcProfit` رفع شد.
- **قاطی‌شدن واحد خام Cent با دلار اقتصادی** (۱۰٬۰۰۰ واحد خام ≠ ۱۰٬۰۰۰ دلار) — منبع اصلی خطای اولیهٔ سایزبندی (v5.04) — با `InpAccountUnitScale` رفع شد.
- **سمت اشتباه قیمت در اعتبارسنجی Stop:** BUY باید با Bid، SELL باید با Ask چک بشه؛ استفاده از سمت اشتباه باعث رد شدن دستور توسط بروکر می‌شد با اینکه clamp داخلی «درست» به‌نظر می‌رسید (v5.05) — رفع شد.
- **`SL_ALREADY_TRIGGERED` به‌عنوان یه حالت طبیعی state machine شناخته نشده بود** — وقتی Trailing SL از قیمت بازار رد شده باشه، clamp کردنش یه سطح جعلی می‌سازه و خطای `Invalid Stops` می‌ده (v5.06-v5.07) — با یه enum جدید (`SYNC_OK`/`SYNC_DEFERRED`/`SYNC_REJECTED`/`ALREADY_TRIGGERED`) و منطق اولویت‌دهی به بدترین وضعیت، رفع شد.

**نتیجه:** تا v5.07، لایهٔ اجرای سفارش (نه استراتژی) از نظر فنی سالمه — صفر Invalid Stops، صفر deadlock.

## ح.۲ نتیجهٔ baseline واقعی — منفیه

تست روی USDJPY و GBPNZD، H1، ۲۰۲۵٫۰۱٫۰۱ تا ۲۰۲۶٫۰۸٫۱۲، مدل «Every tick based on real ticks»، ریسک هر چرخه ۰٫۵٪:

| شاخص | USDJPY | GBPNZD | مجموع |
|---|---:|---:|---:|
| چرخه | ۷۹ | ۵۸ | ۱۳۷ |
| Win Rate | ۲۶٫۶٪ | ۱۲٫۱٪ | ۲۰٫۴٪ |
| Mean R | −۰٫۴۶ | −۰٫۶۷ | ≈−۰٫۵۵ |
| Profit Factor | ۰٫۳۷ | ۰٫۲۰ | ۰٫۲۹ |
| PnL اقتصادی | −۱۸٫۸۸$ | −۱۹٫۰۹$ | −۳۷٫۹۷$ |

**تعداد معامله هم خیلی پایینه:** فقط ~۷٫۲ چرخه در ماه برای هر دو نماد با هم — خیلی دور از هدف ۲۰۰ معاملهٔ مانیفست. علت اصلی طبق شمارندهٔ گیت‌ها: **Range، Cooldown، Consensus، Slope** بیشترین فرصت رو رد می‌کنن — نه حداقل‌لات یا حجم (که قبلاً حل شده بودن).

**تناقض با بخش‌های قبلی این سند:** ز.۲ (AI4) و ز.۶ (AI2) هردو با شبیه‌سازی پایتون روی همین نمادها (کم‌وبیش) نتیجهٔ **مثبت** گرفته بودن. این تناقض حل‌نشده و از هر چیز دیگه‌ای در این سند مهم‌تره.

## ح.۳ کشف کلیدی: خروج قبل از حرکت اصلی

تحلیل OHLC روی معاملات بازنده نشون داد بخش بزرگی از بازنده‌ها **بعد از خروج، در همون جهت معامله ادامه دادن**:

| افق بعد از خروج | USDJPY (بازنده‌ها) | GBPNZD (بازنده‌ها) |
|---|---:|---:|
| ۶ ساعت | ۵۰٫۰٪ | ۶۰٫۸٪ |
| ۲۴ ساعت | ۷۷٫۶٪ | ۷۲٫۵٪ |
| ۲۴ ساعت، حداقل +۱R | ۵۱٫۷٪ | ۵۴٫۹٪ |
| ۲۴ ساعت، حداقل +۲R | ۳۴٫۵٪ | ۴۳٫۱٪ |

زمان نگهداری هم خیلی کوتاهه (میانه ۱-۲ ساعت). **تفسیر محتاطانه (نه قطعی):** این لزوماً به‌معنی «همهٔ Stopها رو حذف کن» نیست — بخشی از معاملات واقعاً کاملاً خلاف جهت رفتن. ولی شواهد کافیه که فاصله/فعال‌سازی Trail و S0 باید با داده دقیق‌تر بررسی بشن، نه فقط فرمول ATR فعلی.

## ح.۴ چارچوب تشخیص: مشکل از سیگنال، خروجه، یا گیت؟

سه‌تا باید از هم جدا بشن:
- **مشکل سیگنال:** ورود اصلاً edge نداره (قیمت اول خلاف می‌ره، به S0 می‌خوره، بعدش هم ادامهٔ معنی‌دار نمی‌ده).
- **مشکل خروج:** قیمت یه‌کم موافق می‌ره، بعد Trail می‌بندتش، بعد از خروج ادامه می‌ده → فاصله/فعال‌سازی Trail زودهنگامه.
- **مشکل گیت:** گیت‌ها اگه ۹۰٪ فرصت‌ها رو حذف کنن، نمونه برای بهره از دُم سود کافی نیست — ولی شل‌کردن بدون حفظ کیفیت هم خطرناکه (دقیقاً همون درس StructOK از V7).

## ح.۵ نقشهٔ راه اصلاح (مرحله‌ای، هرکدوم تک‌متغیره)

۰) تثبیت baseline (بدون تغییر) → ۱) پچ diagnostic (ثبت MFE/MAE/Hold Time/FirstBlockingGate، بدون تغییر منطق) → ۲) Test A (فقط diagnostic) → ۳) Test B (فقط `TrailActivationR=0.5`) → ۴) گیت‌ها تک‌به‌تک (Cooldown، Consensus، MinER) → ۵) هندسهٔ K/S0/Trail → ۶) فیکس ATR3/lag (که ما هم مستقل بهش رسیده بودیم، ز.۱) → ۷) Energy جدا از EA تست بشه (معیار پذیرش سخت‌گیرانه: ≥۵ واحد درصد بهبود دقت، پوشش ≥۲۰٪، مثبت روی ≥۵ از ۶ نماد) → ۸) M15/M1 فقط بعد از اثبات H1 → ۹) فوروارد دمو.

**رد فوری (خط قرمز صریح):** نگاه‌به‌آینده، حذف Stop برای پنهان‌کردن ضرر، Recovery/DCA بدون بودجهٔ مستقل، افزایش MaxAdds فقط برای رسیدن به سود هدف، تغییر همزمان ۵ چیز بدون امکان attribution، بهبود فقط روی یه نماد/بازه، تغییر موتور رژیم بدون تست مستقل.

## ح.۶ سه mode آیندهٔ مستقل (هنوز پیاده نشدن، فقط طراحی)

- **معمولی:** ریسک پایین‌تر، گیت سخت‌تر، توقف بعد افت مشخص.
- **تهاجمی:** گیت‌های قابل‌اندازه‌گیری شل‌تر، سایزبندی بر اساس edge نه فقط رأی، سقف ریسک روزانه/هفتگی مستقل.
- **ضدلیکویید:** ریسک چرخه کمتر، توقف کامل Add در افزایش اسپرد/افت equity، انتقال خودکار به دفاعی در drawdown.

هر mode باید state machine و لاگ مستقل داشته باشه، نه فقط ورودی‌های متفاوت روی همون منطق.

---

**آخرین به‌روزرسانی این سند:** توسط AI4، بعد از بررسی خروجی Lovable، بازخورد AI1، بازخورد AI2، حل مستقل معمای GBPNZD، و افزودن نتایج واقعی MT5 (v5.00-v5.07، علی+AI2).
**در انتظار:** حل تناقض «پایتون مثبت / MT5 واقعی منفی» (اولویت اول)؛ اجرای Test A/B طبق ح.۵؛ بازخورد AI3 (اگه برگرده).
